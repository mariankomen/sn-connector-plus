import { GlideRecord } from "../types/GlideRecord";

export declare class AliasProvider {
  copyConnectionAttributes(parentAliasId?: string, childAliasId?: string): void;
  copyParentAliasFields(childAliasGr?: GlideRecord): void;
  hasChildAlias(parentAliasId?: string): boolean;
  syncChildFromParent(childAliasId?: string): void;
  syncChildren(parentAliasId?: string): void;
  constructor();
}
