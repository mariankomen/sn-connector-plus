import { AddressFilterResult } from "./AddressFilterResult";

export declare class AddressFilter {
  static filter(
    emailAddressFilterIds?: string,
    address?: string
  ): AddressFilterResult;
}
