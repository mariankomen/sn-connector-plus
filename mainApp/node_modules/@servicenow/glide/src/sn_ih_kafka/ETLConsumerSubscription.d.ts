import { GlideRecord } from "../types/GlideRecord";

export declare class ETLConsumerSubscription {
  activate(consumer?: GlideRecord): boolean;
  deactivate(consumer?: GlideRecord): boolean;
  constructor();
}
