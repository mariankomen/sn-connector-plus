import { GlideRecord } from "../types/GlideRecord";

export declare class ContinuousAuthenticationUtil {
  getContinuousAuthenticationPolicyForDataClass(
    classificationSysId: Array<string>
  ): Map<string, GlideRecord>;
  isContinuousAuthenticationAppliedToDataClass(
    classificationSysId: Array<string>
  ): Map<string, boolean>;
  static manageACLForPolicy(policyGR: GlideRecord): void;
  constructor();
}
