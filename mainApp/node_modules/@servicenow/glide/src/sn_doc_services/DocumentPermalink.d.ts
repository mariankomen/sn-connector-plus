export declare class DocumentPermalink {
        accessType(accessType: string): DocumentPermalink
        active(active: boolean): DocumentPermalink
        documentId(documentId: string): DocumentPermalink
        expiration(expiration: string): DocumentPermalink
        linkId(linkId: string): DocumentPermalink
        sysId(sysId: string): DocumentPermalink
        type(type: string): DocumentPermalink
        constructor()

}