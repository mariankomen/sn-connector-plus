import { DocumentPermalink } from "./DocumentPermalink";

export declare class DocumentPermalinkAPI {
  cleanExpiredPermalinks(): void;
  createPermalink(sysPermalink: DocumentPermalink): any;
  deletePermalink(permalinkSysId: string): any;
  getPermalinkInfo(linkId: string): any;
  getPermalinkInputStream(
    permalinkId: string,
    permalinkProperties: Map<string, string>
  ): any;
  getPermalinks(documentSysId: string): any;
  updatePermalink(sysPermalink: DocumentPermalink): any;
  constructor();
}
