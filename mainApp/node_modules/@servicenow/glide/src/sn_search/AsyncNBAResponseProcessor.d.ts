import { GeniusResultAnswer } from "../sn_ais/GeniusResultAnswer";

export declare class AsyncNBAResponseProcessor {
  handleAnswer(
    handlerAnswer?: GeniusResultAnswer,
    metadata?: Record<any, any>
  ): void;
  processCancellation(metadata?: Record<any, any>, errorMessage?: string): void;
  processError(metadata?: Record<any, any>, errorMessage?: string): void;
  constructor();
}
