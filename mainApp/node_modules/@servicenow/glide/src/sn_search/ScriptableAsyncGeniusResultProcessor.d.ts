import { GeniusResultAnswer } from "../sn_ais/GeniusResultAnswer";

export declare class ScriptableAsyncGeniusResultProcessor {
  handleAnswer(
    geniusResultAnswer?: GeniusResultAnswer,
    metadata?: Record<any, any>
  ): void;
  processCancellation(metadata?: Record<any, any>): void;
  processError(metadata?: Record<any, any>, errorMessage?: string): void;
  constructor();
}
