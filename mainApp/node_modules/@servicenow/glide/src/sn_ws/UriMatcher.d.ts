import { UriMatcherResponse } from "./UriMatcherResponse";

export declare class UriMatcher {
  match(
    uri?: string,
    scheme?: string,
    hosts?: Array<any>,
    paths?: Array<any>,
    fragments?: Array<any>,
    disallowQueryParameters?: boolean
  ): UriMatcherResponse;
  constructor();
}
