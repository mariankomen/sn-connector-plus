import { GlideRecord } from "../types/GlideRecord";

export declare class RecordStateValidator {
  static isValid(gr: GlideRecord): boolean;
  static isValidStateTransition(gr: GlideRecord, newState: string): boolean;
}
