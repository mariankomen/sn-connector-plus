import { GlideRecord } from "../types/GlideRecord";

export declare class UCSkippedRecordsTaskBoard {
  static syncUpgradeHistoryLogRecord(
    current?: GlideRecord,
    previous?: GlideRecord
  ): void;
  static syncUpgradeHistoryTaskRecord(
    current?: GlideRecord,
    previous?: GlideRecord
  ): void;
}
