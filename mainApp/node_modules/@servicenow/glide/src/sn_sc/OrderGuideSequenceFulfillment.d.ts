import { GlideRecord } from "../types/GlideRecord";

export declare class OrderGuideSequenceFulfillment {
  static fulfillSequencedOGItem(
    request?: GlideRecord,
    item?: GlideRecord
  ): GlideRecord;
}
