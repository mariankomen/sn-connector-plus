import { PackageDefinition } from "./PackageDefinition";

export declare class PackageStateGenerator {
  /**
   * Generates the package state and returns a wrapped PackageDefinitionScript.
   */
  generatePackageState(): PackageDefinition;

  /**
   * Sets a parameter value for the state generator.
   */
  setParameterValue(arg0: string, arg1: string): void;
}
