import { GlideRecord } from "../types/GlideRecord";

export declare class DeliveryContext {
  getEvent(): GlideRecord;
  getExecution(): GlideRecord;
  getNotification(): GlideRecord;
  getTarget(): GlideRecord;
  constructor();
}
