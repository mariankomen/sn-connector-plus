import { GlideRecord } from "../types/GlideRecord";

export declare class DelegationMapper {
  cleanAndRemapRecord(target: GlideRecord, delegationType: string): void;
  mapDelegate(delegationDefinition: GlideRecord): void;
  mapDelegatesForRecord(target: GlideRecord, delegationType: string): void;
  mapRule(delegationDefinitionRule: GlideRecord): void;
  remapDelegationRecord(
    delegateRecord: GlideRecord,
    changedField: string
  ): void;
  remapUpdatedRecord(record: GlideRecord): void;
  constructor();
}
