import { Outcome } from "./Outcome";
import { GlideRecord } from "../types/GlideRecord";
import { Solution } from "./Solution";

export declare class SolutionStats {
  static recordPredictValues(outcome?: Outcome, solution?: Solution): boolean;
  static recordPredictValuesTopN(
    outcomes?: Array<any>,
    solution?: Solution
  ): boolean;
  static updateFinalValues(
    recordFinalValue?: GlideRecord,
    reason?: any
  ): boolean;
}
