import { GlideRecord } from "../types/GlideRecord";

export declare class MLRecordUtil {
  static getFieldType(tableName?: string, fieldName?: string): string;
  static getNextSolutionVersion(id?: string): number;
  static populateMetadata(
    options?: Record<any, any>,
    solutionGR?: GlideRecord
  ): Record<any, any>;
  static updateSolutionProperties(
    solutionGr?: GlideRecord,
    properties?: Record<any, any>
  ): boolean;
}
