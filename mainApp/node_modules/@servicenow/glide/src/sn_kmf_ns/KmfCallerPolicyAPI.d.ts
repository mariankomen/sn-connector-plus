import { GlideRecord } from "../types/GlideRecord";

export declare class KmfCallerPolicyAPI {
  addModuleDefaultValue(currentModuleGr: GlideRecord): void;
  addTrackCallerPolicy(currentModuleGr: GlideRecord): void;
  constructor();
  getMatchingResourceExchangeModuleAccessPolicy(
    cryptoSpecId: string,
    policyStatus: string,
    instanceRecipient: string
  ): string;
  insecureSetToTrack(moduleSysId: string): boolean;
  setToReject(moduleSysId: string): boolean;
}
