import { GlideRecord } from "../types/GlideRecord";

export declare class ProducerSetBuilder {
  bidirectional(biDirectional?: boolean): ProducerSetBuilder;
  build(): GlideRecord;
  description(description?: string): ProducerSetBuilder;
  discrete(discrete?: boolean): ProducerSetBuilder;
  entrySet(entrySet?: GlideRecord): ProducerSetBuilder;
  getErrors(): Array<any>;
  hasErrors(): boolean;
  isLegacy(isLegacy?: boolean): ProducerSetBuilder;
  name(name?: string): ProducerSetBuilder;
  static withName(name?: string): ProducerSetBuilder;
  static withSysId(sysId?: string): ProducerSetBuilder;
  constructor();
}
