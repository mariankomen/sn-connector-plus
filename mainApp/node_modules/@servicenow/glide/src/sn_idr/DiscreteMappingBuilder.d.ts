import { GlideRecord } from "../types/GlideRecord";

export declare class DiscreteMappingBuilder {
  build(): GlideRecord;
  discreteValue(discreteValue?: string): DiscreteMappingBuilder;
  getErrors(): Array<any>;
  hasErrors(): boolean;
  outboundEntry(outboundEntry?: GlideRecord): DiscreteMappingBuilder;
  referencedTable(referencedTable?: string): DiscreteMappingBuilder;
  subscription(subscription?: GlideRecord): DiscreteMappingBuilder;
  static withSysId(sysId?: string): DiscreteMappingBuilder;
  static withValues(
    discreteValue?: string,
    referencedTable?: string,
    outboundEntry?: GlideRecord,
    subscription?: GlideRecord
  ): DiscreteMappingBuilder;
  constructor();
}
