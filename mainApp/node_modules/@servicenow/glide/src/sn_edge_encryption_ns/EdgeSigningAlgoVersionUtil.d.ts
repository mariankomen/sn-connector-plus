import { GlideRecord } from "../types/GlideRecord";

export declare class EdgeSigningAlgoVersionUtil {
  edgeProxySignatureVersionChange(
    current: GlideRecord,
    previous: GlideRecord
  ): void;
  getAdjustedProxySignatureAlgorithmVersion(
    unadjustedSignatureVersion: number,
    isProxyFIPSModeEnabled: boolean
  ): number;
  getMinimumRequiredSignatureAlgorithmVersion(): number;
  getOtherSupportedOnlineSignatureAlgoVersion(
    excludeThisProxyGuid: string
  ): number;
  constructor();
}
