export declare class GlideMemoryRecord {
  /**
   * Returns the value for the specified field name.
   */
  getValue(name: string): string;

  /**
   * Returns the display value for the specified field name.
   */
  getDisplayValue(name: string): string;

  /**
   * Returns the unique value (usually sys_id) for this record.
   */
  getUniqueValue(): string;
}
