import { GlideRecord } from "./GlideRecord";

export declare class GlideAppInstallValidationHelper {
  constructor();
  getMessage(): string;
  shouldInstallOrUpgrade(
    storeApp: GlideRecord,
    version: string,
    isReinstall: boolean
  ): boolean;
  shouldInstallOrUpgradeCustomization(
    app: GlideRecord,
    version: string,
    isReinstall: boolean
  ): boolean;
  shouldInstallOrUpgradeStoreApp(
    app: GlideRecord,
    version: string,
    isReinstall: boolean
  ): boolean;
}
