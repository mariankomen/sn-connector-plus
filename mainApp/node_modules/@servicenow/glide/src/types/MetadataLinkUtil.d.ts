import { GlideRecord } from "./GlideRecord";

export declare class MetadataLinkUtil {
  static isMetadataLinkExempt(record: GlideRecord): boolean;
  static isTableMetadataLinkExempt(tableName: string): boolean;
}
