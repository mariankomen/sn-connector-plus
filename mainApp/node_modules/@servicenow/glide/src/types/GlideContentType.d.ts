import { GlideRecord } from "./GlideRecord";

export declare class GlideContentType {
  /**
   * Returns the detail page for the given GlideRecord.
   */
  getDetailPage(current: GlideRecord): string;

  /**
   * Returns a GlideContentType instance for the given content GlideRecord.
   */
  static get(content: GlideRecord): GlideContentType;
}
