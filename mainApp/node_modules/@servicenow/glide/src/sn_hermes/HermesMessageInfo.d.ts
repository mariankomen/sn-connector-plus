import { GlideDateTime } from "../types/GlideDateTime";

export declare class HermesMessageInfo {
  getHeaders(): string;
  getKey(): string;
  getOffset(): Record<any, any>;
  getPartition(): number;
  getPayload(): string;
  getTimestamp(): GlideDateTime;
  isKeyError(): boolean;
  isPayloadError(): boolean;
  constructor();
}
