import type { VariableConfig } from '../../Variable'

/**
 * Configuration type for date/time variables
 */
export type DateTimeVariableType<Type extends string | Date = string | Date> = VariableConfig<Type>

/**
 * A Variable for a date/time field.
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the date/time variable including:
 *   * **question** - Question text displayed to the user
 *   * **exampleText**? - Example text for the variable
 *   * **defaultValue**? - Default value for the variable
 *   * And all other properties from the base Variable type
 */
export function DateTimeVariable(config: DateTimeVariableType) {
    return config
}
