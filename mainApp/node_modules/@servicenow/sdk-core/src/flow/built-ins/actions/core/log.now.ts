import { ActionDefinition } from '../../../Action'
import { ChoiceColumn, StringColumn } from '../../../../db'

export const log = ActionDefinition({
    $id: '5bc1bcc6531003003bf1d9109ec587d4',
    name: 'Log',
    description: 'Log a message',
    inputs: {
        log_level: ChoiceColumn({
            label: 'Level',
            default: 'info',
            choices: {
                warn: {
                    label: 'Warning',
                    sequence: 200,
                },
                info: {
                    label: 'Info',
                    sequence: 100,
                },
                error: {
                    label: 'Error',
                    sequence: 300,
                },
            },
        }),
        log_message: StringColumn({
            label: 'Message',
            maxLength: 255,
        }),
    },
    outputs: {},
})
