import { ActionDefinition } from '../../../Action'
import { DocumentIdColumn } from '../../../../db'

export const deleteRecord = ActionDefinition({
    $id: 'c3e09916c31332002841b63b12d3aedf',
    name: 'Delete Record',
    description: 'Deletes a record on any ServiceNow table.',
    inputs: {
        record: DocumentIdColumn({
            label: 'Record',
            mandatory: true,
            dependent: '',
            maxLength: 40,
        }),
    },
    outputs: {},
})
