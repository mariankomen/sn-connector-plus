import { ActionDefinition } from '../../../Action'
import { StringColumn, ReferenceColumn } from '../../../../db'

export const getEmailHeader = ActionDefinition({
    $id: 'a8cfa3efa70223008299b39f0879018b',
    name: 'Get Email Header',
    annotation:
        'Access an email header value as a data pill. If multiple headers have the same name, then the action gets the value of the first header that appears.',
    description:
        'Access an email header value as a data pill. If multiple headers have the same name, then the action gets the value of the first header that appears.',
    category: '3a116084a71223008299b39f087901c1',
    inputs: {
        target_header: StringColumn({
            label: 'Target header',
            mandatory: true,
            maxLength: 8000,
            hint: 'The name of the header that you want the value of (e.g. From, X-ServiceNow-Generated)',
        }),
        email_record: ReferenceColumn({
            label: 'Email Record',
            mandatory: true,
            maxLength: 8000,
            hint: 'A record from the Email [sys_email] table.',
            referenceTable: 'sys_email',
        }),
    },
    outputs: {
        header_value: StringColumn({
            label: 'Header value',
            maxLength: 8000,
            hint: 'The value of the email header that the action returned. This will be an empty string if it is not found.',
        }),
    },
})
