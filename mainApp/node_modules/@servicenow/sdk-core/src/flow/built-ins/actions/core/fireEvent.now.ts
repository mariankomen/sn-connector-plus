import { ActionDefinition } from '../../../Action'
import { StringColumn, TableNameColumn, DocumentIdColumn, ReferenceColumn } from '../../../../db'

export const fireEvent = ActionDefinition({
    $id: '80a30edeff30311077a95dac793bf19b',
    name: 'Fire Event',
    description:
        'This action can be used to fire a specific system event. This action typically passes four parameters that can be used within the event.',
    inputs: {
        parm1: StringColumn({
            label: 'Parameter 1',
            maxLength: 8000,
        }),
        table: TableNameColumn({
            label: 'Table',
        }),
        record: DocumentIdColumn({
            label: 'Record',
            mandatory: true,
            dependent: 'table',
        }),
        parm2: StringColumn({
            label: 'Parameter 2',
            maxLength: 8000,
        }),
        event_name: ReferenceColumn({
            label: 'Event',
            mandatory: true,
            referenceTable: 'sysevent_register',
        }),
    },
    outputs: {},
})
