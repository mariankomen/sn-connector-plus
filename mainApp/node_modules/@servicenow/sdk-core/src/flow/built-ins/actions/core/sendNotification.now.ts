import { ActionDefinition } from '../../../Action'
import { TableNameColumn, DocumentIdColumn, ReferenceColumn } from '../../../../db'

export const sendNotification = ActionDefinition({
    $id: '0d8dd9f10f11101051218e8ebc767e27',
    name: 'Send Notification',
    description:
        'Send a notification in one or more formats as specified by a notification record. The notification record you select determines the notification format and recipients.',
    inputs: {
        table_name: TableNameColumn({
            label: 'Table',
        }),
        record: DocumentIdColumn({
            label: 'Record',
            dependent: 'table_name',
        }),
        notification: ReferenceColumn({
            label: 'Notification',
            mandatory: true,
            referenceTable: 'sysevent_email_action',
        }),
    },
    outputs: {},
})
