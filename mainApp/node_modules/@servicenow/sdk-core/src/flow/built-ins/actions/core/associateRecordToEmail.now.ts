import { ActionDefinition } from '../../../Action'
import { DocumentIdColumn } from '../../../../db'

export const associateRecordToEmail = ActionDefinition({
    $id: '7b95a4c0a73133008299b39f087901b4',
    name: 'Associate Record to Email',
    annotation:
        'Associate a record with an Email [sys_email] record so that you can track which record is affected by the email. This action updates the Target field on the email record.',
    description:
        'Associate a record with an Email [sys_email] record so that you can track which record is affected by the email. This action updates the Target field on the email record.',
    category: '0f722c44a73133008299b39f08790130',
    inputs: {
        target_record: DocumentIdColumn({
            label: 'Target Record',
            mandatory: true,
            dependent: '',
        }),
        email_record: DocumentIdColumn({
            label: 'Email Record',
            mandatory: true,
            dependent: '',
        }),
    },
    outputs: {},
})
