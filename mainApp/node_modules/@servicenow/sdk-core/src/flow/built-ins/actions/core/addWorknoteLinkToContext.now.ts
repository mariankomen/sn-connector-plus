import { ActionDefinition } from '../../../Action'
import { FieldNameColumn, DocumentIdColumn, TableNameColumn, StringColumn } from '../../../../db'

export const addWorknoteLinkToContext = ActionDefinition({
    $id: 'fd4a0fb70f003300ecf0cc52ff767e2a',
    name: 'Add Worknote Link to Context',
    description: 'Adds a Worknote link referencing to the execution context.',
    inputs: {
        journal_field: FieldNameColumn({
            label: 'Journal Field',
            mandatory: true,
        }),
        record: DocumentIdColumn({
            label: 'Record',
            mandatory: true,
            maxLength: 8000,
            dependent: 'table',
        }),
        table: TableNameColumn({
            label: 'Table',
            mandatory: true,
        }),
        additional_comments: StringColumn({
            label: 'Additional Comments',
            maxLength: 8000,
        }),
    },
    outputs: {},
})
