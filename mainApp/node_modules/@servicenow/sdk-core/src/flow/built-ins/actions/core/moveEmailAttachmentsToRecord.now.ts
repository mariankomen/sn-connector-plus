import { ActionDefinition } from '../../../Action'
import { DocumentIdColumn } from '../../../../db'

export const moveEmailAttachmentsToRecord = ActionDefinition({
    $id: 'bd8fcd48a73133008299b39f087901fc',
    name: 'Move Email Attachments to Record',
    annotation:
        'Moves all email attachments to a record and updates the email attachments table. To prevent issues with large email messages, the system enforces configured limits on the maximum allowed email body size, total attachment file size, and number of atta',
    description:
        'Moves all email attachments to a record and updates the email attachments table. To prevent issues with large email messages, the system enforces configured limits on the maximum allowed email body size, total attachment file size, and number of attachments per email.',
    category: '0f722c44a73133008299b39f08790130',
    inputs: {
        target_record: DocumentIdColumn({
            label: 'Target Record',
            dependent: '',
            mandatory: true,
        }),
        email_record: DocumentIdColumn({
            label: 'Email Record',
            dependent: '',
            mandatory: true,
        }),
    },
    outputs: {},
})
