import { TriggerDefinition } from '../../TriggerDefinition'
import { DateTimeColumn } from '../../../db'

export const runOnceTrigger = TriggerDefinition({
    $id: '0a76e504c32222002841b63b12d3aeac',
    name: 'Run Once',
    type: 'run_once',
    inputs: {
        run_in: DateTimeColumn({
            label: 'Run in',
            mandatory: true,
            maxLength: 100,
        }),
    },
    outputs: {
        run_start_time: DateTimeColumn({
            label: 'Run Start Time',
        }),
        run_start_date_time: DateTimeColumn({
            label: 'Run Start Date/Time',
        }),
    },
})
