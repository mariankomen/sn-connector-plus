import { TriggerDefinition } from '../../TriggerDefinition'
import { DateTimeColumn, DurationColumn } from '../../../db'

export const repeatTrigger = TriggerDefinition({
    $id: 'f63f0d94c32222002841b63b12d3aeed',
    name: 'Repeat',
    type: 'repeat',
    inputs: {
        repeat: DurationColumn({
            label: 'Repeat',
            mandatory: true,
        }),
    },
    outputs: {
        run_start_date_time: DateTimeColumn({
            label: 'Run Start Date/Time',
        }),
        run_start_time: DateTimeColumn({
            label: 'Run Start Time',
        }),
    },
})
