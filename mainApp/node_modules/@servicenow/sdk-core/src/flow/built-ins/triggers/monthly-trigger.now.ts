import { TriggerDefinition } from '../../TriggerDefinition'
import { DateTimeColumn, IntegerColumn, TimeColumn } from '../../../db'

export const monthlyTrigger = TriggerDefinition({
    $id: '2ca52504c32222002841b63b12d3ae4a',
    name: 'Monthly',
    type: 'monthly',
    inputs: {
        day_of_month: IntegerColumn({
            label: 'Day of Month',
            mandatory: true,
            attributes: {
                integer_type: 'day_of_month',
            },
        }),
        time: TimeColumn({
            label: 'Time',
            mandatory: true,
        }),
    },
    outputs: {
        run_start_time: DateTimeColumn({
            label: 'Run Start Time',
        }),
        run_start_date_time: DateTimeColumn({
            label: 'Run Start Date/Time',
        }),
    },
})
