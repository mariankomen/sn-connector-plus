import { TriggerDefinition } from '../../TriggerDefinition'
import { DateTimeColumn, DayOfWeekColumn, TimeColumn } from '../../../db'

export const weeklyTrigger = TriggerDefinition({
    $id: 'cf352104c32222002841b63b12d3ae1f',
    name: 'Weekly',
    type: 'weekly',
    inputs: {
        day_of_week: DayOfWeekColumn({
            label: 'Day of Week',
            mandatory: true,
        }),
        time: TimeColumn({
            label: 'Time',
            mandatory: true,
        }),
    },
    outputs: {
        run_start_date_time: DateTimeColumn({
            label: 'Run Start Date/Time',
        }),
        run_start_time: DateTimeColumn({
            label: 'Run Start Time',
        }),
    },
})
