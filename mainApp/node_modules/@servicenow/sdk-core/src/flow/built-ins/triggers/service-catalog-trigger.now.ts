import { TriggerDefinition } from '../../TriggerDefinition'
import { ChoiceColumn, DateTimeColumn, ReferenceColumn, TableNameColumn } from '../../../db'

export const serviceCatalogTrigger = TriggerDefinition({
    $id: 'c43a1011c36813002841b63b12d3ae15',
    name: 'Service Catalog',
    type: 'service_catalog',
    inputs: {
        run_flow_in: ChoiceColumn({
            label: 'Run flow In',
            default: 'any',
            choices: {
                any: {
                    label: 'Run flow in any context',
                    sequence: 0,
                },
                background: {
                    label: 'Run flow in background (default)',
                    sequence: 1,
                },
                foreground: {
                    label: 'Run flow in foreground',
                    sequence: 2,
                },
            },
            attributes: {
                advanced: 'true',
            },
        }),
    },
    outputs: {
        request_item: ReferenceColumn({
            label: 'Request Item',
            mandatory: true,
            referenceTable: 'sc_req_item',
        }),
        run_start_time: DateTimeColumn({
            label: 'Run Start Time',
        }),
        table_name: TableNameColumn({
            label: 'Table Name',
            maxLength: 40,
            default: 'sc_req_item',
        }),
        run_start_date_time: DateTimeColumn({
            label: 'Run Start Date/Time',
        }),
    },
})
