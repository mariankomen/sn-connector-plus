// Complex Data transformations

type ToXml = (input: object) => string

export interface ComplexDataFns {
    /**
     * Serializes the input Complex Object to XML.
     *
     * @param input The complex object to convert.
     * @returns XML string representation of the object.
     * @example
     * toXml({ name: 'John', age: 30 }); // → '<name>John</name><age>30</age>'
     *
     * @note Don't enclose the input data pill in quotes. The system automatically wraps the input value
     * with the type of quotes or backticks that apply to your database type. If input data type is not String,
     * the data is not transformed at runtime.
     */
    toXml: ToXml
}

export declare const complexDataFns: ComplexDataFns
