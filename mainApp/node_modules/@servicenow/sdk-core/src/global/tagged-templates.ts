declare global {
    /**
     * @deprecated Recommended that you use `Now.include` and move the code to a separate file
     */
    function script(_strings: TemplateStringsArray, ..._keys: unknown[]): string

    /**
     * @deprecated Recommended that you use `Now.include` and move the code to a separate file
     */
    function html(_strings: TemplateStringsArray, ..._keys: unknown[]): string
}
