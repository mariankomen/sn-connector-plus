import { BooleanColumn, ReferenceColumn, Table, StringColumn } from '../../db'

export const sys_ws_query_parameter = Table({
    name: 'sys_ws_query_parameter',
    schema: {
        name: StringColumn({}),
        required: BooleanColumn({}),
        example_value: StringColumn({}),
        short_description: StringColumn({}),
        web_service_definition: ReferenceColumn({ referenceTable: 'sys_ws_definition' }),
    },
})
