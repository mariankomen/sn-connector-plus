import { Table, StringColumn, IntegerColumn } from '../../db'

export const sys_security_operation = Table({
    name: 'sys_security_operation',
    extends: 'sys_metadata',
    schema: {
        name: StringColumn({ mandatory: true }),
        description: StringColumn({}),
        order: IntegerColumn({}),
    },
})
