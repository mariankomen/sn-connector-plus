import { BooleanColumn, StringColumn, ReferenceColumn, Table } from '../../db'

export const sys_ui_annotation = Table({
    name: 'sys_ui_annotation',
    schema: {
        is_plain_text: BooleanColumn({ default: true }),
        text: StringColumn({}),
        type: ReferenceColumn({ referenceTable: 'sys_ui_annotation_type' }),
    },
})
