import { BooleanColumn, IntegerColumn, StringColumn, TranslatedFieldColumn, Table } from '../../db'

export const sys_ui_application = Table({
    name: 'sys_ui_application',
    extends: 'sys_metadata',
    schema: {
        active: BooleanColumn({ default: true }),
        roles: StringColumn({}),
        order: IntegerColumn({}),
        hint: StringColumn({ maxLength: 255 }),
        //sys_overrides: ReferenceColumn({ referenceTable: sys_ui_application }),
        name: TranslatedFieldColumn({}),
    },
})
