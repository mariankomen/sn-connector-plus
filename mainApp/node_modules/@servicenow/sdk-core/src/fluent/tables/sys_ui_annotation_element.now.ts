import { StringColumn, ReferenceColumn, Table } from '../../db'

export const sys_ui_annotation_element = Table({
    name: 'sys_ui_annotation_element',
    schema: {
        type: StringColumn({
            choices: { element: '', annotation: 'annotation' },
        }),
        element: ReferenceColumn({ referenceTable: 'sys_ui_annotation' }),
    },
})
