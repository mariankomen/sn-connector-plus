import { BooleanColumn, StringColumn, Table } from '../../db'

export const sys_ui_annotation_type = Table({
    name: 'sys_ui_annotation_type',
    schema: {
        active: BooleanColumn({ default: true }),
        name: StringColumn({ mandatory: true }),
        omit_spacer: BooleanColumn({ default: false }),
        style: StringColumn({}),
    },
})
