import { BooleanColumn, ReferenceColumn, StringColumn, Table } from '../../db'

export const sys_ws_header = Table({
    name: 'sys_ws_header',
    extends: 'sys_metadata',
    schema: {
        name: StringColumn({}),
        required: BooleanColumn({}),
        example_value: StringColumn({}),
        short_description: StringColumn({}),
        web_service_definition: ReferenceColumn({ referenceTable: 'sys_ws_definition' }),
    },
})
