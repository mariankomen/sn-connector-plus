import { StringColumn, Table } from '../../db'

export const sys_security_type = Table({
    name: 'sys_security_type',
    extends: 'sys_metadata',
    schema: {
        description: StringColumn({}),
        name: StringColumn({ mandatory: true }),
    },
})
