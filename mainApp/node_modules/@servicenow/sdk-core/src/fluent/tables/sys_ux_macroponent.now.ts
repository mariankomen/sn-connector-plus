import { Table, StringColumn, VersionColumn } from '../../db'

export const sys_ux_macroponent = Table({
    name: 'sys_ux_macroponent',
    schema: {
        category: StringColumn({}),
        composition: StringColumn({}),
        extends: StringColumn({}), // TODO: Reference?
        internal_event_mappings: StringColumn({}),
        layout: StringColumn({}),
        name: StringColumn({}),
        props: StringColumn({}),
        schema_version: VersionColumn({}),
    },
})
