import { DocumentIdColumn, StringColumn, Table, TableNameColumn } from '../../../db'

export const atf_input_variable = Table({
    name: 'atf_input_variable',
    schema: {
        element: StringColumn({}),
        model_table: TableNameColumn({}),
        model_id: DocumentIdColumn({ dependent: 'model_table' }),
    },
})
