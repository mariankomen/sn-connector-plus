import { Table, ReferenceColumn } from '../../db'

export const sys_user_role_contains = Table({
    name: 'sys_user_role_contains',
    extends: 'sys_metadata',
    schema: {
        contains: ReferenceColumn({ mandatory: true, referenceTable: 'sys_user_role' }),
        role: ReferenceColumn({ mandatory: true, referenceTable: 'sys_user_role' }),
    },
})
