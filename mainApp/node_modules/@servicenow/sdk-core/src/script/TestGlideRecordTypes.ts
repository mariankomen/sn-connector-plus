import { newGlideRecord, type GlideRecord } from './GlideRecord'

const x = {} as GlideRecord<'sys_atf_step'>
console.info(x.active)

function isBoolean(arg1: boolean) {
    console.info(arg1)
}
if (typeof x.active !== 'undefined') {
    isBoolean(x.active)
}

if (typeof x.inputs !== 'undefined') {
    //isString(x.inputs)//JL: this should work but its not compiling at present. Seems like a bug with string fields
}

x.addQuery('active', true)

const gr = newGlideRecord('sys_atf_step')
gr.addQuery('active', true)
