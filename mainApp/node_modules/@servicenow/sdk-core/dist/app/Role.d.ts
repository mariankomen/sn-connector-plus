type canDelegate = {
    /** @deprecated Indicates if the role can be delegated to other users. */
    can_delegate?: boolean;
    canDelegate?: never;
} | {
    /** Indicates if the role can be delegated to other users. */
    canDelegate?: boolean;
    can_delegate?: never;
};
type assignableBy = {
    /** @deprecated Other roles that can assign this role to users. */
    assignable_by?: string;
    assignableBy?: never;
} | {
    /** Other roles that can assign this role to users. */
    assignableBy?: string;
    assignable_by?: never;
};
type elevatedPrivilege = {
    /** @deprecated Indicates whether manually accepting the responsibility of using the role before accessing its features is required. */
    elevated_privilege?: boolean;
    elevatedPrivilege?: never;
} | {
    /** Indicates whether manually accepting the responsibility of using the role before accessing its features is required. */
    elevatedPrivilege?: boolean;
    elevated_privilege?: never;
};
type containsRoles = {
    /** @deprecated Other roles that this role contains. */
    contains_roles?: (string | Role)[];
    containsRoles?: never;
} | {
    /** Other roles that this role contains. */
    containsRoles?: (string | Role)[];
    contains_roles?: never;
};
type scopedAdmin = {
    /** @deprecated Indicates whether the role is an Application Administrator role. */
    scoped_admin?: boolean;
    scopedAdmin?: never;
} | {
    /** Indicates whether the role is an Application Administrator role. */
    scopedAdmin?: boolean;
    scoped_admin?: never;
};
export type Role = {
    /** @deprecated Role IDs are now derived from {@link Role.name}. The explicit $id is no longer used. */
    $id?: Now.Internal.ExplicitKey | string | number;
    /** Name for the role beginning with the application scope in the format: `<scope>.<name>`. */
    name: string;
    /** A description of what the role can access. */
    description?: string;
    /** Indicates whether the role can be granted independently. */
    grantable?: boolean;
} & canDelegate & assignableBy & elevatedPrivilege & scopedAdmin & containsRoles;
/**
 * Creates a new Role (`sys_user_role`)
 *
 * @see https://docs.servicenow.com/csh?topicname=role-api-now-ts.html&version=latest
 *
 * @param config - an object containing the following properties:
 *   * **name** - name for the role beginning with the application scope in the following format: `<scope>.<name>`.
 *   * **assignableBy**? - other roles that can assign this role to users.
 *   * **canDelegate**? - indicates if the role can be delegated to other users.
 *   * **containsRoles**? - variable identifiers of other `Role` objects that this role contains.
 *   * **description**? - a description of what the role can access.
 *   * **elevatedPrivilege**? - indicates whether manually accepting the responsibility of using the role before you can access the features of the role is required.
 *   * **grantable**? - indicates whether the role can be granted independently
 *   * **scopedAdmin**? - indicates whether the role is an Application Administrator role.
 */
export declare function Role(config: Role & Now.Internal.Meta): Required<Role>;
export {};
