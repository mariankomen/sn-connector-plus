export declare const runOnceTrigger: {
    readonly $id: "0a76e504c32222002841b63b12d3aeac";
    readonly name: "Run Once";
    readonly type: "run_once";
    readonly inputs: {
        readonly run_in: import("../../../db").Typed<`${number}-${number}-${number} ${number}:${number}:${number}`, {
            readonly label: "Run in";
            readonly mandatory: true;
            readonly maxLength: 100;
        }>;
    };
    readonly outputs: {
        readonly run_start_time: import("../../../db").Typed<`${number}-${number}-${number} ${number}:${number}:${number}`, {
            readonly label: "Run Start Time";
        }>;
        readonly run_start_date_time: import("../../../db").Typed<`${number}-${number}-${number} ${number}:${number}:${number}`, {
            readonly label: "Run Start Date/Time";
        }>;
    };
};
