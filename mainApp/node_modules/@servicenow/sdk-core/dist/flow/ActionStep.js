"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ActionStepDefinition = ActionStepDefinition;
exports.ActionStep = ActionStep;
/**
 * StepDefinition: returns an ActionStepWithMeta that preserves the allow_* flags at the type level.
 * - AI / AO are inferred from the provided config when you pass literal true/false values.
 */
function ActionStepDefinition(_config) {
    // runtime: no-op; types are what matters
    return undefined;
}
/**
 * StepInstance: enforces input/output extended properties depending on the step's type flags.
 *
 * Note:
 *  - _step must be the value returned by StepDefinition; its type carries AI/AO flags.
 *  - _inputs is the actual inputs object passed when creating a step instance.
 */
function ActionStep(_step, _config, _inputs) {
    return undefined;
}
//# sourceMappingURL=ActionStep.js.map