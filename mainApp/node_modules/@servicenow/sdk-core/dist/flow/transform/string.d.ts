type Contains = (input: string, substring: string) => boolean;
type StringToNumber = (input: string) => number;
type DoesNotContain = (input: string, substring: string) => boolean;
type EndsWith = (input: string, suffix: string) => boolean;
type FirstCharacter = (input: string) => string;
type LastCharacter = (input: string) => string;
type ReplaceString = (input: string, regex: string, replaceWith: string) => string;
type Size = (input: string) => number;
type Split = (input: string, separator: string) => string[];
type StartsWith = (input: string, prefix: string) => boolean;
type Substring = (input: string, startIndex: number, endIndex?: number) => string;
type ToLowerCase = (input: string) => string;
type ToProperCase = (input: string) => string;
type ToUpperCase = (input: string) => string;
type Trim = (input: string) => string;
export interface StringFns {
    /**
     * A utility function that checks whether the input value contains a sequence of characters.
     *
     * @param input      The String
     * @param substring  String - The characters to be searched for.
     * @returns Boolean - True if the input String contains the specified substring, false otherwise
     * @example
     * contains('Cheese Pizza', 'Cheese'); // → true
     *
     * @note The transfom is case sensitive. If input data type is not String, the data is not transformed at runtime.
     */
    contains: Contains;
    /**
     * Converts a string to number.
     *
     * @param input The string value that should represent a number, e.g. "500".
     * @returns Number - The number representation of the input string
     * @example
     * stringToNumber('500'); // → 500
     * @note If input data type is not string, the data is not transformed at runtime.
     */
    stringToNumber: StringToNumber;
    /**
     * A utility function that checks whether the input value does not contain a sequence of characters.
     *
     * @param input      The String
     * @param substring  String - The characters to be searched for.
     * @returns Boolean - True if the input String does not contain the specified substring, false otherwise
     * @example
     * doesNotContain('Cheese Pizza', 'Joey'); // → true
     *
     * @note The transfom is case sensitive. If input data type is not String, the data is not transformed at runtime.
     */
    doesNotContain: DoesNotContain;
    /**
     * A utility function that checks whether the input value ends with a sequence of characters.
     *
     * @param input  The String
     * @param suffix String - The characters to be searched for.
     * @returns Boolean - True if the input String ends with the specified suffix, false otherwise
     * @example
     * endsWith('Cheese Pizza', 'Pizza'); // → true
     *
     * @note The transfom is case sensitive. If input data type is not String, the data is not transformed at runtime.
     */
    endsWith: EndsWith;
    /**
     * Returns the first character of the input string.
     *
     * @param input A non-empty string value.
     * @returns String - Transformed String as first character of input String
     * @example
     * firstCharacter('Example input string.'); // → 'E'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    firstCharacter: FirstCharacter;
    /**
     * Returns the last character of the input String.
     *
     * @param input A non-empty string value.
     * @returns String - Transformed String as last character of input String
     * @example
     * lastCharacter('Example input string.'); // → '.'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    lastCharacter: LastCharacter;
    /**
     * Returns a Replaced String from the input String based on the provided Regex and Replacement String.
     *
     * @param input        The source string.
     * @param regex        Regular expression to be matched for replace
     * @param replaceWith  Replacement String
     * @returns String - Transformed String with replaced characters
     * @example
     * replaceString("Example input string.", "\"", "\\\"");
     * // → '"Example input string."'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    replaceString: ReplaceString;
    /**
     * Returns the total number of characters in the input String.
     *
     * @param input The string whose length to return.
     * @returns Number - Total number of characters in the input String
     * @example
     * size('Example input string.'); // → 21
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    size: Size;
    /**
     * Returns an Array.String based on a provided Separator that splits the input String.
     *
     * @param input      The string to split.
     * @param separator  A delimiter that specifies where the input String should be split. If left blank, the input String is not transformed at runtime.
     * @returns Array.String - Transformed String split based on the provided separator
     * @example
     * split('Example,input,string.', ',');
     * // → ['Example', 'input', 'string.']
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    split: Split;
    /**
     * A utility function that checks whether the input value starts with a sequence of characters.
     *
     * @param input  The source string.
     * @param prefix The characters to be searched for.
     * @returns Boolean - True if the input String starts with the specified prefix, false otherwise
     * @example
     * startsWith('Cheese Pizza', 'Chees'); // → true
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    startsWith: StartsWith;
    /**
     * Returns a substring from the input String based on the provided Start Index and End Index.
     *
     * @param input      The original string.
     * @param startIndex Index of the first character to include in the returned substring
     * @param endIndex   Index of the last character to include in the returned substring
     * @returns String - Transformed String based on the provided Start Index and End Index
     * @example
     * substring('Example input string.', 3, 6); // → 'mpl'
     *
     * @note Input String index starts at 0. If End Index is not provided, Start Index character and remaineder
     * of input String is returned. If input data type is not String, the data is not transformed at runtime.
     */
    substring: Substring;
    /**
     * Converts the input String to all lowercase characters.
     *
     * @param input The string to transform.
     * @returns String - Transformed String in lowercase
     * @example
     * toLowerCase('ExamPle inpuT stRing.'); // → 'example input string.'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    toLowerCase: ToLowerCase;
    /**
     * Changes the case of words in the input string. Capitalizes the first letter of each word, and
     * makes every other letter in the word lower case. A word is considered any string separated by
     * a space, hyphen, backslash, or forward slash character. The transform function always evaluates
     * words from left-to-right to determine the first letter.
     *
     * @param input The string to transform.
     * @returns String - Transformed String with proper case
     * @example
     * toProperCase('exAMPle-input string/TEXT');
     * // → 'Example-Input String/Text'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    toProperCase: ToProperCase;
    /**
     * Converts the input string to all uppercase characters.
     *
     * @param input The string to transform.
     * @returns String - Transformed String in uppercase
     * @example
     * toUpperCase('ExamPle inpuT stRing.'); // → 'EXAMPLE INPUT STRING.'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    toUpperCase: ToUpperCase;
    /**
     * Removes white space from the beginning and end of the input String.
     *
     * @param input The string to trim.
     * @returns String - Transformed String with trimmed white space
     * @example
     * trim('   Example input string.   '); // → 'Example input string.'
     *
     * @note Does not remove white space within the input String. If input data type is not String, the data is not transformed at runtime.
     */
    trim: Trim;
}
export declare const stringFns: StringFns;
export {};
