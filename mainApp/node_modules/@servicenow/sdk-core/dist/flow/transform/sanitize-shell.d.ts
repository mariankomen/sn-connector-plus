type SanitizeBashShellArguments = (input: string | string[]) => string | string[];
export interface SanitizeShellFns {
    /**
     * Returns a String free of any unsafe command injections in your Bash shell script.
     * Wraps the input String with single quotes and escapes any existing single quotes
     * so that you can pass the String directly to a shell function as a safe argument.
     *
     * @param input The string
     * @returns The sanitized string or array of strings.
     * @example
     * sanitizeBashShellArguments('echo "Hello, World!"'); // → 'echo "Hello, World!"'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    sanitizeBashShellArguments: SanitizeBashShellArguments;
}
export declare const sanitizeShellFns: SanitizeShellFns;
export {};
