type Abs = (input: number) => number;
type Add = (input: number, number: number) => number;
type Average = (input: number[]) => number;
type Divide = (input: number, number: number) => number;
type Max = (input: number[]) => number;
type Median = (input: number[]) => number;
type Min = (input: number[]) => number;
type Multiply = (input: number, number: number) => number;
type Power = (input: number, number: number) => number;
type Round = (input: number, digits: number) => number;
type Sqrt = (input: number) => number;
type Subtract = (input: number, number: number) => number;
type Sum = (input: number[]) => number;
type ToFixedValue = (input: number, digits: number) => number;
export interface MathFns {
    /**
     * A mathematical function that returns the distance from zero for any real number.
     * An absolute value is always a positive or zero value.
     *
     * @param input The numeric value whose absolute value should be returned.
     * @returns as the absolute value of the result of the input number.
     * @example
     * abs(-3); // → 3
     *
     * @note If the transform function receives a non-numeric input value, it returns the original input value unchanged.
     */
    abs: Abs;
    /**
     * A mathematical function that adds the given value to the input.
     *
     * @param input   The base number.
     * @param number  Number to be added.
     * @returns As the addition of the input value by the parameter.
     * @example
     * add(12, 4); // → 16
     *
     * @note If the input and parameter data types are not Number, the data is not transformed at runtime.
     */
    add: Add;
    /**
     * A mathematical function that returns the average value of the elements in the input Array.
     *
     * @param input An array of numbers, e.g. `[1, 2, 3]`.
     * @returns Number - As average value in input Array.
     * @example
     * average([1, 2, 3]); // → 2
     *
     * @note If the input data type is not Array, the data is not transformed at runtime.
     */
    average: Average;
    /**
     * A mathematical function that divides the input value by a given value.
     *
     * @param input   The dividend.
     * @param number  The divisor (non-zero) -  Number to be divided.
     * @returns Number - As division of the input value by the parameter.
     * @example
     * divide(10, 2); // → 5
     *
     * @note If the input and parameter data types are not Number, the data is not transformed at runtime.
     */
    divide: Divide;
    /**
     * Returns the highest value found in the input Array.
     *
     * @param input An array of numbers.
     * @returns Number - As highest value in input Array.
     * @example
     * max([3, 7, 2]); // → 7
     *
     * @note If the input data type is not Array.Number, the data is not transformed at runtime.
     */
    max: Max;
    /**
     * A mathematical function that returns the median value of elements in the input Array.
     *
     * @param input An array of numbers.
     * @returns Number - As median value in input Array.
     * @example
     * median([1, 3, 5]); // → 3
     *
     * @note If the input data type is not Array.Number, the data is not transformed at runtime.
     */
    median: Median;
    /**
     * Returns the lowest value found in the input Array.
     *
     * @param input An array of numbers.
     * @returns Number - As lowest value in input Array.
     * @example
     * min([3, 7, 2]); // → 2
     *
     * @note If the input data type is not Array.Number, the data is not transformed at runtime.
     */
    min: Min;
    /**
     * A mathematical function that multiplies the input value by a given value.
     *
     * @param input   The base number.
     * @param number  The multiplier.
     * @returns Number - As multiplication of the input value by the parameter.
     * @example
     * multiply(6, 2); // → 12
     *
     * @note If the input and parameter data types are not Number, the data is not transformed at runtime.
     */
    multiply: Multiply;
    /**
     * A mathematical function that returns the value of the input value raised to the power of a given value.
     *
     * @param input   The base number.
     * @param number  Number - Exponent of the power.
     * @returns Number - As the power of input value to the parameter.
     * @example
     * power(2, 3); // → 8
     *
     * @note If the input and parameter data types are not Number, the data is not transformed at runtime.
     */
    power: Power;
    /**
     * A mathematical function that approximates a numeric value based on rounding rules and a digit position.
     * The function rounds up by adding one to the digit to be rounded and then replacing all digits to its right with zeroes.
     *
     * @param input   The number to round.
     * @param digits  Number of Digits - A positive integer specifying the position of the digit to be rounded starting on the left.
     * @returns Number - as the rounded value of the input Number.
     * @example
     * round(1.93456, 2); // → 1.93
     *
     * @note The function uses the digit to the right of the parameter digit to round up or down. If the digit to the right has a value of 0 to 4, the function rounds down. If the digit to the right has a value of 5 to 9, then the function rounds up. If there is no digit to the right, then the function rounds down.
     */
    round: Round;
    /**
     * A mathematical function that computes a positive number that when multiplied by itself produces the input value.
     * The input value must be a positive real number.
     *
     * @param input A non-negative number.
     * @returns Number - as the square root of the input Number.
     * @example
     * sqrt(9); // → 3
     *
     * @note If the transform function receives a non-numeric or negative input value, it returns the original input value unchanged.
     */
    sqrt: Sqrt;
    /**
     * A mathematical function that subtracts the given value from the input.
     *
     * @param input   A number value
     * @param number  Number to be subtracted.
     * @returns Number - as the subtraction of the input value by the parameter.
     * @example
     * subtract(10, 4); // → 6
     *
     * @note  If the input and parameter data types are not Number, the data is not transformed at runtime.
     */
    subtract: Subtract;
    /**
     * Returns the sum of all values in the input Array.
     *
     * @param input An array of numbers, e.g. `[1, 2, 3]`.
     * @returns Number - as the sum of all values in the input Array.
     * @example
     * sum([1, 2, 3]); // → 6
     *
     * @note If the input data type is not Array.Number, the data is not transformed at runtime.
     */
    sum: Sum;
    /**
     * Returns a string representation of a floating point number fixed to the number of digits specified
     *
     * @param input   The number to format.
     * @param digits  Number of Digits - A positive integer specifying the position of the digit to be precisioned starting on the left.
     * @returns Number - as the fixed value of the input Number.
     * @example
     * toFixedValue(1.93456, 2); // → 1.93
     *
     * @note If the input data type is not Number, the data is not transformed at runtime.
     */
    toFixedValue: ToFixedValue;
}
export declare const mathFns: MathFns;
export {};
