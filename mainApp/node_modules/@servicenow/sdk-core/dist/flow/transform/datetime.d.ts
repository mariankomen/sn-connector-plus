/**
 * Object representation of a time duration. Each unit is optional and
 * defaults to `0` when omitted.
 */
interface TimeDuration {
    days?: number;
    hours?: number;
    minutes?: number;
    seconds?: number;
}
/**
 * Supported predefined date/time format tokens. Selecting `Custom` indicates a
 * custom format which must be provided via the `customFormat` parameter.
 */
type DateFormatChoice = 'Custom' | 'yyyy-MM-dd' | "yyyy-MM-dd'T'HH:mm:ss'Z'" | 'yyyy-MM-dd HH:mm:ss' | 'EEE, MMM dd, yyyy' | 'EEE, MMM dd, yyyy h:mm a' | 'MM-dd-yyyy' | 'MM-dd-yyyy h:mm a' | 'MM-dd-yyyy HH:mm:ss' | 'MM/dd/yyyy' | 'MM/dd/yyyy h:mm a' | 'MM/dd/yyyy HH:mm:ss' | 'MMM dd, yyyy' | 'MMM dd, yyyy h:mm a' | 'yyyy-MM-dd h:mm a' | 'yyyy/MM/dd' | 'yyyy/MM/dd h:mm a' | 'yyyy/MM/dd HH:mm:ss';
type AddTime = (input: Date, duration?: TimeDuration) => Date;
type DateDifference = (input: Date, dateTime: Date) => Date;
type DateToString = <F extends DateFormatChoice>(input: Date, format: F, ...rest: F extends 'Custom' ? [customFormat: string] : []) => string;
type Day = (input: Date) => number;
type EndOfMonth = (input: Date) => Date;
type Hour = (input: Date) => number;
type Minute = (input: Date) => number;
type Month = (input: Date) => number;
type Second = (input: Date) => number;
type StringToDate = (input: string, format: DateFormatChoice, customFormat?: string) => Date;
type SubtractTime = (input: Date, duration?: TimeDuration) => Date;
type Week = (input: Date) => number;
type Year = (input: Date) => number;
export interface DateTimeFns {
    /**
     * Adds days, hours, minutes, and/or seconds to the input Date/Time, Date, or Due Date.
     *
     * @param input     Date/Time, Date, or Due Date
     * If the input is a Date or Due Date, the transform creates a full date/time value using time values of hour 0, minute 0, and second 0.
     *
     * @param duration  Object specifying any of `days`, `hours`, `minutes`, `seconds`. Omitted units default to `0`.
     * @returns Date/Time - Transformed Date/Time after adding the specified Duration
     * @example
     * addTime(new Date('2021-05-02 09:10:12'), { days: 2, hours: 3 }) // → '2021-05-04 12:10:12'
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    addTime: AddTime;
    /**
     * Return a duration of time between the input date and the parameter date time.
     *
     * @param input     DateTime/DueDate/Date
     * @param dateTime  Date/Time, Due Date or Date to calculate difference between the given input.
     * @returns Duration- Returns the duration of time added to epoch time (1970-01-01) in Date/Time format, representing the difference between two input dates.
     * @example
     * dateDifference('2021-05-02 09:10:12', '2021-04-07 06:02:23') // → '1970-01-26 03:07:49'
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    dateDifference: DateDifference;
    /**
     * Converts a Date/Time, Date, or Due Date to a formatted string.
     *
     * @param input  Date/Time, Date, or Due Date - If the input is a Date or Due Date,
     *               the transform creates a full date/time value using time values of hour 0, minute 0, and second 0.
     * @param format Date/time or date format of the output String. Predefined format choice value. Use `Custom` for custom
     * @param customFormat ***Required*** Valid date/time or date format represented as a String. Required only if Custom Format is selected as the Date Format.
     * @returns String - String formatted as specified in Output Date Format
     * @example
     * dateToString('1996-01-16 19:56:10', 'MM/dd/yy') // → '01/16/96'
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     *
     * @note Common Date Formats
     *
     * |--------------------|--------|----------------------------|
     * | Format Type        | Format | Example                    |
     * |--------------------|--------|----------------------------|
     * | Year               | y      | 1970, 1971, ... 2004, 2005 |
     * | Month              | MM     | 01, 02, ... 11, 12         |
     * | Month Name         | MMM    | Jan, Feb, ... Nov, Dec     |
     * | Day in Month       | dd     | 01, 02, ... 30, 31         |
     * | Day Name           | EEE    | Sun, Mon, ... Fri, Sat     |
     * | Hour (12-hour)     | h      | 1, 2, ... 11, 12           |
     * | Hour (24-hour)     | HH     | 00, 01, ... 22, 23         |
     * | Minute in Hour     | mm     | 00, 01, ... 58, 59         |
     * | Second in Minute   | ss     | 00, 01, ... 58, 59         |
     * | Escape Character   | '      | 'at' h:mm a = at 1:30 PM   |
     * | (Single Quote)     |        |                            |
     * |--------------------|--------|----------------------------|
     */
    dateToString: DateToString;
    /**
     * Returns the day of the month for a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - returns day component of the specified input.
     * @example
     * day('2021-11-20 13:06:12') // → 20
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    day: Day;
    /**
     * Returns a date representing the last day of a month which falls a
     * specified number of months before or after specified date.
     *
     * @param input  Base date.
     * @param offset Number of months to be added to specified date.
     * @returns Date/Time - Returns a date representing the last day of a month which falls a specified number of months before or after specified date.
     * @example
     * endOfMonth('2021-05-15', 1) // → '2021-06-30 00:00:00'
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    endOfMonth: EndOfMonth;
    /**
     * Returns the hour component of a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - Hour component of the specified input.
     * @example
     * hour('2021-11-20 13:06:12') // → 13
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    hour: Hour;
    /**
     * Returns the minute component of a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - Minute component of the specified input.
     * @example
     * minute('2021-11-20 13:06:12') // → 6
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    minute: Minute;
    /**
     * Returns the month component of a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - Month component of the specified input.
     * @example
     * month('2021-11-20 13:06:12') // → 11
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    month: Month;
    /**
     * Returns the seconds component of a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - Seconds component of the specified input.
     * @example
     * second('2021-11-20 13:06:12') // → 12
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    second: Second;
    /**
     * Converts the input String to a Date/Time.
     *
     * @param input  String formatted as specified in Input Date Format
     * @param format Date/time or date format of the input String. Predefined format choice value. Use `Custom` for custom
     * @param customFormat ***Required*** Valid date/time or date format represented as a String. Required only if Custom Format is selected as the Date Format.
     * @returns Date/Time - If the date/time value for the Custom Format is incomplete, the transform creates a full date/time value using default dates and times.
     *          The transform defaults to the current year, the current month, day 1 of a month, hour 0, minute 0, and second 0.
     * @example
     * stringToDate('1995-11-20', 'yyyy-MM-dd') // → '1995-11-20 00:00:00'
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     *
     * @note Common Date Formats
     *
     * |--------------------|--------|----------------------------|
     * | Format Type        | Format | Example                    |
     * |--------------------|--------|----------------------------|
     * | Year               | y      | 1970, 1971, ... 2004, 2005 |
     * | Month              | MM     | 01, 02, ... 11, 12         |
     * | Month Name         | MMM    | Jan, Feb, ... Nov, Dec     |
     * | Day in Month       | dd     | 01, 02, ... 30, 31         |
     * | Day Name           | EEE    | Sun, Mon, ... Fri, Sat     |
     * | Hour (12-hour)     | h      | 1, 2, ... 11, 12           |
     * | Hour (24-hour)     | HH     | 00, 01, ... 22, 23         |
     * | Minute in Hour     | mm     | 00, 01, ... 58, 59         |
     * | Second in Minute   | ss     | 00, 01, ... 58, 59         |
     * | Escape Character   | '      | 'at' h:mm a = at 1:30 PM   |
     * | (Single Quote)     |        |                            |
     * |--------------------|--------|----------------------------|
     */
    stringToDate: StringToDate;
    /**
     * Subtracts days, hours, minutes, and/or seconds from the input Date/Time, Date, or Due Date.
     *
     * @param input     Date/Time, Date, or Due Date - If the date/time value for the Custom Format is incomplete, the transform creates a full date/time value using default dates and times.
     * @param duration  Object specifying any of `days`, `hours`, `minutes`, `seconds`. Omitted units default to `0`.
     * @returns Date/Time - Transformed Date/Time after subtracting the specified Duration
     * @example
     * subtractTime(new Date('2021-05-04 12:10:12'), { days: 2, hours: 3 }) // → '2021-05-02 09:10:12'
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    subtractTime: SubtractTime;
    /**
     * Returns the week of the year that a specific date falls on.
     *
     * @param input Date/Time value.
     * @returns Integer - Week of the year that a specific date falls on
     * @example
     * week('2021-04-07 12:01:12') // → 15
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    week: Week;
    /**
     * Returns the year component of a specific date and time.
     *
     * @param input Date/Time value.
     * @returns Integer - Year component of the specified input
     * @example
     * year('2021-11-20 13:06:12') // → 2021
     *
     * @note Runtime Date/Time values are not localized and appear in the UTC (Coordinated Universal Time) time zone.
     *
     * @note If input data type is not Date/Time, Date, or Due Date, the data is not transformed at runtime.
     */
    year: Year;
}
export declare const dateTimeFns: DateTimeFns;
export {};
