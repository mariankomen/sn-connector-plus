type GetFirstItem = <T>(input: T[]) => T;
type GetNthItem = <T>(input: T[], index: number) => T;
type GetLastItem = <T>(input: T[]) => T;
type GetItemFromNameValues = (input: {
    name: string;
    value: string;
}[], key: string, defaultValue: string) => string;
type IsBlank = (input: any) => boolean;
type IsFalse = (input: any) => boolean;
type IsNotBlank = (input: any) => boolean;
type IsNull = (input: any) => boolean;
type IsTrue = (input: any) => boolean;
type Join = (input: any[], delimiter: string) => string;
type KeyValueMap = (input: string, defaultValue: string, nameValues: {
    name: string;
    value: string;
}[]) => string;
type Sort = <T>(input: T[], order: 'ascending' | 'descending') => T[];
type Unique = <T>(input: T[]) => T[];
export interface UtilitiesFns {
    /**
     * Returns the first item from the input Array as a Complex Object.
     *
     * @param input Array of any values.
     * @returns Complex Object - First item found in the input Array
     * @example
     * getFirstItem([1,2,3]) // → 1
     *
     * @note If input data type is not Array, the data is not transformed at runtime.
     */
    getFirstItem: GetFirstItem;
    /**
     * Returns a Complex Object from the input Array.
     *
     * @param input Array of any values.
     * @param index Index of the target object in the input Array. Array index starts at 0.
     * @returns Complex Object - Target object found in the input Array
     * @example
     * getNthItem(['a','b','c'],1) // → 'b'
     *
     * @note If input data type is not an array, the data is not transformed at runtime.
     */
    getNthItem: GetNthItem;
    /**
     * Returns the last item from the input Array as a Complex Object.
     *
     * @param input Array of any values.
     * @returns Complex Object - Last item found in the input Array
     * @example
     * getLastItem([1,2,3]) // → 3
     *
     * @note If input data type is not an array, the data is not transformed at runtime.
     */
    getLastItem: GetLastItem;
    /**
     * Returns the value for a given key from a map of name/value pairs.
     *
     * @param input Array of `{name,value}` objects.
     * @param key  Name of the key used to look up its corresponding value
     * @param defaultValue The default value returned if the provided Key is not found in the map of Name/Values
     * @returns String - Value associated with matching key
     * @example
     * getItemFromNameValues([{name:'a',value:'x'}],'a','d') // → 'x'
     *
     * @note If input data type is not Name/Values or Name-Value Pairs, the data is not transformed at runtime.
     */
    getItemFromNameValues: GetItemFromNameValues;
    /**
     * A utility function that checks whether the input value is blank. The function supports Integers, Double, Boolean, Complex Objects and String.
     *
     * @param input Pills or values of above mentioned Types
     * @returns Boolean - True if string is empty or whitespace
     * @example
     * isBlank('') // → true
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    isBlank: IsBlank;
    /**
     * A utility function that checks whether the input value is false. The function supports Integers, Double, Boolean, Complex Objects, and String.
     *
     * @param input Pills or values of above mentioned Types
     * @returns Boolean - True if input strictly equals false
     * @example
     * isFalse(false) // → true
     *
     * @note For Integer and Double values that are 0, the transform returns true.
     * For strings that are empty or "0", the transform returns true.
     * For Complex objects and arrays that have a value, the transform returns false.
     * For false boolean values, the transform returns true.
     */
    isFalse: IsFalse;
    /**
     * A utility function that checks whether the input value is NOT blank. The function supports Integers, Double, Boolean, Complex Objects and String.
     *
     * @param input Pills or values of above mentioned Types
     * @returns Boolean - True if string has non-whitespace chars
     * @example
     * isNotBlank('abc') // → true
     *
     * @note For Integer and Double values which are not 0, the transform returns true.
     * For strings that are not empty or not "0", the transform returns true.
     * For Complex objects and arrays that have a value, the transform returns true.
     * For true boolean values, the transform returns true.
     */
    isNotBlank: IsNotBlank;
    /**
     * A utility function that checks whether the input value is null.
     *
     * @param input Pills or values of above mentioned Types
     * @returns Boolean - True if input strictly equals null
     * @example
     * isNull(null) // → true
     *
     * @note If the input value or complex object is null, the transform returns true.
     */
    isNull: IsNull;
    /**
     * A utility function that checks whether the input value is logically true. The function supports Integers, Double, Boolean, Complex Objects, and String.
     *
     * @param input Pills or values of above mentioned Types
     * @returns Boolean - True if input strictly equals true
     * @example
     * isTrue(13) // → true
     *
     * @note  For Integer and Double values that are not 0, the transform returns true.
     * For strings that are not empty or not "0", the transform returns true.
     * For Complex objects and arrays that have a value, the transform returns true.
     * For true boolean values, the transform returns true.
     */
    isTrue: IsTrue;
    /**
     * Concatenates the given input with the delimiter and returns the concatenated string.
     *
     * @param input     Array of primitive values.
     * @param delimiter Delimiter to be attached with each given input element.
     * @returns String - String joined with delimiter.
     * @example
     * join(['a','b','c'],'<') // → 'a<b<c'
     *
     * @note If input data type is not an array.string, array.integer, array.boolean and array.datetime, the data is not transformed at runtime.
     *
     * @note array.object type input is not supported.
     */
    join: Join;
    /**
     * Returns a value associated with a key from a map of Name/Values.
     *
     * @param input        String
     * @param defaultValue The default value returned if the provided Key is not found in the map of Name/Values
     * @param name_values  Map of key-value pairs in which to locate the Key input
     * @returns String - Value associated with matching key
     * @example
     * keyValueMap("3","Medium",[{name:'1',value:'High'},{name:'2',value:'Medium'},{name:'3',value:'Low'}]) // → 'Low'
     *
     * @note If input data type is not String, the data is not transformed at runtime.
     */
    keyValueMap: KeyValueMap;
    /**
     * Returns sorted array, sorted in ascending or descending order.
     *
     * @param input Array.
     * @param order Sort order: `ascending` or `descending`.
     * @returns Array - Sorted array
     * @example
     * sort([3,1,2],'ascending') // → [1,2,3]
     *
     * @note If input data type is not an array.string, array.integer, array.boolean and array.datetime, the data is not transformed at runtime.
     *
     * @note array.object type input is not supported.
     */
    sort: Sort;
    /**
     * Removes duplicate elements from the input array.
     *
     * @param input Array.
     * @returns Array - Array with unique elements
     * @example
     * unique([7,2,3,2]) // → [7,2,3]
     *
     * @note If input data type is not an array.string, array.integer, array.boolean and array.datetime, the data is not transformed at runtime.
     *
     * @note array.object type input is not supported.
     */
    unique: Unique;
}
export declare const utilitiesFns: UtilitiesFns;
export {};
