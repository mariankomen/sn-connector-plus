"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GoBackTo = GoBackTo;
/**
 * `GoBackTo` is used to create a loop in a flow by returning to a previous step.
 * It takes a label that identifies the target step and an object with an `$id` property for itself.
 *
 * @param step - The label of the step to go back to.
 * @param config - An object containing an `$id` property for this specific instance of the `GoBackTo` logic and an optional `annotation` property.
 */
function GoBackTo(_config, _step) {
    return;
}
//# sourceMappingURL=go-back-to.js.map