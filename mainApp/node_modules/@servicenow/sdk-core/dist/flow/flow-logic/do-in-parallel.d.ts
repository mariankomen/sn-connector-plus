/**
 * Function type for parallel execution blocks
 */
type ParallelBlockFunction = () => void;
/**
 * Configuration parameters for DoInParallel flow logic
 */
type DoInParallelParams = Now.Internal.WithID<{
    annotation?: string;
}>;
/**
 * Executes multiple code blocks in parallel within a flow.
 *
 * @param params - Configuration with $id and optional annotation
 * @param blocks - Functions to execute in parallel
 *
 * @example
 * ```typescript
 * DoInParallel(
 *   { $id: Now.ID['parallel_1'] },
 *   () => { send_notification(...) },
 *   () => { assign_to_group(...) },
 *   () => { update_record(...) }
 * )
 * ```
 */
export declare function DoInParallel(_params: DoInParallelParams, ..._blocks: ParallelBlockFunction[]): {
    type: 'doinparallel';
};
export {};
