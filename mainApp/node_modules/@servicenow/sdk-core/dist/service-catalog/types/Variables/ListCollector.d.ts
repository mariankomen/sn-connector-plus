import type { VariableConfig } from '../../Variable';
import type { Record } from '../../../db/Record';
import type { TableName } from '../../../db/Table';
/**
 * Configuration type for list collector variables
 */
export type ListCollectorVariableType<RefTable extends TableName = TableName, Type extends Record<RefTable>[] | Now.Internal.ExplicitKey[] | string[] = Record<RefTable>[] | Now.Internal.ExplicitKey[] | string[]> = VariableConfig<Type> & {
    /** Table that is being referenced by this variable */
    listTable: RefTable;
    /** Filter list based on a filter condition */
    referenceQual?: string;
};
/**
 * A Variable for a list collector field.
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the list collector variable including:
 *   * **question** - Question text displayed to the user
 *   * **listTable** - Table that is being referenced by this variable
 *   * **referenceQual**? - Filter reference based on a filter condition
 *   * **defaultValue**? - Default value for the variable
 *   * And all other properties from the base Variable type
 */
export declare function ListCollectorVariable(config: ListCollectorVariableType): ListCollectorVariableType<keyof Now.Internal.Tables, string[] | Now.Internal.ExplicitKey<string>[] | Record<keyof Now.Internal.Tables>[]>;
