import type { BaseVariableConfig } from '../../Variable';
/**
 * Configuration type for container start variables
 */
type ContainerStartVariableBase<Type = unknown, Default extends Type = Type> = Omit<BaseVariableConfig<Type, Default>, 'question'> & {
    /** Layout style for the container */
    layout?: 'normal' | '2across' | '2down';
};
export type ContainerStartVariableType<Type = unknown, Default extends Type = Type> = (ContainerStartVariableBase<Type, Default> & {
    /** Question text displayed to the user (container title) - required when displayTitle is true */
    question: string;
    /** Whether to display the title */
    displayTitle: true;
}) | (ContainerStartVariableBase<Type, Default> & {
    /** Question text displayed to the user (container title) */
    question?: string;
    /** Whether to display the title */
    displayTitle?: false;
});
/**
 * A Variable for a container start field (used for grouping variables).
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the container start variable including:
 *   * **question** - Question text displayed to the user (container title)
 *   * **displayTitle**? - Whether to display the title
 *   * **layout**? - Layout style for the container
 *   * 1 Column Wide
 *   * 2 Columns Wide, alternating sides
 *   * 2 Columns Wide, one side, then the other
 *   * And all other properties from the base Variable type
 */
export declare function ContainerStartVariable(config: ContainerStartVariableType): ContainerStartVariableType<unknown, unknown>;
export {};
