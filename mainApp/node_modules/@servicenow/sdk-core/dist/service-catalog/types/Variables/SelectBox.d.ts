import type { VariableConfig } from '../../Variable';
import type { TableName } from '../../../db/Table';
import type { ExtendedChoices } from './ExtendedChoices';
/**
 * Configuration type for select box variables
 */
export type SelectBoxVariableType<TChoices extends ExtendedChoices = ExtendedChoices, Type extends keyof TChoices = keyof TChoices, Default extends Type = Type, RefTable extends TableName = TableName> = VariableConfig<Type, Default> & {
    /** Table containing choices for the select box */
    choiceTable?: RefTable;
    /** Field containing choices for the select box */
    choiceField?: string;
    /** Choices for the select box */
    choices?: TChoices;
    /** Whether to include a "None" option */
    includeNone?: boolean;
    /** Whether to include a "None" option */
    uniqueValuesOnly?: boolean;
};
/**
 * A Variable for a select box field.
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the select box variable including:
 *   * **question** - Question text displayed to the user
 *   * **choiceTable** - Table containing choices for the select box
 *   * **choiceField** - Field containing choices for the select box
 *   * **choices** - Choices for the select box as key-value pairs with labels
 *   * **includeNone**? - Whether to include a "None" option
 *   * **defaultValue**? - Default value for the variable
 *   * And all other properties from the base Variable type
 */
export declare function SelectBoxVariable(config: SelectBoxVariableType): SelectBoxVariableType<ExtendedChoices, string | number, string | number, keyof Now.Internal.Tables>;
