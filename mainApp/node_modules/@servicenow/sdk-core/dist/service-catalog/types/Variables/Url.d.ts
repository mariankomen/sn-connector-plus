import type { VariableConfig } from '../../Variable';
/**
 * Configuration type for URL variables
 */
export type UrlVariableType<Type extends string = string> = VariableConfig<Type>;
/**
 * A Variable for a URL field.
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the URL variable including:
 *   * **question** - Question text displayed to the user
 *   * **defaultValue**? - Default value for the variable
 *   * And all other properties from the base Variable type
 */
export declare function UrlVariable(config: UrlVariableType): UrlVariableType<string>;
