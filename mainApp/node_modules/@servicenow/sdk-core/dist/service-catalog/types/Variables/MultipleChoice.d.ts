import type { VariableConfig } from '../../Variable';
import type { ExtendedChoices } from './ExtendedChoices';
/**
 * Configuration type for multiple choice variables
 */
export type MultipleChoiceVariableType<TChoices extends ExtendedChoices = ExtendedChoices, Type extends keyof TChoices = keyof TChoices> = VariableConfig<Type> & {
    /** Choices for the multiple choice */
    choices?: TChoices;
    /** Direction to display choices */
    choiceDirection?: 'down' | 'across';
    /** Whether to include a "None" option */
    includeNone?: boolean;
    /** Do not select the first choice */
    doNotSelectFirstChoice?: boolean;
};
/**
 * A Variable for a multiple choice field.
 *
 * @see https://docs.servicenow.com/csh?topicname=variable-types.html&version=latest
 *
 * @param config - Configuration for the multiple choice variable including:
 *   * **question** - Question text displayed to the user
 *   * **choices** - Choices for the multiple choice as key-value pairs with labels
 *   * **choiceDirection**? - Direction to display choices ('down' or 'across')
 *   * **includeNone**? - Whether to include a "None" option
 *   * **doNotSelectFirstChoice**? - Do not select the first choice
 *   * **defaultValue**? - Default value for the variable
 *   * And all other properties from the base Variable type
 */
export declare function MultipleChoiceVariable(config: MultipleChoiceVariableType): MultipleChoiceVariableType<ExtendedChoices, string | number>;
