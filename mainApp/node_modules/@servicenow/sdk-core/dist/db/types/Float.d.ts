import type { Column, Typed } from '../Column';
export type FloatColumnType<Type extends number = number, Default extends Type = Type> = Column<Type, Default> & {
    /**
     * Number of decimal places to display.
     * Controls the precision of the floating-point number.
     */
    scale?: number;
};
/**
 * A Column for a floating-point number type field.
 * Stores decimal numbers with floating-point precision.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **scale**? - number of decimal places to display (e.g., 2 for 99.99)
 *
 * @example
 * ```typescript
 * const price = FloatColumn({
 *     label: 'Price',
 *     default: 99.99,
 *     scale: 2,
 * })
 *
 * const temperature = FloatColumn({
 *     label: 'Temperature (Celsius)',
 *     default: 23.5,
 *     scale: 1,
 *     mandatory: true,
 * })
 * ```
 */
export declare function FloatColumn<const Type extends number, const Default extends Type, const C extends FloatColumnType<Type, Default>>(config: C & FloatColumnType<Type, Default>): Typed<Type, C>;
