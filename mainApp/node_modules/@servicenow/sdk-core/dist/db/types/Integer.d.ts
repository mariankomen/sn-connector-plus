import type { TableName } from '../Table';
import type { choiceDropdownType } from './Choice';
import type { Typed } from '../Column';
import type { ChoiceColumnType, ChoiceType, Choices, SuggestionOrChoiceType } from './Choice';
export type IntegerColumnType<TChoices extends Choices<number> = Choices<number>, Type extends SuggestionOrChoiceType<Dropdown, TChoices> = ChoiceType<TChoices>, Default extends Type = Type, Dropdown extends choiceDropdownType = choiceDropdownType> = ChoiceColumnType<TChoices, Type, Default, TableName, Dropdown> & {
    min?: number;
    max?: number;
};
/**
 * A Column for an integer type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **min**? - smallest allowed value. Note that this is just notional and not enforced by the platform.
 *   * **max**? - largest allowed value. Note that this is just notional and not enforced by the platform.
 */
export declare function IntegerColumn<const TChoices extends Choices<number>, const Type extends SuggestionOrChoiceType<Dropdown, TChoices>, const Default extends Type, const C extends IntegerColumnType<TChoices, Type, Default, Dropdown>, const Dropdown extends choiceDropdownType = 'none'>(config: C & IntegerColumnType<TChoices, Type, Default, Dropdown>): Typed<Type, C>;
