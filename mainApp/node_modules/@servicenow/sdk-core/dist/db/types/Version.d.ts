import type { Column, Typed } from '../Column';
/**
 * A Column for a version type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 */
export declare function VersionColumn<const Type extends `${bigint}.${bigint}.${bigint}`, const Default extends Type, const C extends Column<Type, Default>>(config: C & Column<Type, Default>): Typed<Type, C>;
