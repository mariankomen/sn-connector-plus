import type { Column, Typed } from '../Column';
import type { Record } from '../Record';
import type { TableName } from '..';
type RecordsColumnType<RefTable extends TableName = TableName, Type extends Record<RefTable>[] = Record<RefTable>[], Default extends Type = Type> = Column<Type, Default> & {
    referenceTable: RefTable;
};
/**
 * A Column for a list type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **referenceTable**? - table containing possible values for this list
 */
export declare function RecordsColumn<const RefTable extends TableName, const Type extends Record<RefTable>[], const Default extends Type, const C extends RecordsColumnType<RefTable, Type, Default>>(config: C & RecordsColumnType<RefTable, Type, Default>): Typed<Type, C>;
export {};
