"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DayOfWeekColumn = DayOfWeekColumn;
exports.DaysOfWeekColumn = DaysOfWeekColumn;
/**
 * A Column for a day of week field.
 * Stores a single day as a number (1-7) where 1=Monday, 2=Tuesday, ..., 7=Sunday.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *
 * @example
 * ```typescript
 * const dayOfWeek = DayOfWeekColumn({
 *     label: 'Day of Week',
 *     default: 1, // Monday
 * })
 *
 * const meetingDay = DayOfWeekColumn({
 *     label: 'Meeting Day',
 *     default: 5, // Friday
 * })
 * ```
 */
function DayOfWeekColumn(config) {
    return config;
}
/**
 * A Column for a days of week field.
 * Stores multiple days (1-7) as concatenated digits where 1=Monday, 2=Tuesday, ..., 7=Sunday.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *
 * @example
 * ```typescript
 * const daysOfWeek = DaysOfWeekColumn({
 *     label: 'Days of Week',
 *     default: '126', // Monday, Tuesday, Saturday
 * })
 *
 * const weekdays = DaysOfWeekColumn({
 *     label: 'Weekdays',
 *     default: '12345', // Monday-Friday
 * })
 * ```
 */
function DaysOfWeekColumn(config) {
    return config;
}
//# sourceMappingURL=DayOfWeek.js.map