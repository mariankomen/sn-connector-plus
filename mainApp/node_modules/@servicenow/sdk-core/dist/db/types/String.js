"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringColumn = StringColumn;
exports.MultiLineTextColumn = MultiLineTextColumn;
exports.ApprovalRulesColumn = ApprovalRulesColumn;
exports.SlushBucketColumn = SlushBucketColumn;
/**
 * A Column for a string type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 */
function StringColumn(config) {
    return config;
}
/**
 * A Column for a multiple line small text area field.
 * Allows multi-line text input without the complexity of rich text editing.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 *
 * @example
 * ```typescript
 * const description = MultiLineTextColumn({
 *     label: 'Description',
 *     maxLength: 1000,
 * })
 *
 * const notes = MultiLineTextColumn({
 *     label: 'Notes',
 *     default: 'Enter notes here...',
 * })
 * ```
 */
function MultiLineTextColumn(config) {
    return config;
}
/**
 * A Column for an approval rules field.
 * Stores approval workflow rules configuration.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 *
 * @example
 * ```typescript
 * const approvalRules = ApprovalRulesColumn({
 *     label: 'Approval Rules',
 * })
 *
 * const workflowRules = ApprovalRulesColumn({
 *     label: 'Workflow Rules',
 *     mandatory: true,
 * })
 * ```
 */
function ApprovalRulesColumn(config) {
    return config;
}
/**
 * A Column for a slush bucket field.
 * Provides a dual-list selection interface for moving items between available and selected lists.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **script**? - script method to populate the slush bucket options (e.g., 'getRoles()')
 *
 * @example
 * ```typescript
 * const selectedItems = SlushBucketColumn({
 *     label: 'Selected Items',
 * })
 *
 * // Using script method
 * const assignedRoles = SlushBucketColumn({
 *     label: 'Assigned Roles',
 *     script: 'getRoles()',
 *     mandatory: true,
 * })
 * ```
 */
function SlushBucketColumn(config) {
    return config;
}
//# sourceMappingURL=String.js.map