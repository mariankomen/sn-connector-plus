import type { Column, Typed } from '../Column';
/**
 * Day of week as a number (1-7, where 1=Monday, 2=Tuesday, ..., 7=Sunday).
 */
type DayOfWeekValue = 1 | 2 | 3 | 4 | 5 | 6 | 7;
type DayOfWeekColumnType<Type extends DayOfWeekValue = DayOfWeekValue, Default extends Type = Type> = Column<Type, Default>;
type DaysOfWeekColumnType<Type extends string = string, Default extends Type = Type> = Column<Type, Default>;
/**
 * A Column for a day of week field.
 * Stores a single day as a number (1-7) where 1=Monday, 2=Tuesday, ..., 7=Sunday.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *
 * @example
 * ```typescript
 * const dayOfWeek = DayOfWeekColumn({
 *     label: 'Day of Week',
 *     default: 1, // Monday
 * })
 *
 * const meetingDay = DayOfWeekColumn({
 *     label: 'Meeting Day',
 *     default: 5, // Friday
 * })
 * ```
 */
export declare function DayOfWeekColumn<const Type extends DayOfWeekValue = DayOfWeekValue, const Default extends Type = Type, const C extends DayOfWeekColumnType<Type, Default> = DayOfWeekColumnType<Type, Default>>(config: C & DayOfWeekColumnType<Type, Default>): Typed<Type, C>;
/**
 * A Column for a days of week field.
 * Stores multiple days (1-7) as concatenated digits where 1=Monday, 2=Tuesday, ..., 7=Sunday.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *
 * @example
 * ```typescript
 * const daysOfWeek = DaysOfWeekColumn({
 *     label: 'Days of Week',
 *     default: '126', // Monday, Tuesday, Saturday
 * })
 *
 * const weekdays = DaysOfWeekColumn({
 *     label: 'Weekdays',
 *     default: '12345', // Monday-Friday
 * })
 * ```
 */
export declare function DaysOfWeekColumn<const Type extends string = string, const Default extends Type = Type, const C extends DaysOfWeekColumnType<Type, Default> = DaysOfWeekColumnType<Type, Default>>(config: C & DaysOfWeekColumnType<Type, Default>): Typed<Type, C>;
export {};
