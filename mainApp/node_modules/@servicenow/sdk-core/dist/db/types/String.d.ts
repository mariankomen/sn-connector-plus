import type { choiceDropdownType } from './Choice';
import type { Column, Typed } from '../Column';
import type { TableName } from '../Table';
import type { ChoiceColumnType, Choices, SuggestionOrChoiceType } from './Choice';
export type StringColumnType<TChoices extends Choices<string> = Choices<string>, Dropdown extends choiceDropdownType = choiceDropdownType, Type extends SuggestionOrChoiceType<Dropdown, TChoices> = SuggestionOrChoiceType<Dropdown, TChoices>, Default extends Type = Type, ReferenceSchema extends TableName = TableName> = ChoiceColumnType<TChoices, Type, Default, ReferenceSchema, Dropdown> & {
    /**
     * Whether the string should be stored as full UTF-8.
     * column type will be string_full_utf8 if true
     */
    isFullUTF8?: boolean;
};
/**
 * A Column for a string type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 */
export declare function StringColumn<const TChoices extends Choices<string>, const Type extends SuggestionOrChoiceType<Dropdown, TChoices>, const Default extends Type, const C extends StringColumnType<TChoices, Dropdown, Type, Default, ReferenceSchema>, const ReferenceSchema extends TableName = TableName, const Dropdown extends choiceDropdownType = 'none'>(config: C & StringColumnType<TChoices, Dropdown, Type, Default, ReferenceSchema>): Typed<Type, C>;
/**
 * A Column for a multiple line small text area field.
 * Allows multi-line text input without the complexity of rich text editing.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 *
 * @example
 * ```typescript
 * const description = MultiLineTextColumn({
 *     label: 'Description',
 *     maxLength: 1000,
 * })
 *
 * const notes = MultiLineTextColumn({
 *     label: 'Notes',
 *     default: 'Enter notes here...',
 * })
 * ```
 */
export declare function MultiLineTextColumn<const Type extends string, const Default extends Type, const C extends Column<Type, Default>>(config: C & Column<Type, Default>): Typed<Type, C>;
/**
 * A Column for an approval rules field.
 * Stores approval workflow rules configuration.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 *
 * @example
 * ```typescript
 * const approvalRules = ApprovalRulesColumn({
 *     label: 'Approval Rules',
 * })
 *
 * const workflowRules = ApprovalRulesColumn({
 *     label: 'Workflow Rules',
 *     mandatory: true,
 * })
 * ```
 */
export declare function ApprovalRulesColumn<const Type extends string, const Default extends Type, const C extends Column<Type, Default>>(config: C & Column<Type, Default>): Typed<Type, C>;
type SlushBucketColumnType<Type extends string | unknown[] = string | unknown[], Default extends Type = Type> = Column<Type, Default> & {
    /**
     * Script method to populate the slush bucket options.
     * Example: 'getMethod()' - a server-side script that returns available options
     */
    script?: string;
};
/**
 * A Column for a slush bucket field.
 * Provides a dual-list selection interface for moving items between available and selected lists.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **script**? - script method to populate the slush bucket options (e.g., 'getRoles()')
 *
 * @example
 * ```typescript
 * const selectedItems = SlushBucketColumn({
 *     label: 'Selected Items',
 * })
 *
 * // Using script method
 * const assignedRoles = SlushBucketColumn({
 *     label: 'Assigned Roles',
 *     script: 'getRoles()',
 *     mandatory: true,
 * })
 * ```
 */
export declare function SlushBucketColumn<const Type extends string | unknown[], const Default extends Type, const C extends SlushBucketColumnType<Type, Default>>(config: C & SlushBucketColumnType<Type, Default>): Typed<Type, C>;
export {};
