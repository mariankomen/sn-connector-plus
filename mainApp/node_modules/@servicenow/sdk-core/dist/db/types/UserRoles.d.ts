import type { Role } from '../../app';
import type { Column, Typed } from '../Column';
/**
 * A Column for a user roles type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 */
export declare function UserRolesColumn<const Type extends (Role | string)[], const Default extends Type, const C extends Column<Type, Default>>(config: C & Column<Type, Default>): Typed<Type, C>;
