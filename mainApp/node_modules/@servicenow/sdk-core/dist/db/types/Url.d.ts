import type { Choices, SuggestionOrChoiceType } from './Choice';
import type { StringColumnType } from './String';
import type { Typed } from '../Column';
import type { TableName } from '../Table';
import type { choiceDropdownType } from './Choice';
/**
 * URL format validation (advisory only):
 * - Should start with http:// or https://
 * - Followed by domain and optional path
 * Note: Format hints are not strictly enforced by TypeScript
 */
type UrlColumnTypeFormat = `http://${string}` | `https://${string}`;
export type UrlColumnType<TChoices extends Choices<string> = Choices<string>, Dropdown extends choiceDropdownType = choiceDropdownType, Type extends SuggestionOrChoiceType<Dropdown, TChoices> & UrlColumnTypeFormat = SuggestionOrChoiceType<Dropdown, TChoices> & UrlColumnTypeFormat, Default extends Type = Type, ReferenceSchema extends TableName = TableName> = StringColumnType<TChoices, Dropdown, Type, Default, ReferenceSchema>;
/**
 * A Column for a URL type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 *   * **maxLength**? - maximum number of characters allowed in this field
 *   * **choices**? - optional choice values for dropdown
 *   * **dropdown**? - dropdown display style
 */
export declare function UrlColumn<const TChoices extends Choices<string>, const Type extends SuggestionOrChoiceType<Dropdown, TChoices> & UrlColumnTypeFormat, const Default extends Type, const C extends UrlColumnType<TChoices, Dropdown, Type, Default, TableName>, const Dropdown extends choiceDropdownType = 'none'>(config: C & UrlColumnType<TChoices, Dropdown, Type, Default, TableName>): Typed<Type, C>;
export {};
