import type { TableName } from '../Table';
import type { Column, Typed } from '../Column';
export type TableNameColumnType<Type extends TableName = TableName, Default extends Type = Type> = Column<Type, Default>;
/**
 * A Column for a table name type field.
 *
 * @see https://docs.servicenow.com/csh?topicname=table-api-now-ts.html&version=latest
 *
 * @param config - an object that can include all base `Column` properties
 */
export declare function TableNameColumn<const Type extends TableName, const Default extends Type, const C extends TableNameColumnType<Type, Default>>(config: C & TableNameColumnType<Type, Default>): Typed<Type, C>;
