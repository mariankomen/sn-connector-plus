// import { CommandModule } from 'yargs'
// import { logger } from '../../logger'
// import * as fs from 'fs'
// import { createTelemetryClient } from '../../telemetry'

// interface Arguments {
//     debug: boolean
// }

// export const upgrade: CommandModule<unknown, Arguments> = {
//     command: 'upgrade',
//     describe: 'Upgrade globally installed nowSDK to latest version',
//     handler: async function (arg): Promise<void> {
//         const telemetryClient = createTelemetryClient(fs, __dirname)
//         const telemetryTimer = telemetryClient.startTimerEvent('upgrade')

//         const { upgrade } = await import('@servicenow/sdk-cli-core/dist/command/upgrade/index.js')
//         try {
//             const result = await upgrade(__dirname, arg.debug, fs, logger)

//             await telemetryClient.sendEvent(telemetryTimer.end({ data: result }))
//         } catch (error) {
//             logger.error(error)

//             telemetryClient.error('upgrade', error)
//             process.exitCode = 1
//         }
//     },
// }
