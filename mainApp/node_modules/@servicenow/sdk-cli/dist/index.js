"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs = __importStar(require("yargs"));
const usage_1 = require("./usage");
const epilogue_1 = require("./epilogue");
const clean_1 = require("./command/clean");
const init_1 = require("./command/init");
const build_1 = require("./command/build");
const run_1 = require("./command/run");
const pack_1 = require("./command/pack");
const install_1 = require("./command/install");
const download_1 = require("./command/download");
const transform_1 = require("./command/transform");
const auth_1 = require("./command/auth");
const move_to_app_1 = require("./command/move-to-app");
const explain_1 = require("./command/explain");
const semver_1 = require("semver");
const logger_1 = require("./logger");
const package_json_1 = __importDefault(require("../package.json"));
const dependencies_1 = require("./command/dependencies");
try {
    yargs
        .check(() => {
        const neededVersion = package_json_1.default['engines']['node'];
        if (!(0, semver_1.satisfies)(process.version, neededVersion)) {
            logger_1.logger.error(`now-sdk requires node version to be ${neededVersion}`);
            process.exit(1);
        }
        return true;
    })
        .usage(usage_1.usage)
        .option('debug', {
        describe: 'Print debug output',
        alias: 'd',
        default: false,
        type: 'boolean',
    })
        .command(auth_1.auth)
        .command(init_1.init)
        .command(download_1.download)
        .command(build_1.build)
        .command(install_1.install)
        .command(dependencies_1.dependencies)
        .command(transform_1.transform)
        .command('run [script]', false, run_1.run)
        .command(clean_1.clean)
        .command(pack_1.pack)
        .command(explain_1.explain)
        .command('move', false, move_to_app_1.moveToApp)
        .demandCommand()
        .middleware(async (argv) => {
        if (argv.debug) {
            logger_1.logger.setLevel('debug');
            logger_1.logger.debug('Debug logging enabled');
        }
    })
        .epilogue(epilogue_1.epilogue)
        .strictCommands()
        .help()
        .updateStrings({
        'Positionals:': 'Parameters:',
    })
        .alias('h', 'help')
        .alias('v', 'version')
        .scriptName('now-sdk').argv;
}
catch (error) {
    logger_1.logger.error(error);
}
//# sourceMappingURL=index.js.map