"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getOAuthTokens = void 0;
exports.refreshAccessToken = refreshAccessToken;
const CodeGrant_1 = require("./CodeGrant");
const getOAuthTokens = async (host, logger) => {
    try {
        // initialize the oauth flow and wait for the user to authorize the app
        const token = await (0, CodeGrant_1.initOAuthFlow)(host, logger);
        if (token) {
            return {
                access_token: token.access_token,
                token_type: token.token_type,
                refresh_token: token.refresh_token,
                expires_at: token.expires_at,
            };
        }
        return;
    }
    catch (error) {
        logger?.error('Access Token Error', error);
        return;
    }
};
exports.getOAuthTokens = getOAuthTokens;
async function refreshAccessToken(creds, logger) {
    const { instanceUrl, refresh_token, expires_at } = creds;
    const now = Math.floor(Date.now() / 1000);
    const expiresIn = expires_at - now;
    const client = await (0, CodeGrant_1.oAuthClient)(instanceUrl);
    // check if the token is about to expire in 10 minutes
    if (expiresIn > 15 * 60) {
        return;
    }
    logger?.info('Access Token has expired, refreshing token');
    try {
        const token = await client.refresh(refresh_token);
        // get the access token from the store
        return {
            access_token: token.access_token,
            token_type: token.token_type,
            refresh_token: token.refresh_token,
            expires_at: token.expires_at,
        };
    }
    catch (error) {
        logger?.error('Error refreshing token, Use "$now-sdk auth --add <instance>" command to initiate OAuth and retrieve new access/refresh tokens', error);
        process.exit(1);
    }
}
//# sourceMappingURL=index.js.map