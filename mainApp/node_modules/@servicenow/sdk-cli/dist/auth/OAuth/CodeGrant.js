"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.initOAuthFlow = exports.oAuthClient = void 0;
const openid_client_1 = require("openid-client");
const open_1 = __importDefault(require("open"));
const prompts_1 = require("@inquirer/prompts");
const SDK_OAUTH_APP_CLIENT_ID = 'SDK_OAUTH_APP_CLIENT_ID';
const DEFAULT_SDK_OAUTH_APP_CLIENT_ID = '543e5655f77746a28228c6009a599dfb';
const oAuthClient = async (host, redirect_uri) => {
    const issuer = new openid_client_1.Issuer({
        issuer: 'ServiceNow',
        authorization_endpoint: `${host}/oauth_auth.do`,
        token_endpoint: `${host}/oauth_token.do`,
    });
    const client = new issuer.Client({
        client_id: process.env[SDK_OAUTH_APP_CLIENT_ID] ?? DEFAULT_SDK_OAUTH_APP_CLIENT_ID,
        ...(redirect_uri && { redirect_uris: [redirect_uri] }),
        response_types: ['code'],
        token_endpoint_auth_method: 'none',
    });
    return client;
};
exports.oAuthClient = oAuthClient;
const initOAuthFlow = async (host, logger) => {
    try {
        // generate a random state and code verifier
        const state = openid_client_1.generators.state();
        const codeVerifier = openid_client_1.generators.codeVerifier();
        const codeChallenge = openid_client_1.generators.codeChallenge(codeVerifier);
        const redirectUri = '/sdk-oauth.do';
        const client = await (0, exports.oAuthClient)(host, redirectUri);
        const authorizationUri = client.authorizationUrl({
            state,
            redirect_uri: redirectUri,
            code_challenge: codeChallenge,
            code_challenge_method: 'S256',
        });
        logger?.info(`Opening web browser to authenticate with ${host}, or visit ${authorizationUri} to continue OAuth...`);
        try {
            await (0, open_1.default)(authorizationUri);
        }
        catch (error) {
            logger?.warn('Could not open the browser automatically. Please navigate to this URL to continue authentication (we will attempt to copy it to your clipboard automatically):');
            logger?.info(authorizationUri);
            try {
                const { default: clipboard } = await import('clipboardy');
                clipboard.writeSync(authorizationUri);
            }
            catch (e) {
                //leaving empty. it's just nice if they get the clipboard but not necessary
            }
        }
        const authCode = await (0, prompts_1.password)({
            message: 'Copy the code from the browser and paste it here: ',
        });
        const tokenSet = await client.oauthCallback(redirectUri, { code: authCode, state }, {
            code_verifier: codeVerifier,
            state,
            response_type: 'code',
        });
        return tokenSet;
    }
    catch (error) {
        logger?.error('Access Token Error', error);
        return;
    }
};
exports.initOAuthFlow = initOAuthFlow;
//# sourceMappingURL=CodeGrant.js.map