import type { Logger } from '@servicenow/sdk-build-core';
export type OAuthTokenSet = {
    access_token: string;
    token_type: string;
    refresh_token: string;
    expires_at: number;
};
export declare const getOAuthTokens: (host: string, logger?: Logger) => Promise<OAuthTokenSet | undefined>;
export declare function refreshAccessToken(creds: {
    instanceUrl: string;
    refresh_token: string;
    expires_at: number;
}, logger?: Logger): Promise<OAuthTokenSet | undefined>;
