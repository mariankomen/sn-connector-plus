import { CookieJar } from 'tough-cookie';
import type { Logger } from '@servicenow/sdk-build-core';
import type { UserSession } from '..';
export declare class UISession {
    private readonly instanceUrl;
    private readonly logger?;
    userToken: string;
    cookieJar: CookieJar;
    constructor(instanceUrl: string, logger?: Logger | undefined);
    private parseUserToken;
    private refreshUserToken;
    private instanceLogin;
    login(username: string, password: string): Promise<UserSession>;
}
