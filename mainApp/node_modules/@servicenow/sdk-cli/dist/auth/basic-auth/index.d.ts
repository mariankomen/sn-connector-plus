import type { Logger } from '@servicenow/sdk-build-core';
export declare function getUISessionToken(creds: {
    instanceUrl: string;
    username: string;
    password: string;
}, logger?: Logger): Promise<import("..").UserSession | undefined>;
