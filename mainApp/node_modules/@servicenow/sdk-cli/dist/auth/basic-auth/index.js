"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUISessionToken = getUISessionToken;
const UISession_1 = require("./UISession");
async function getUISessionToken(creds, logger) {
    const { username, password, instanceUrl } = creds;
    const session = new UISession_1.UISession(instanceUrl, logger);
    try {
        // verify basic auth credentials and get user token
        return session.login(username, password);
    }
    catch (error) {
        logger?.error(`Login to instance ${instanceUrl} failed: ${error.message}`);
        return;
    }
}
//# sourceMappingURL=index.js.map