"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.withProfiling = withProfiling;
const inspector = __importStar(require("node:inspector/promises"));
const logger_1 = require("../logger");
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
// These values come from the Chrome DevTools Protocol:
// https://chromedevtools.github.io/devtools-protocol/tot/Profiler/
const PROFILER_ENABLE = 'Profiler.enable';
const PROFILER_START = 'Profiler.start';
const PROFILER_STOP = 'Profiler.stop';
const PROFILER_DISABLE = 'Profiler.disable';
async function startProfiling(session) {
    session.connect();
    await session.post(PROFILER_ENABLE);
    await session.post(PROFILER_START);
    logger_1.logger.info('Profiling started');
}
async function stopProfiling(session) {
    const { profile } = (await session.post(PROFILER_STOP));
    await session.post(PROFILER_DISABLE);
    session.disconnect();
    return profile;
}
async function writeProfilingResults(name, fs, profile) {
    const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
    const outputPath = sdk_build_core_1.path.resolve(`${name}-${timestamp}.cpuprofile`);
    const dir = sdk_build_core_1.path.dirname(outputPath);
    if (!sdk_build_core_1.FileSystem.existsSync(fs, dir)) {
        fs.mkdirSync(dir, { recursive: true });
    }
    fs.writeFileSync(outputPath, JSON.stringify(profile));
    logger_1.logger.info(`Profile written to ${outputPath}`);
}
async function withProfiling(enabled, name, fs, fn) {
    if (!enabled) {
        return fn();
    }
    const session = new inspector.Session();
    await startProfiling(session);
    try {
        return await fn();
    }
    finally {
        const profile = await stopProfiling(session);
        await writeProfilingResults(name, fs, profile);
    }
}
//# sourceMappingURL=profiler.js.map