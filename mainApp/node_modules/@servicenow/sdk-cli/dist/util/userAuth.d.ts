import { LazyCredential } from '@servicenow/sdk-api';
import type { SessionOrToken } from '../auth';
export declare function userSessionToCredential(session: SessionOrToken): LazyCredential;
export declare function getAuthCookie(auth: SessionOrToken): string | undefined;
