import { FileSystem } from '@servicenow/sdk-build-core';
export declare function createDirIfNotExists(dirPath: string, fs: FileSystem): void;
/**
 * Finds the first package.json in the path
 * Copied from 'find-root' package to remove direct dependency on 'fs'
 * @param start path to start from
 * @param fs FileSystem
 * @returns path to first package.json in path
 */
export declare function findRoot(start: string | string[], fs: FileSystem): string;
