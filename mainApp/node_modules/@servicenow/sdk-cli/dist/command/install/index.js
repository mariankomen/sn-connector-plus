"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.install = void 0;
exports.getSummaryURL = getSummaryURL;
const util_1 = require("../../util");
const process_1 = require("process");
const logger_1 = require("../../logger");
const fs = __importStar(require("fs"));
const open_1 = __importDefault(require("open"));
const telemetry_1 = require("../../telemetry");
const sdk_api_1 = require("@servicenow/sdk-api");
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const auth_1 = require("../../auth");
exports.install = {
    command: 'install',
    aliases: 'deploy',
    describe: 'Install or update application on instance',
    builder: async (yargs) => {
        return yargs
            .option('source', {
            describe: `Path to the directory that contains package.json configuration`,
            default: (0, process_1.cwd)(),
            type: 'string',
        })
            .option('reinstall', {
            describe: 'Uninstall and reinstall the application on the instance to ensure metadata on instance matches installation package.\n' +
                'Warning: Metadata created on-instance that is not present locally will be lost',
            type: 'boolean',
            alias: 'r',
            default: false,
        })
            .option('auth', {
            describe: 'Credential alias to use for authentication with instance',
            type: 'string',
            alias: 'a',
        })
            .option('open-browser', {
            describe: 'Open sys_app page in the default browser on successful install',
            default: false,
            type: 'boolean',
            alias: 'b',
        })
            .option('info', {
            describe: 'Get information from instance for most recent install of this app',
            default: false,
            type: 'boolean',
            alias: 'i',
        })
            .option('store', {
            describe: 'Install the app as a sys_store_app record instead of a sys_app custom application',
            default: false,
            type: 'boolean',
            alias: 's',
            hidden: true,
        })
            .option('async', {
            describe: 'Install the app asynchronously',
            default: false,
            type: 'boolean',
            hidden: true,
        })
            .option('maven', {
            describe: 'Install *-app.zip output from maven assembly',
            default: false,
            type: 'boolean',
            hidden: true,
        })
            .option('targetUpdateSet', {
            describe: 'Upload the output to the target update set',
            type: 'string',
            hidden: true,
        })
            .option('demoData', {
            describe: 'Install demo data',
            type: 'boolean',
            default: true,
        });
    },
    handler: async (args) => {
        let telemetryClient;
        try {
            const credential = await (0, auth_1.credentialProvider)(args.auth);
            telemetryClient = (0, telemetry_1.createTelemetryClient)(fs, __dirname, credential.getUrl());
            if (args.info) {
                logger_1.logger.info(`View install information in the Upgrade History table at ${getSummaryURL(credential.getUrl())}`);
                return;
            }
            logger_1.logger.info('Starting installation...');
            const workingDir = (0, util_1.validateWorkingDir)(args.source);
            const config = sdk_build_core_1.NowConfig.parseFromDirectory(workingDir, fs, logger_1.logger);
            const project = new sdk_api_1.Project({ config, fileSystem: fs, rootDir: workingDir, logger: logger_1.logger, telemetryClient });
            const orchestrator = new sdk_api_1.Orchestrator(project, credential);
            const { scopeId, type } = project.getConfig();
            if (type === 'configuration') {
                const updateSetResult = await orchestrator.installConfigurations(args.targetUpdateSet ? { targetUpdateSetId: args.targetUpdateSet } : undefined);
                logger_1.logger.info(`Changes installed successfully to update set: ${updateSetResult}`);
            }
            else {
                await orchestrator.install({
                    clean: args.reinstall,
                    installAsStoreApp: args.store,
                    installAsync: args.async,
                    demoData: args.demoData,
                });
                const sysAppUrl = getSysAppURL(credential.getUrl(), scopeId, args.store);
                logger_1.logger.info(`Installation completed. Access the application at: ${sysAppUrl}`);
                if (args.openBrowser) {
                    await (0, open_1.default)(sysAppUrl);
                }
            }
        }
        catch (error) {
            logger_1.logger.error(error);
            process.exitCode = 1;
        }
    },
};
function getSysAppURL(host, sysId, isStoreApp) {
    const table = isStoreApp ? 'sys_store_app' : 'sys_app';
    return `${host.origin}/${table}.do?sys_id=${sysId}`;
}
function getSummaryURL(host, sysId) {
    if (sysId) {
        return `${host.origin}/nav_to.do?uri=sys_upgrade_history.do?sys_id=${sysId}`;
    }
    return `${host.origin}/sys_upgrade_history_list.do`;
}
//# sourceMappingURL=index.js.map