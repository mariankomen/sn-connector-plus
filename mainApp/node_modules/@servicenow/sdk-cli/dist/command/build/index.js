"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.build = void 0;
const process_1 = require("process");
const fs = __importStar(require("fs"));
const util_1 = require("../../util");
const profiler_1 = require("../../util/profiler");
const logger_1 = require("../../logger");
const telemetry_1 = require("../../telemetry");
const sdk_api_1 = require("@servicenow/sdk-api");
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
exports.build = {
    command: 'build [source]',
    describe: 'Compile sources into app files and generate installable package',
    builder: (yargs) => {
        return yargs
            .positional('source', {
            describe: `Path to the directory that contains package.json configuration`,
            default: (0, process_1.cwd)(),
            type: 'string',
        })
            .option('frozenKeys', {
            describe: 'Validate that Keys/SysIds are up to date for CI build',
            default: false,
            type: 'boolean',
        })
            .option('ids', {
            describe: `Array of sysIds of records to build`,
            type: 'array',
            string: true,
            hidden: true,
        })
            .option('profile', {
            describe: 'Profile build and output a .cpuprofile file',
            type: 'boolean',
            default: false,
            hidden: true,
        });
    },
    handler: async (args) => {
        await (0, profiler_1.withProfiling)(args.profile, 'build', fs, async () => {
            logger_1.logger.info(`Initiating the build process ${args.frozenKeys ? 'with keys validation' : ''}...`);
            const telemetryClient = (0, telemetry_1.createTelemetryClient)(fs, __dirname);
            logger_1.logger.info(`Building project from ${args.source}...`);
            try {
                const workingDir = (0, util_1.validateWorkingDir)(args.source);
                const config = sdk_build_core_1.NowConfig.parseFromDirectory(workingDir, fs, logger_1.logger);
                const project = new sdk_api_1.Project({ config, fileSystem: fs, rootDir: workingDir, logger: logger_1.logger, telemetryClient });
                const orchestrator = new sdk_api_1.Orchestrator(project);
                const output = await orchestrator.build({ frozenKeys: args.frozenKeys, sysIds: args.ids });
                if (!output.success) {
                    logger_1.logger.error('Build failed due to errors');
                    process.exitCode = 1; // set exit code to 1 to indicate failure for CI
                    return;
                }
                logger_1.logger.debug('Build output\n' + output.files.map((f) => f.path).join('\n'));
                logger_1.logger.info('Build completed successfully');
            }
            catch (error) {
                logger_1.logger.error('Build failed:', error);
                process.exitCode = 1; // set exit code to 1 to indicate failure for CI
            }
        });
    },
};
//# sourceMappingURL=index.js.map