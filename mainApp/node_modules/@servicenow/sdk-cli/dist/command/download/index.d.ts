import type { CommandModule } from 'yargs';
interface Arguments {
    directory: string;
    source: string;
    incremental: boolean;
}
export declare const download: CommandModule<unknown, Arguments>;
export {};
