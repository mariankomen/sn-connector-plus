import type { Logger } from '@servicenow/sdk-build-core';
declare class CLILogger implements Logger {
    private readonly level;
    private readonly logger;
    readonly info: Logger['info'];
    readonly debug: Logger['debug'];
    readonly warn: Logger['warn'];
    readonly error: Logger['error'];
    constructor(level?: LogLevel);
    setLevel(level: LogLevel): void;
}
export type LogLevel = 'info' | 'debug' | 'warn' | 'error';
export declare const logger: CLILogger;
export {};
