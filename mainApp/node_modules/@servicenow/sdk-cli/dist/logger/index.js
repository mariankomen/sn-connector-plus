"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.logger = void 0;
const winston_1 = __importDefault(require("winston"));
const chalk_1 = __importDefault(require("chalk"));
class CLILogger {
    level;
    logger;
    info;
    debug;
    warn;
    error;
    constructor(level = 'info') {
        this.level = level;
        function printLogLevel(level) {
            if (['error', 'warn', 'debug'].includes(level)) {
                return ` ${level.toUpperCase()}:`;
            }
            return '';
        }
        this.logger = winston_1.default.createLogger({
            level: this.level,
            format: winston_1.default.format.printf(({ level, message, stack }) => {
                const stacktrace = stack && this.logger.level === 'debug' ? `\n${stack}` : '';
                const logMessage = `[now-sdk]${printLogLevel(level)} ${message}${stacktrace}`;
                switch (level) {
                    case 'error':
                        return chalk_1.default.red(logMessage);
                    case 'warn':
                        return chalk_1.default.yellow(logMessage);
                    default:
                        return logMessage;
                }
            }),
            transports: [new winston_1.default.transports.Console()],
        });
        this.info = this.logger.info.bind(this.logger);
        this.warn = this.logger.warn.bind(this.logger);
        this.debug = this.logger.debug.bind(this.logger);
        this.error = this.logger.error.bind(this.logger);
        if (this.level !== 'debug' && process.env['LOG_LEVEL']) {
            this.level = process.env['LOG_LEVEL'];
            this.info(`Setting log level to ${this.level} from process.env.LOG_LEVEL`);
        }
    }
    setLevel(level) {
        this.logger.level = level;
    }
}
exports.logger = new CLILogger();
//# sourceMappingURL=index.js.map