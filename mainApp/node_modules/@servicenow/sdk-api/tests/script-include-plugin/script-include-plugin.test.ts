import { Project } from '../../src'
import { compareOutputXml } from '../test-utils'
import { path } from '@servicenow/sdk-build-core'

describe('ScriptInclude plugin test suite', () => {
    describe('Fluent to XML', () => {
        const project = new Project({
            config: {
                scope: 'x_snc_test',
                scopeId: 'aaaa1111bbbb2222cccc3333dddd4444',
            },
            packageJson: {
                name: 'x_snc_test',
                version: '1.0.0',
            },
        })

        afterAll(() => {
            project?.dispose()
        })

        afterEach(() => {
            project
                .getFiles()
                .filter((f) => f.getPath().endsWith('now.ts') || f.getPath().endsWith('.js'))
                .forEach((file) => {
                    project.removeFile(file)
                })
        })

        test('convert script include private', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            const file = project.addFile({
                path: 'si.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: 'test',
      script: "gs.info('hi');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      active: false,
    })
    `,
            })

            const output = await file.getOutput()
            expect(output).toHaveLength(1)

            const diagnostics = await file.getDiagnostics()
            if (diagnostics.length > 0) {
                diagnostics.forEach((d) => d.print(console))
            }
            expect(diagnostics).toHaveLength(0)

            const xml = output[0]
            expect(xml).toBeDefined()

            const diffs = compareOutputXml(
                xml.content,
                `<?xml version="1.0"?>
    <record_update>
      <sys_script_include action="INSERT_OR_UPDATE">
        <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
        <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
        <api_name>x_snc_test.test</api_name>
        <caller_access/>
        <client_callable>true</client_callable>
        <description/>
        <mobile_callable>false</mobile_callable>
        <name>test</name>
        <sandbox_callable>false</sandbox_callable>
        <script><![CDATA[gs.info('hi');]]></script>
        <access>package_private</access>
        <active>false</active>
      </sys_script_include>
    </record_update>`
            )

            if (diffs.length > 0) {
                console.table(diffs)
            }
            expect(diffs).toHaveLength(0)
        })

        test('convert script include public', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            const file = project.addFile({
                path: 'si.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: 'test',
      apiName: 'x_snc_test.test2',
      script: "gs.info('hi');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      active: false,
      accessibleFrom: 'public',
      callerAccess: 'restriction'
    })
    `,
            })

            const output = await file.getOutput()
            expect(output).toHaveLength(1)

            const diagnostics = await file.getDiagnostics()
            if (diagnostics.length > 0) {
                diagnostics.forEach((d) => d.print(console))
            }
            expect(diagnostics).toHaveLength(0)

            const xml = output[0]
            expect(xml).toBeDefined()

            const diffs = compareOutputXml(
                xml.content,
                `<?xml version="1.0"?>
    <record_update>
      <sys_script_include action="INSERT_OR_UPDATE">
        <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
        <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
        <api_name>x_snc_test.test2</api_name>
        <caller_access>2</caller_access>
        <client_callable>true</client_callable>
        <description/>
        <mobile_callable>false</mobile_callable>
        <name>test</name>
        <sandbox_callable>false</sandbox_callable>
        <script><![CDATA[gs.info('hi');]]></script>
        <access>public</access>
        <active>false</active>
      </sys_script_include>
    </record_update>`
            )

            if (diffs.length > 0) {
                console.table(diffs)
            }
            expect(diffs).toHaveLength(0)
        })

        test('convert script include without callerAccess', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            const file = project.addFile({
                path: 'si.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: 'test',
      apiName: 'x_snc_test.test2',
      script: "gs.info('hi');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      active: false,
      accessibleFrom: 'public',
    })
    `,
            })

            const output = await file.getOutput()
            expect(output).toHaveLength(1)

            const diagnostics = await file.getDiagnostics()
            if (diagnostics.length > 0) {
                diagnostics.forEach((d) => d.print(console))
            }
            expect(diagnostics).toHaveLength(0)

            const xml = output[0]
            expect(xml).toBeDefined()

            const diffs = compareOutputXml(
                xml.content,
                `<?xml version="1.0"?>
    <record_update>
      <sys_script_include action="INSERT_OR_UPDATE">
        <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
        <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
        <api_name>x_snc_test.test2</api_name>
        <caller_access/>
        <client_callable>true</client_callable>
        <description/>
        <mobile_callable>false</mobile_callable>
        <name>test</name>
        <sandbox_callable>false</sandbox_callable>
        <script><![CDATA[gs.info('hi');]]></script>
        <access>public</access>
        <active>false</active>
      </sys_script_include>
    </record_update>`
            )

            if (diffs.length > 0) {
                console.table(diffs)
            }
            expect(diffs).toHaveLength(0)
        })

        test('convert script include with now include', async () => {
            const sourceFile = project.addFile({
                path: 'testtwo.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: 'test',
      apiName: 'x_snc_test.test',
      script: Now.include('./testtwo.server.js'),
      callerAccess: 'restriction',
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      accessibleFrom: 'public',
      active: true,
    })
    `,
            })

            project.addFile({
                path: 'testtwo.server.js',
                content: `gs.info('now.include test');`,
            })

            const output = await sourceFile.getOutput()
            expect(output).toHaveLength(1)

            const xml = output[0]
            expect(xml).toBeDefined()

            const diffs = compareOutputXml(
                xml.content,
                `<?xml version="1.0"?>
    <record_update>
      <sys_script_include action="INSERT_OR_UPDATE">
        <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
        <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
        <access>public</access>
        <api_name>x_snc_test.test</api_name>
        <caller_access>2</caller_access>
        <client_callable>true</client_callable>
        <description/>
        <mobile_callable>false</mobile_callable>
        <name>test</name>
        <sandbox_callable>false</sandbox_callable>
        <script><![CDATA[gs.info('now.include test');]]></script>
        <active>true</active>
      </sys_script_include>
    </record_update>`
            )

            if (diffs.length > 0) {
                throw new Error(
                    `XML output does not match expected output. Differences:\n${diffs.map((d) => d.message).join('\n')}`
                )
            }
        })

        test('validate diagnostic errors', async () => {
            const sourceFile = project.addFile({
                path: 'testtwo.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: '123123test',
      apiName: 'test',
      script: "gs.info('now.include test');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      accessibleFrom: 'public',
      active: true
    })
    `,
            })

            const diagnostics = await sourceFile.getDiagnostics()
            expect(diagnostics).toHaveLength(3)
            expect(diagnostics[0].message).toBe("Type '\"test\"' is not assignable to type '`${string}.${string}`'.")
            expect(diagnostics[1].message).toBe('name must be a valid JavaScript identifier')
            expect(diagnostics[2].message).toBe(`apiName must begin with '${project.getConfig().scope}.'`)
        })

        test('DEF0729482: allow users to set sys_policy on ScriptInclude', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            const file = project.addFile({
                path: 'si.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['test1'],
      name: 'test',
      apiName: 'x_snc_test.test2',
      script: "gs.info('hi');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      active: false,
      accessibleFrom: 'public',
      callerAccess: 'restriction',
      protectionPolicy: 'read',
    })
    `,
            })

            const output = await file.getOutput()
            expect(output).toHaveLength(1)

            const diagnostics = await file.getDiagnostics()
            if (diagnostics.length > 0) {
                diagnostics.forEach((d) => d.print(console))
            }
            expect(diagnostics).toHaveLength(0)

            const xml = output[0]
            expect(xml).toBeDefined()

            const diffs = compareOutputXml(
                xml.content,
                `<?xml version="1.0"?>
    <record_update>
      <sys_script_include action="INSERT_OR_UPDATE">
        <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
        <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
        <api_name>x_snc_test.test2</api_name>
        <caller_access>2</caller_access>
        <client_callable>true</client_callable>
        <description/>
        <mobile_callable>false</mobile_callable>
        <name>test</name>
        <sandbox_callable>false</sandbox_callable>
        <script><![CDATA[gs.info('hi');]]></script>
        <access>public</access>
        <active>false</active>
        <sys_policy>read</sys_policy>
      </sys_script_include>
    </record_update>`
            )

            if (diffs.length > 0) {
                console.table(diffs)
            }
            expect(diffs).toHaveLength(0)
        })

        test('handles ]]> in script CDATA properly', async () => {
            project.registerExplicitId('sys_script_include', 'cdata_test', '9cd8740c05e6473e8b830a3c7091a17f')

            const file = project.addFile({
                path: 'si-cdata.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

    ScriptInclude({
      $id: Now.ID['cdata_test'],
      name: 'cdataTest',
      script: "gs.info('Script with ]]> sequence in it');",
      clientCallable: true,
      mobileCallable: false,
      sandboxCallable: false,
      active: true,
    })
    `,
            })

            const output = await file.getOutput()
            expect(output).toHaveLength(1)

            const diagnostics = await file.getDiagnostics()
            if (diagnostics.length > 0) {
                diagnostics.forEach((d) => d.print(console))
            }
            expect(diagnostics).toHaveLength(0)

            const xml = output[0]
            expect(xml).toBeDefined()

            // Verify that ]]> is properly escaped in CDATA section
            expect(xml.content).toContain(
                "<script><![CDATA[gs.info('Script with ]]]]><![CDATA[> sequence in it');]]></script>"
            )
        })
    })

    describe('XML to Fluent', () => {
        const project = new Project({
            config: {
                scope: 'x_snc_test',
                scopeId: 'aaaa1111bbbb2222cccc3333dddd4444',
            },
            packageJson: {
                name: 'x_snc_test',
                version: '1.0.0',
            },
        })

        afterAll(() => {
            project?.dispose()
        })

        afterEach(() => {
            project
                .getFiles()
                .filter((f) => f.getPath().endsWith('now.ts') || f.getPath().endsWith('.js'))
                .forEach((file) => {
                    project.removeFile(file)
                })
        })

        test('convert to ScriptInclude', async () => {
            const sysId = '9cd8740c05e6473e8b830a3c7091a17f'
            project.registerExplicitId('sys_script_include', 'test1', sysId)

            const transformed = await project.transform({
                path: 'scriptinclude.xml',
                content: `<?xml version="1.0"?>
<record_update>
  <sys_script_include action="INSERT_OR_UPDATE">
    <sys_id>${sysId}</sys_id>
    <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
    <access>public</access>
    <api_name>x_snc_test.test</api_name>
    <caller_access>1</caller_access>
    <client_callable>true</client_callable>
    <description>some description</description>
    <mobile_callable>true</mobile_callable>
    <name>test</name>
    <sandbox_callable>true</sandbox_callable>
    <active>true</active>
    <script><![CDATA[gs.info('now.include test');]]></script>
  </sys_script_include>
</record_update>`,
            })

            expect(transformed.changedFiles.length).toBe(2) // Should generate a Fluent file as well as JS file for the included script
            const fluentFile = transformed.changedFiles.find((f) => f.getPath().endsWith('scriptinclude.now.ts'))
            const scriptFile = transformed.changedFiles.find((f) => f.getPath().endsWith('.js'))

            expect(fluentFile).toBeDefined()
            expect(scriptFile).toBeDefined()

            expect(fluentFile.getContent()).toBe(`import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['${sysId}'],
    name: 'test',
    script: Now.include('./sys_script_include_${sysId}.server.js'),
    description: 'some description',
    apiName: 'x_snc_test.test',
    callerAccess: 'tracking',
    clientCallable: true,
    mobileCallable: true,
    sandboxCallable: true,
    accessibleFrom: 'public',
    active: true,
})
`)

            expect(scriptFile.getContent()).toBe("gs.info('now.include test');")
        })

        test('update script include', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            project.addFile({
                path: 'scriptinclude.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['9cd8740c05e6473e8b830a3c7091a17f'],
    name: 'test',
    script: Now.include('./test.server.js'),
    apiName: 'x_snc_test.test',
    callerAccess: 'restriction',
    clientCallable: true,
    mobileCallable: true,
    sandboxCallable: true,
    accessibleFrom: 'public',
    active: true,
})`,
            })

            project.addFile({
                path: 'test.server.js',
                content: `gs.info('now.include test');`,
            })

            const transformed = await project.transform({
                path: 'scriptinclude.xml',
                content: `<?xml version="1.0"?>
<record_update>
  <sys_script_include action="INSERT_OR_UPDATE">
    <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
    <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
    <access>package_private</access>
    <api_name>x_snc_test.test</api_name>
    <caller_access>1</caller_access>
    <client_callable>false</client_callable>
    <description/>
    <mobile_callable>false</mobile_callable>
    <name>test2</name>
    <sandbox_callable>false</sandbox_callable>
    <script><![CDATA[gs.info('now.include test update');]]></script>
    <active>false</active>
  </sys_script_include>
</record_update>`,
            })

            expect(transformed.changedFiles.length).toBe(2)
            const sourceFile = transformed.changedFiles.find((f) => f.getPath().endsWith('scriptinclude.now.ts'))
            const includeFile = transformed.changedFiles.find((f) => f.getPath().endsWith('test.server.js'))

            expect(sourceFile).toBeDefined()
            expect(includeFile).toBeDefined()

            expect(sourceFile.getContent()).toBe(`import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['9cd8740c05e6473e8b830a3c7091a17f'],
    name: 'test2',
    script: Now.include('./test.server.js'),
    apiName: 'x_snc_test.test',
    callerAccess: 'tracking',
    clientCallable: false,
    mobileCallable: false,
    sandboxCallable: false,
    accessibleFrom: 'package_private',
    active: false,
})
`)

            expect(includeFile.getContent()).toBe("gs.info('now.include test update')\n")
            expect(path.dirname(sourceFile.getPath())).toEqual(path.dirname(includeFile.getPath()))
        })

        test('update script include without caller_access', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            project.addFile({
                path: 'scriptinclude.now.ts',
                content: `import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['9cd8740c05e6473e8b830a3c7091a17f'],
    name: 'test',
    script: Now.include('./test.server.js'),
    apiName: 'x_snc_test.test',
    callerAccess: 'restriction',
    clientCallable: true,
    mobileCallable: true,
    sandboxCallable: true,
    accessibleFrom: 'public',
    active: true,
})`,
            })

            project.addFile({
                path: 'test.server.js',
                content: `gs.info('now.include test');`,
            })

            const transformed = await project.transform({
                path: 'scriptinclude.xml',
                content: `<?xml version="1.0"?>
<record_update>
  <sys_script_include action="INSERT_OR_UPDATE">
    <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
    <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
    <access>public</access>
    <api_name>x_snc_test.test</api_name>
    <caller_access/>
    <client_callable>true</client_callable>
    <description/>
    <mobile_callable>true</mobile_callable>
    <name>test</name>
    <sandbox_callable>true</sandbox_callable>
    <script><![CDATA[gs.info('now.include test update');]]></script>
    <active>true</active>
  </sys_script_include>
</record_update>`,
            })

            expect(transformed.changedFiles.length).toBe(2)
            const sourceFile = transformed.changedFiles.find((f) => f.getPath().endsWith('scriptinclude.now.ts'))
            const includeFile = transformed.changedFiles.find((f) => f.getPath().endsWith('test.server.js'))

            expect(sourceFile).toBeDefined()
            expect(includeFile).toBeDefined()

            expect(sourceFile.getContent()).toBe(`import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['9cd8740c05e6473e8b830a3c7091a17f'],
    name: 'test',
    script: Now.include('./test.server.js'),
    apiName: 'x_snc_test.test',
    callerAccess: undefined,
    clientCallable: true,
    mobileCallable: true,
    sandboxCallable: true,
    accessibleFrom: 'public',
    active: true,
})
`)

            expect(includeFile.getContent()).toBe("gs.info('now.include test update')\n")
            expect(path.dirname(sourceFile.getPath())).toEqual(path.dirname(includeFile.getPath()))
        })

        test('DEF0729482: transform sets sys_policy in ScriptInclude', async () => {
            project.registerExplicitId('sys_script_include', 'test1', '9cd8740c05e6473e8b830a3c7091a17f')

            const transformed = await project.transform({
                path: 'scriptinclude.xml',
                content: `<?xml version="1.0"?>
<record_update>
  <sys_script_include action="INSERT_OR_UPDATE">
    <sys_id>9cd8740c05e6473e8b830a3c7091a17f</sys_id>
    <sys_scope display_value="x_snc_test">aaaa1111bbbb2222cccc3333dddd4444</sys_scope>
    <access>public</access>
    <api_name>x_snc_test.test</api_name>
    <caller_access>1</caller_access>
    <client_callable>true</client_callable>
    <description>some description</description>
    <mobile_callable>true</mobile_callable>
    <name>test</name>
    <sandbox_callable>true</sandbox_callable>
    <active>true</active>
    <script><![CDATA[gs.info('now.include test');]]></script>
    <sys_policy>read</sys_policy>
  </sys_script_include>
</record_update>`,
            })

            expect(transformed.changedFiles.length).toBe(2)
            const sourceFile = transformed.changedFiles.find((f) => f.getPath().endsWith('scriptinclude.now.ts'))

            expect(sourceFile).toBeDefined()

            const content = sourceFile.getContent()

            expect(content).toBe(`import { ScriptInclude } from '@servicenow/sdk/core'

ScriptInclude({
    $id: Now.ID['9cd8740c05e6473e8b830a3c7091a17f'],
    name: 'test',
    script: Now.include('./sys_script_include_9cd8740c05e6473e8b830a3c7091a17f.server.js'),
    description: 'some description',
    apiName: 'x_snc_test.test',
    callerAccess: 'tracking',
    clientCallable: true,
    mobileCallable: true,
    sandboxCallable: true,
    accessibleFrom: 'public',
    active: true,
    protectionPolicy: 'read',
})
`)
        })
    })
})
