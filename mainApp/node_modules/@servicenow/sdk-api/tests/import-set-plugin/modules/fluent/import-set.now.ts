import { ImportSet } from '@servicenow/sdk/core'
import { transformMapScript } from '../server/transform-map-script.js'

ImportSet({
    $id: Now.ID['test_transform_map_id'],
    name: 'User Import with Module',
    targetTable: 'sys_user',
    sourceTable: 'u_import_users',
    runScript: true,
    script: transformMapScript,
    fields: {
        user_name: 'username',
    },
})
