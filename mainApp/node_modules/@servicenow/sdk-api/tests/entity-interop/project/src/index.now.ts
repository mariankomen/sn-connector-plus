import { ApplicationMenu, Record } from '@servicenow/sdk/core'

export const appMenu = ApplicationMenu({
    $id: Now.ID[1],
    title: 'Test Menu',
})

export const appModule = Record({
    $id: Now.ID[2],
    table: 'sys_app_module',
    data: {
        title: 'Test Module',
        application: appMenu,
    },
})
