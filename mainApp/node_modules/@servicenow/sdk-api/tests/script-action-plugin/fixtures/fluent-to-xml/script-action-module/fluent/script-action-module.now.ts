import { ScriptAction } from '@servicenow/sdk/core'
import { scriptAction } from '../server/module.ts'

ScriptAction({
    $id: Now.ID['sample-script-action'],
    name: 'SampleScriptAction2',
    description: 'some description new',
    script: scriptAction,
    eventName: 'some.otherevent',
    order: 100,
})
