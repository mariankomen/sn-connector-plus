import { ScriptAction } from '@servicenow/sdk/core'
import { scriptAction } from '../server/script-action-utils'

ScriptAction({
    $id: Now.ID['script-action-with-module'],
    name: 'Script action using module',
    script: scriptAction,
    eventName: 'eat.tacos',
})
