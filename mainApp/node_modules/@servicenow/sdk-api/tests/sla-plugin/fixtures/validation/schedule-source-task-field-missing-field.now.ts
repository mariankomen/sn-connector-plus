import { Sla } from '@servicenow/sdk/core'

export const TestTaskFieldScheduleMissingFieldSLA = Sla({
    $id: Now.ID['test-task-field-missing'],
    name: 'Test Task Field Schedule Missing Field',
    table: 'incident',
    // duration cannot be specified when scheduleSource is 'task_field'
    scheduleSource: 'task_field',
    // Missing scheduleSourceField - should fail validation
    active: true,
})
