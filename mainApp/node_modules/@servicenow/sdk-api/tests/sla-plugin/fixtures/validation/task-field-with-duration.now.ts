import { Sla } from '@servicenow/sdk/core'

export const TestTaskFieldWithDurationSLA = Sla({
    $id: Now.ID['test-task-field-duration'],
    name: 'Test Task Field with Duration',
    table: 'incident',
    duration: Duration({ hours: 4 }),
    scheduleSource: 'task_field',
    scheduleSourceField: 'cmdb_ci.schedule',
    active: true,
})
