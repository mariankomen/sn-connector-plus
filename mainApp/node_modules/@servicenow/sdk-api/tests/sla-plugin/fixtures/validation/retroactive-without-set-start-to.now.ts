import { Sla } from '@servicenow/sdk/core'

export const TestRetroactiveWithoutSetStartToSLA = Sla({
    $id: Now.ID['test-retroactive-no-set-start'],
    name: 'Test Retroactive Without Set Start To',
    table: 'incident',
    duration: Duration({ hours: 4 }),
    schedule: 'b1992362eb601100fcfb858ad106fe16',
    retroactive: {
        start: true,
        // Missing setStartTo - should fail validation
    },
    active: true,
})
