import { Sla } from '@servicenow/sdk/core'

export const TestNoScheduleSourceSLA = Sla({
    $id: Now.ID['test-no-schedule-source'],
    name: 'Test No Schedule Source',
    table: 'incident',
    duration: Duration({ hours: 4 }),
    scheduleSource: 'no_schedule',
    // Schedule will be forced to default value
    active: true,
})
