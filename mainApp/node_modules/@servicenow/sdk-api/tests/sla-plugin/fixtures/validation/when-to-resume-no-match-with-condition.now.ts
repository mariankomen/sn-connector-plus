import { Sla } from '@servicenow/sdk/core'

export const TestNoMatchResumeWithConditionSLA = Sla({
    $id: Now.ID['test-no-match-resume-condition'],
    name: 'Test No Match Resume with Condition',
    table: 'incident',
    duration: Duration({ hours: 4 }),
    schedule: 'b1992362eb601100fcfb858ad106fe16',
    whenTo: {
        resume: 'no_match',
    },
    conditions: {
        start: 'priority=1',
        resume: 'state=2', // Should warn - restricted when whenTo.resume is no_match
    },
    active: true,
})
