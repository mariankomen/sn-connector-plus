import { Sla } from '@servicenow/sdk/core'

Sla({
    $id: Now.ID['test-sla-no-duration'],
    name: 'Test SLA Without Duration',
    table: 'incident',
    schedule: '8-5 weekdays excluding holidays',
})
