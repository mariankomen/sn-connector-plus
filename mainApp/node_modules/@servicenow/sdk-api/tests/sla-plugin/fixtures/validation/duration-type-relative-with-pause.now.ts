import { Sla } from '@servicenow/sdk/core'

export const TestRelativeDurationWithPauseSLA = Sla({
    $id: Now.ID['test-relative-duration-pause'],
    name: 'Test Relative Duration with Pause Condition',
    table: 'incident',
    duration: Duration({ hours: 4 }),
    durationType: 'some-relative-duration-sys-id',
    schedule: 'b1992362eb601100fcfb858ad106fe16',
    active: true,
    conditions: {
        start: 'priority=1',
        pause: 'state=3', // Should hint - restricted with relative duration
    },
})
