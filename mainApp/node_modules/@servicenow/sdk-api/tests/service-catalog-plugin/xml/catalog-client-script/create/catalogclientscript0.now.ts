import { CatalogClientScript } from '@servicenow/sdk/core'

CatalogClientScript({
    $id: Now.ID['sc_ccs_id'],
    name: 'catalog client script test2',
    type: 'onChange',
    script: `function onChange() {
    g_form.addInfoMessage('cool')
}`,
    catalogItem: 'test-catalog-item',
    variableName: 'variable-name',
    uiType: 'desktop',
    vaSupported: true,
    publishedRef: '',
})
