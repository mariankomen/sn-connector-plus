import { CatalogClientScript } from '@servicenow/sdk/core'
import { onSubmitScript } from './script.now'

CatalogClientScript({
    $id: Now.ID['sc_ccs_id'],
    name: 'catalog client script test5',
    type: 'onSubmit',
    script: onSubmitScript,
    variableSet: 'variable-set-name',
    appliesTo: 'set',
    uiType: 'all',
    active: true,
    appliesOnCatalogItemView: true,
    appliesOnRequestedItems: false,
    appliesOnCatalogTasks: false,
    appliesOnTargetRecord: false,
})
