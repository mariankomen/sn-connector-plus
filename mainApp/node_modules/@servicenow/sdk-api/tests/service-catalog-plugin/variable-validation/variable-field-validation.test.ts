import { Project } from '../../../src'
import { runTestOnFunction, validateDiagnostic } from '../../test-utils'
import { ts, path } from '@servicenow/sdk-build-core'

describe('Variable Field Diagnostic Validation', () => {
    describe('mapToField / field consistency', () => {
        test('emits diagnostic when mapToField is true but field is missing', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['map_to_field_no_field'],
                    name: 'MapToField No Field',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            mapToField: true,
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.TrueKeyword,
                "'field' is required when 'mapToField' is true. Specify the field to map to."
            )
        })

        test('no diagnostic when field is specified but mapToField is not true', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['field_no_map'],
                    name: 'Field No Map',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            field: 'some_field',
                        }),
                    },
                })
            })
            expect(output.filter((d) => d.message.includes('mapToField'))).toHaveLength(0)
        })

        test('no diagnostic when mapToField is true and field is specified', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['valid_map_to_field'],
                    name: 'Valid MapToField',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            mapToField: true,
                            field: 'some_field',
                        }),
                    },
                })
            })
            expect(output.filter((d) => d.message.includes('mapToField'))).toHaveLength(0)
        })
    })

    describe('mandatory / readOnly / hidden consistency', () => {
        test('emits diagnostic when mandatory and readOnly are both true', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['mandatory_readonly'],
                    name: 'Mandatory ReadOnly',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            mandatory: true,
                            readOnly: true,
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.TrueKeyword,
                "'mandatory' cannot be true when 'readOnly' is true. A field cannot be both required and read-only."
            )
        })

        test('emits diagnostic when mandatory and hidden are both true', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['mandatory_hidden'],
                    name: 'Mandatory Hidden',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            mandatory: true,
                            hidden: true,
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.TrueKeyword,
                "'mandatory' cannot be true when 'hidden' is true. A field cannot be both required and hidden."
            )
        })

        test('no diagnostic when mandatory is true and readOnly/hidden are false', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['valid_mandatory'],
                    name: 'Valid Mandatory',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: SingleLineTextVariable({
                            question: 'Test',
                            mandatory: true,
                            readOnly: false,
                            hidden: false,
                        }),
                    },
                })
            })
            expect(output.filter((d) => d.message.includes('mandatory'))).toHaveLength(0)
        })
    })

    describe('Lookup source exclusivity', () => {
        test("emits diagnostic when lookupSource is 'choices' and lookupFromTable is specified", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['lookup_both'],
                    name: 'Lookup Both',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: LookupSelectBoxVariable({
                            question: 'Test',
                            lookupFromTable: 'sys_user',
                            lookupValueField: 'sys_id',
                            lookupSource: 'choices',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "'lookupFromTable' should not be specified when 'lookupSource' is 'choices'."
            )
        })

        test("emits diagnostic when lookupSource is 'choices' and lookupValueField is specified", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['lookup_choices_with_value_field'],
                    name: 'Lookup Choices With Value Field',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: LookupSelectBoxVariable({
                            question: 'Test',
                            lookupSource: 'choices',
                            lookupValueField: 'sys_id',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "'lookupValueField' should not be specified when 'lookupSource' is 'choices'."
            )
        })

        test("emits diagnostic when lookupSource is not 'choices' and lookupFromTable is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['lookup_table_missing_lookup_from_table'],
                    name: 'Lookup Table Missing lookupFromTable',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: LookupSelectBoxVariable({
                            question: 'Test',
                            lookupValueField: 'sys_id',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.ObjectLiteralExpression,
                "'lookupFromTable' is required when 'lookupSource' is not 'choices'."
            )
        })

        test("emits diagnostic when lookupSource is not 'choices' and lookupValueField is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['lookup_table_missing_lookup_value_field'],
                    name: 'Lookup Table Missing lookupValueField',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: LookupSelectBoxVariable({
                            question: 'Test',
                            lookupFromTable: 'sys_user',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.ObjectLiteralExpression,
                "'lookupValueField' is required when 'lookupSource' is not 'choices'."
            )
        })

        test('no diagnostic when lookupFromTable and lookupValueField are set without lookupSource', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['valid_lookup_table'],
                    name: 'Valid Lookup Table',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: LookupSelectBoxVariable({
                            question: 'Test',
                            lookupFromTable: 'sys_user',
                            lookupValueField: 'sys_id',
                        }),
                    },
                })
            })
            expect(output.filter((d) => d.message.includes('lookupFromTable'))).toHaveLength(0)
        })
    })

    describe('Reference qualifier exclusivity', () => {
        test("emits diagnostic when useReferenceQualifier is 'simple' but referenceQualCondition is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['ref_simple_missing_condition'],
                    name: 'Ref Simple Missing Condition',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: ReferenceVariable({
                            question: 'Test',
                            referenceTable: 'sys_user',
                            useReferenceQualifier: 'simple',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "ReferenceVariable: 'referenceQualCondition' is required when 'useReferenceQualifier' is 'simple'."
            )
        })

        test("emits diagnostic when useReferenceQualifier is 'dynamic' but dynamicRefQual is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['ref_dynamic_missing_qual'],
                    name: 'Ref Dynamic Missing Qual',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: ReferenceVariable({
                            question: 'Test',
                            referenceTable: 'sys_user',
                            useReferenceQualifier: 'dynamic',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "ReferenceVariable: 'dynamicRefQual' is required when 'useReferenceQualifier' is 'dynamic'."
            )
        })

        test("emits diagnostic when useReferenceQualifier is 'advanced' but referenceQual is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['ref_advanced_missing_qual'],
                    name: 'Ref Advanced Missing Qual',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: ReferenceVariable({
                            question: 'Test',
                            referenceTable: 'sys_user',
                            useReferenceQualifier: 'advanced',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "ReferenceVariable: 'referenceQual' is required when 'useReferenceQualifier' is 'advanced'."
            )
        })

        test('no diagnostic when referenceQualCondition used without useReferenceQualifier', async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['valid_simple_ref'],
                    name: 'Valid Simple Ref',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: ReferenceVariable({
                            question: 'Test',
                            referenceTable: 'sys_user',
                            referenceQualCondition: 'active=true',
                        }),
                    },
                })
            })
            expect(output.filter((d) => d.message.includes('reference qualifier'))).toHaveLength(0)
        })
    })

    describe('RequestedFor reference qualifier exclusivity', () => {
        test("emits diagnostic when useReferenceQualifier is 'simple' but referenceQualCondition is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['reqfor_simple_missing_condition'],
                    name: 'ReqFor Simple Missing Condition',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: RequestedForVariable({
                            question: 'Test',
                            useReferenceQualifier: 'simple',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "RequestedForVariable: 'referenceQualCondition' is required when 'useReferenceQualifier' is 'simple'."
            )
        })

        test("emits diagnostic when useReferenceQualifier is 'dynamic' but dynamicRefQual is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['reqfor_dynamic_missing_qual'],
                    name: 'ReqFor Dynamic Missing Qual',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: RequestedForVariable({
                            question: 'Test',
                            useReferenceQualifier: 'dynamic',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "RequestedForVariable: 'dynamicRefQual' is required when 'useReferenceQualifier' is 'dynamic'."
            )
        })

        test("emits diagnostic when useReferenceQualifier is 'advanced' but referenceQual is missing", async () => {
            const project = new Project({ rootDir: path['resolve'](__dirname, 'project') })
            const output = await runTestOnFunction(project, () => {
                //@ts-expect-error avoid imports
                CatalogItem({
                    $id: Now.ID['reqfor_advanced_missing_qual'],
                    name: 'ReqFor Advanced Missing Qual',
                    shortDescription: 'Test',
                    variables: {
                        //@ts-expect-error avoid imports
                        testVar: RequestedForVariable({
                            question: 'Test',
                            useReferenceQualifier: 'advanced',
                        }),
                    },
                })
            })
            validateDiagnostic(
                output,
                ts.SyntaxKind.StringLiteral,
                "RequestedForVariable: 'referenceQual' is required when 'useReferenceQualifier' is 'advanced'."
            )
        })
    })
})
