import { BusinessRule } from '@servicenow/sdk/core'
//valite .ts imports allowImportingTsExtensions
import { rule } from '../server/script.ts'

BusinessRule({
    $id: Now.ID[1],
    name: 'Test2',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: rule,
})
