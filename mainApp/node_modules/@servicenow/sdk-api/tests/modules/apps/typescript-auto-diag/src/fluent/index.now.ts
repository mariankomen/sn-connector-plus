import { BusinessRule } from '../../../../../../../sdk/src/core'
import { rule } from '../server/script.js'
import { getFields } from '../server/arrayscript.js'

BusinessRule({
    $id: Now.ID[0],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: rule,
})

BusinessRule({
    $id: Now.ID[1],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: getFields,
})