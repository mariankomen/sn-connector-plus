import { BusinessRule } from '@servicenow/sdk/core'
import { rule } from '../server/script'
import { unmapped } from '../server/unmapped'

BusinessRule({
    $id: Now.ID[0],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: rule,
})

BusinessRule({
    $id: Now.ID[1],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: unmapped,
})
