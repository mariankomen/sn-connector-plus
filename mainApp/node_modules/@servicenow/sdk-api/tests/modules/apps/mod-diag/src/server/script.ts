import { toUpper } from 'lodash'

export function rule(current: any, previous: any) {
    callAnother(0)
    return `${toUpper(current)} ${toUpper(previous)}`
}

function callAnother(arg: string) {
    return arg
}

