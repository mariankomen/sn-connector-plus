import { BusinessRule } from '@servicenow/sdk/core'
import { rule } from '../server/script'

BusinessRule({
    $id: Now.ID[1],
    name: 'Test',
    table: 'incident_task',
    when: 'after',
    action: ['update'],
    script: rule,
})
