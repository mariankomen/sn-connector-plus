import { GlideRecord, gs } from '@servicenow/glide'
import { MyAppTables } from './constants'
//Adding a comment to ensure its different
export function rule(current, previous) {
    gs.info(current + previous)
    const foo = {
        a: true,
        b: 'hello',
    }
    gs.info(`foo: ${foo.a} ${foo.b}`)
    const record = new GlideRecord(MyAppTables.x_foo_1)
    record.query()
}
