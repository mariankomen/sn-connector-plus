import { BusinessRule } from '../../../../../../../sdk/src/core'
import { rule } from '../server/script'

BusinessRule({
    $id: Now.ID[0],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: rule,
})
