import { BusinessRule } from '@servicenow/sdk/core'
import { rule } from '../server/script'

BusinessRule({
    $id: Now.ID[0],
    name: 'Test',
    table: 'incident',
    when: 'after',
    action: ['insert'],
    script: rule,
})
