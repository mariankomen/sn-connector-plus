import { BooleanColumn, IntegerColumn, StringColumn, Table } from '@servicenow/sdk/core'

export const x_snc_testapp_demo = Table({
    name: 'x_snc_testapp_demo',
    schema: {
        description: StringColumn({
            maxLength: 255,
            required: true,
        }),
        active: BooleanColumn({}),
        category: StringColumn({
            choices: {
                one: { label: 'One' },
                two: { label: 'Two' },
                '100': { label: '100' },
            },
            dropdown: 'none',
        }),
        price: IntegerColumn({}),
    },
})
