import { ClientScript } from '@servicenow/sdk/core'
import { onCellEditScript } from './script.now'

//OnCellEdit type, Fail because view is set and global is true which should error
ClientScript({
    $id: Now.ID['script-id'],
    name: 'my_scripts',
    table: 'x_cs_table',
    script: onCellEditScript,
    active: true,
    applies_extended: false,
    description: 'cool',
    messages: '',
    isolate_script: false,
    ui_type: 'all',
    global: true,
    view: 'app_task_view',
    type: 'onCellEdit',
    field: 'state',
})
