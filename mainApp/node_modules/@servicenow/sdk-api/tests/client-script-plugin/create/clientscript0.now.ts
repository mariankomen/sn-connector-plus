import { ClientScript } from '@servicenow/sdk/core'
import { onChangeScript } from './script.now'

//OnChange type, ui_type empty to default to desktop
ClientScript({
    $id: Now.ID['script-id'],
    name: 'my_scripts',
    table: 'x_cs_table',
    script: onChangeScript,
    applies_extended: false,
    description: 'cool',
    messages: '',
    global: false,
    active: true,
    isolate_script: false,
    view: 'app_task_view',
    type: 'onChange',
    field: 'state',
})
