import { ClientScript } from '@servicenow/sdk/core'
import { onLoadScript } from './script.now'

ClientScript({
    $id: Now.ID['script-id'],
    name: 'my_scripts',
    table: 'x_cs_table',
    active: true,
    applies_extended: false,
    global: true,
    ui_type: 'all',
    description: 'cool',
    messages: '',
    isolate_script: false,
    type: 'onLoad',
    script: onLoadScript,
})
