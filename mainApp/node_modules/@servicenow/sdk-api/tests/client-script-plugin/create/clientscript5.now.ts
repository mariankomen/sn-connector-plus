import { ClientScript } from '@servicenow/sdk/core'

//OnLoad type, using script tag to define script, active left empty
ClientScript({
    $id: Now.ID['script-id'],
    name: 'my_scripts',
    table: 'x_cs_table',
    script: `function onLoad(){ g_form.addInfoMessage("hello") }`,
    applies_extended: false,
    description: 'cool',
    global: false,
    messages: '',
    isolate_script: false,
    ui_type: 'desktop',
    view: 'app_task_view',
    type: 'onLoad',
})
