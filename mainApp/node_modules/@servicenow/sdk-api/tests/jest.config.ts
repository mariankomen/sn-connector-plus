import baseConfig from '../../../jest.config.base'
import type { Config } from '@jest/types'

// Disable dependency resolution for all tests (performance optimization)
process.env['FLUENT_RESOLVE_DEPENDENCIES'] = 'false'

const config: Config.InitialOptions = {
    ...baseConfig,
    //override any build package specific configuration below
    testTimeout: 60000,
    testPathIgnorePatterns: ['/node_modules/', '/modules/apps/'],
    roots: ['<rootDir>'],
    moduleNameMapper: {
        '^virtual:templates$': '<rootDir>/../dist/project-factory/templates.js',
    },
}

export default config
