import { ReferenceColumn, StringColumn, Table } from '@servicenow/sdk/core'

export const sn_table_app_incoming = Table({
    name: 'sn_table_app_incoming',
    label: 'Incoming Table',
    schema: {
        string_column: StringColumn({}),
        reference_column: ReferenceColumn({
            audit: true,
            readOnly: false,
            referenceTable: 'task',
        }),
    },
    //biome-ignore format: intentionally bad formatting
    index: [{ "name": 'index', "element": "string_column", "unique": false },
     { "name": 'index2', "element": "reference_column", "unique": false }],
})
