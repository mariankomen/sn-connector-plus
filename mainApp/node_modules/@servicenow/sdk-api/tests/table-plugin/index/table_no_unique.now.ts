import { Table, StringColumn, ReferenceColumn } from '@servicenow/sdk/core'

export const sn_table_app_incoming_no_unique = Table({
    index: [
        {
            name: 'index',
            unique: false,
            element: ['string_column', 'reference_column'],
        },
    ],
    label: 'Incoming Table',
    name: 'sn_table_app_incoming_no_unique',
    schema: {
        string_column: StringColumn({}),
        reference_column: ReferenceColumn({
            referenceTable: 'task',
        }),
    },
})
