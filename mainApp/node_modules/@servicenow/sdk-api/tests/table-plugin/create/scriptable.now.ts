import { Table, StringColumn } from '@servicenow/sdk/core'

export const sn_table_app_scriptable = Table({
    label: 'Create Table',
    name: 'sn_table_app_scriptable',
    schema: {
        string_column: StringColumn({}),
    },
    scriptable_table: true,
})
