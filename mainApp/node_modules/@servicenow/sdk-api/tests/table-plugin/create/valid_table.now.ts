import { StringColumn, Table } from '@servicenow/sdk/core'

export const table = Table({
    name: 'table',
    schema: {
        sn_table_app_column: StringColumn({}),
    },
})

export const sn_table_app_table = Table({
    name: 'sn_table_app_table',
    schema: {
        column: StringColumn({}),
    },
})
