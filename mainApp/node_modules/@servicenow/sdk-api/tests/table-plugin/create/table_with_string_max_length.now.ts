import { Table, StringColumn } from '@servicenow/sdk/core'

export const x_test_string_max_length = Table({
    label: 'Table with String Max Length',
    name: 'x_test_string_max_length',
    schema: {
        string_field: StringColumn({
            label: 'String Field',
            maxLength: 'large',
        }),
        normal_field: StringColumn({
            label: 'Normal Field',
            maxLength: 255,
        }),
    },
})
