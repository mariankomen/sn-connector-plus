import { StringColumn, BooleanColumn } from '@servicenow/sdk/core'
import { FlowArray, FlowObject, ActionDefinition } from '@servicenow/sdk/automation'

export const unit_test = ActionDefinition({
    $id: Now.ID['7f6d4a28ffbcf6103c50ffffffffff69'],
    name: 'Unit Test',
    inputs: {
        flow_array: FlowObject({
            fields: {
                child_two: BooleanColumn({
                    label: 'FA Child Two',
                }),
                child_one: StringColumn({
                    label: 'FA Child One',
                }),
            },
            label: 'Flow Array Child',
        }),
        flow_object: FlowArray({
            elementType: StringColumn({
                label: 'FO Child',
            }),
            label: 'Flow Object',
            childName: 'fo_child',
        }),
    },
    outputs: {},
})
