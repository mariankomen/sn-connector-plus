import { BooleanColumn, StringColumn } from '@servicenow/sdk/core'
import { ActionDefinition } from '@servicenow/sdk/automation'

export const fl_action_1 = ActionDefinition({
    $id: Now.ID['action1'],
    name: 'fl-action-1',
    annotation: 'the annotation!',
    description: 'actions description',
    category: '',
    access: 'package_private',
    protection: 'read',
    inputs: {
        input1: StringColumn({
            label: 'inputOne',
            mandatory: true,
            default: 'John Doe',
            hint: 'This is the string hint',
        }),
        input2: BooleanColumn({
            label: 'inputTwo',
            mandatory: true,
        }),
    },
    outputs: {
        someOutput: BooleanColumn({
            label: 'someOutput',
            mandatory: true,
            default: true,
            hint: 'This is a boolean hint',
        }),
    },
})
