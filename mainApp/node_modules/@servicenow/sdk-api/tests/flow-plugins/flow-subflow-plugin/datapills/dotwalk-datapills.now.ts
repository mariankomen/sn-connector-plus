/**
 * Test file for ReferenceColumn dotwalk datapills
 * Tests Fluent -> XML transformation for:
 * 1. flowVariables with ReferenceColumn (multi-level dotwalk)
 * 2. Subflow outputs with ReferenceColumn (multi-level dotwalk)
 * 3. trigger.current dotwalk (table-aware)
 * 4. Mix of ReferenceColumn with FlowObject/FlowArray
 */
import '@servicenow/sdk/global'
import { BooleanColumn, ReferenceColumn, StringColumn } from '@servicenow/sdk/core'
import { Flow, FlowArray, FlowObject, Subflow, trigger, wfa } from '@servicenow/sdk/automation'
import { ActionDefinition } from '@servicenow/sdk/automation'

// =============================================================================
// Action definition for testing datapills
// =============================================================================
export const logAction = ActionDefinition({
    $id: Now.ID['dotwalk-test-action'],
    name: 'Log Action for Dotwalk Test',
    inputs: {
        message: StringColumn({ label: 'Message', mandatory: true }),
    },
    outputs: {
        logged: BooleanColumn({ label: 'Logged', mandatory: true }),
    },
})

// =============================================================================
// Subflow with ReferenceColumn outputs for dotwalk testing
// =============================================================================
export const refOutputSubflow = Subflow(
    {
        $id: Now.ID['dotwalk-ref-output-subflow'],
        name: 'Reference Output Subflow',
        description: 'Subflow with ReferenceColumn outputs for dotwalk testing',
        runAs: 'system',
        inputs: {
            inputId: StringColumn({ label: 'Input ID', mandatory: true }),
        },
        outputs: {
            // ReferenceColumn output - should support dotwalk
            userRef: ReferenceColumn({
                label: 'User Reference',
                referenceTable: 'sys_user',
                mandatory: true,
            }),
            // FlowObject output - should still work correctly
            metadata: FlowObject({
                label: 'Metadata',
                fields: {
                    createdAt: StringColumn({ label: 'Created At' }),
                    status: StringColumn({ label: 'Status' }),
                },
            }),
        },
    },
    (_params) => {
        // Subflow body
    }
)

// =============================================================================
// Main Flow with ReferenceColumn flowVariables and dotwalk datapills
// =============================================================================
Flow(
    {
        $id: Now.ID['dotwalk-test-flow'],
        name: 'Dotwalk Test Flow',
        description: 'Tests ReferenceColumn dotwalk in flowVariables, subflow outputs, and trigger.current',
        runAs: 'system',
        flowVariables: {
            // ReferenceColumn flowVariable - should support dotwalk
            assignedUser: ReferenceColumn({
                label: 'Assigned User',
                referenceTable: 'sys_user',
            }),
            // Another ReferenceColumn for multi-level dotwalk testing
            incidentRef: ReferenceColumn({
                label: 'Incident Reference',
                referenceTable: 'incident',
            }),
            // FlowObject to verify non-regression
            config: FlowObject({
                label: 'Config',
                fields: {
                    name: StringColumn({ label: 'Name' }),
                    enabled: BooleanColumn({ label: 'Enabled' }),
                },
            }),
            // FlowArray to verify non-regression
            items: FlowArray({
                label: 'Items',
                elementType: FlowObject({
                    label: 'Item',
                    fields: {
                        itemId: StringColumn({ label: 'Item ID' }),
                    },
                }),
                childName: 'items',
            }),
        },
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['dotwalk-test-trigger'] },
        {
            table: 'incident',
            condition: '',
            run_on_extended: 'false',
            run_flow_in: 'background',
            run_when_user_list: [],
            run_when_setting: 'both',
            run_when_user_setting: 'any',
        }
    ),
    (params) => {
        // =================================================================
        // Test 1: flowVariable ReferenceColumn 2-level dotwalk
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-1'],
                uuid: 'a1111111-1111-1111-1111-111111111111',
            },
            {
                // 2-level dotwalk: assignedUser.manager.email
                message: `Manager email: ${wfa.dataPill(params.flowVariables.assignedUser.manager.email, 'string')}`,
            }
        )

        // =================================================================
        // Test 2: flowVariable ReferenceColumn 4-level dotwalk
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-2'],
                uuid: 'a2222222-2222-2222-2222-222222222222',
            },
            {
                // 4-level dotwalk: assignedUser.manager.manager.manager.name
                message: `Skip-level manager: ${wfa.dataPill(params.flowVariables.assignedUser.manager.manager.manager.name, 'string')}`,
            }
        )

        // =================================================================
        // Test 3: flowVariable incident ref -> assigned_to dotwalk
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-3'],
                uuid: 'a3333333-3333-3333-3333-333333333333',
            },
            {
                // incidentRef.assigned_to.manager.email
                message: `Incident assignee manager: ${wfa.dataPill(params.flowVariables.incidentRef.assigned_to.manager.email, 'string')}`,
            }
        )

        // =================================================================
        // Test 4: FlowObject flowVariable (non-regression)
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-4'],
                uuid: 'a4444444-4444-4444-4444-444444444444',
            },
            {
                // FlowObject field access should still work
                message: `Config name: ${wfa.dataPill(params.flowVariables.config.name, 'string')}`,
            }
        )

        // =================================================================
        // Test 5: trigger.current dotwalk (table-aware incident)
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-5'],
                uuid: 'a5555555-5555-5555-5555-555555555555',
            },
            {
                // trigger.current.assigned_to.manager.email (incident table)
                message: `Trigger assignee manager: ${wfa.dataPill(params.trigger.current.assigned_to.manager.email, 'string')}`,
            }
        )

        // =================================================================
        // Test 6: trigger.current 3-level dotwalk
        // =================================================================
        wfa.action(
            logAction,
            {
                $id: Now.ID['dotwalk-action-6'],
                uuid: 'a6666666-6666-6666-6666-666666666666',
            },
            {
                // 3-level: trigger.current.caller_id.manager.manager.name
                message: `Caller skip-manager: ${wfa.dataPill(params.trigger.current.caller_id.manager.manager.name, 'string')}`,
            }
        )

        // =================================================================
        // Test 7: Subflow with ReferenceColumn output - COMMENTED: subflow support removed
        // =================================================================
        // const subResult = wfa.subflow(
        //     refOutputSubflow,
        //     {
        //         $id: Now.ID['dotwalk-subflow-1'],
        //         uuid: 'b1111111-1111-1111-1111-111111111111',
        //     },
        //     {
        //         inputId: 'test-input',
        //     }
        // )

        // =================================================================
        // Test 8: Subflow output ReferenceColumn dotwalk - COMMENTED: subflow support removed
        // =================================================================
        // wfa.action(
        //     logAction,
        //     {
        //         $id: Now.ID['dotwalk-action-7'],
        //         uuid: 'a7777777-7777-7777-7777-777777777777',
        //     },
        //     {
        //         // subResult.userRef.manager.email
        //         message: `Subflow user manager: ${wfa.dataPill(subResult.userRef.manager.email, 'string')}`,
        //     }
        // )

        // =================================================================
        // Test 9: Subflow output ReferenceColumn 3-level dotwalk - COMMENTED: subflow support removed
        // =================================================================
        // wfa.action(
        //     logAction,
        //     {
        //         $id: Now.ID['dotwalk-action-8'],
        //         uuid: 'a8888888-8888-8888-8888-888888888888',
        //     },
        //     {
        //         // subResult.userRef.manager.manager.manager.name
        //         message: `Subflow skip-manager: ${wfa.dataPill(subResult.userRef.manager.manager.manager.name, 'string')}`,
        //     }
        // )

        // =================================================================
        // Test 10: Subflow FlowObject output (non-regression) - COMMENTED: subflow support removed
        // =================================================================
        // wfa.action(
        //     logAction,
        //     {
        //         $id: Now.ID['dotwalk-action-9'],
        //         uuid: 'a9999999-9999-9999-9999-999999999999',
        //     },
        //     {
        //         // FlowObject field access should still work
        //         message: `Subflow metadata: ${wfa.dataPill(subResult.metadata.status, 'string')}`,
        //     }
        // )

        // =================================================================
        // Test 11: Mixed datapills - flowVar + trigger + subflow output - COMMENTED: subflow support removed
        // =================================================================
        // wfa.action(
        //     logAction,
        //     {
        //         $id: Now.ID['dotwalk-action-10'],
        //         uuid: 'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa',
        //     },
        //     {
        //         message: `Mixed: fv=${wfa.dataPill(params.flowVariables.assignedUser.manager.name, 'string')}, trigger=${wfa.dataPill(params.trigger.current.assigned_to.name, 'string')}, sub=${wfa.dataPill(subResult.userRef.email, 'string')}`,
        //     }
        // )
    }
)
