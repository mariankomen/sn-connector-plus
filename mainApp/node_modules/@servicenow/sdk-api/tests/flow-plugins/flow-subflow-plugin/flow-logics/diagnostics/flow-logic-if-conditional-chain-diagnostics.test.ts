import { Diagnostic, path } from '@servicenow/sdk-build-core'
import { createTestContext } from '../../../../test-utils'

describe('If/ElseIf/Else Sibling Chain Validation Diagnostics', () => {
    const ctx = createTestContext({ rootDir: path.resolve(__dirname, 'project') })
    beforeAll(() => ctx.initialize())
    afterEach(() => ctx.cleanup())
    describe('ElseIf Sibling Validation', () => {
        test('should error when ElseIf is not preceded by If or ElseIf', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'invalid-elseif-sibling.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.forEach({ $id: Now.ID['foreach1'] }, 'some_array', () => {
            console.log('in loop')
        })

        // Invalid: ElseIf without preceding If
        wfa.flowLogic.elseIf(
            { $id: Now.ID['elseif1'], condition: 'some_condition' },
            () => {
                console.log('elseif branch')
            }
        )
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            // Should have error for ElseIf not preceded by If or ElseIf
            const elseIfError = diagnostics.find(
                (d) =>
                    d.message === 'ElseIf clause must follow an If or ElseIf clause, but found wfa.flowLogic.forEach.'
            )

            expect(elseIfError).toBeDefined()
            expect(elseIfError?.level).toBe(Diagnostic.Level.Error)
        })

        test('should error when ElseIf follows forEach instead of If', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'elseif-after-foreach.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.forEach({ $id: Now.ID['foreach1'] }, 'some_array', () => {
            console.log('in loop')
        })

        // Invalid: ElseIf following forEach
        wfa.flowLogic.elseIf(
            { $id: Now.ID['elseif1'], condition: 'some_condition' },
            () => {
                console.log('elseif branch')
            }
        )
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const elseIfError = diagnostics.find(
                (d) =>
                    d.message === 'ElseIf clause must follow an If or ElseIf clause, but found wfa.flowLogic.forEach.'
            )

            expect(elseIfError).toBeDefined()
            expect(elseIfError?.level).toBe(Diagnostic.Level.Error)
        })

        test('should error when ElseIf is the first statement in Flow body', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'elseif-first-statement.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // Invalid: ElseIf as first statement
        wfa.flowLogic.elseIf(
            { $id: Now.ID['elseif1'], condition: 'some_condition' },
            () => {
                console.log('elseif branch')
            }
        )
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const elseIfError = diagnostics.find(
                (d) => d.message === 'ElseIf clause must follow an If or ElseIf clause.'
            )

            expect(elseIfError).toBeDefined()
            expect(elseIfError?.level).toBe(Diagnostic.Level.Error)
        })
    })

    describe('Else Sibling Validation', () => {
        test('should error when Else is not preceded by If or ElseIf', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'invalid-else-sibling.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.forEach({ $id: Now.ID['foreach1'] }, 'some_array', () => {
            console.log('in loop')
        })

        // Invalid: Else without preceding If or ElseIf
        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const elseError = diagnostics.find(
                (d) => d.message === 'Else clause must follow an If or ElseIf clause, but found wfa.flowLogic.forEach.'
            )

            expect(elseError).toBeDefined()
            expect(elseError?.level).toBe(Diagnostic.Level.Error)
        })

        test('should error when Else follows forEach instead of If or ElseIf', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'else-after-foreach.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.forEach({ $id: Now.ID['foreach1'] }, 'some_array', () => {
            console.log('in loop')
        })

        // Invalid: Else following forEach
        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const elseError = diagnostics.find(
                (d) => d.message === 'Else clause must follow an If or ElseIf clause, but found wfa.flowLogic.forEach.'
            )

            expect(elseError).toBeDefined()
            expect(elseError?.level).toBe(Diagnostic.Level.Error)
        })

        test('should error when Else is the first statement in Flow body', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'else-first-statement.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // Invalid: Else as first statement
        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const elseError = diagnostics.find((d) => d.message === 'Else clause must follow an If or ElseIf clause.')

            expect(elseError).toBeDefined()
            expect(elseError?.level).toBe(Diagnostic.Level.Error)
        })
    })

    describe('Valid Conditional Chains', () => {
        test('should NOT error for valid If -> ElseIf -> Else chain', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'valid-chain.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // Valid chain
        wfa.flowLogic.if({ $id: Now.ID['if1'], condition: 'condition1' }, () => {
            console.log('if branch')
        })

        wfa.flowLogic.elseIf({ $id: Now.ID['elseif1'], condition: 'condition2' }, () => {
            console.log('elseif branch')
        })

        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            // Should have no sibling validation errors
            const siblingErrors = diagnostics.filter((d) => d.message.includes('clause must follow'))

            expect(siblingErrors.length).toBe(0)
        })

        test('should NOT error for valid If -> ElseIf -> ElseIf -> Else chain', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'valid-multiple-elseif.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.if({ $id: Now.ID['if1'], condition: 'condition1' }, () => {
            console.log('if branch')
        })

        wfa.flowLogic.elseIf({ $id: Now.ID['elseif1'], condition: 'condition2' }, () => {
            console.log('elseif1 branch')
        })

        wfa.flowLogic.elseIf({ $id: Now.ID['elseif2'], condition: 'condition3' }, () => {
            console.log('elseif2 branch')
        })

        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const siblingErrors = diagnostics.filter((d) => d.message.includes('clause must follow'))

            expect(siblingErrors.length).toBe(0)
        })

        test('should NOT error for standalone If without Else', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'valid-standalone-if.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // Valid: standalone If
        wfa.flowLogic.if({ $id: Now.ID['if1'], condition: 'condition1' }, () => {
            console.log('if branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const siblingErrors = diagnostics.filter((d) => d.message.includes('clause must follow'))

            expect(siblingErrors.length).toBe(0)
        })

        test('should NOT error for valid If -> Else chain (no ElseIf)', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'valid-if-else.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        wfa.flowLogic.if({ $id: Now.ID['if1'], condition: 'condition1' }, () => {
            console.log('if branch')
        })

        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const siblingErrors = diagnostics.filter((d) => d.message.includes('clause must follow'))

            expect(siblingErrors.length).toBe(0)
        })
    })

    describe('Multiple Separate If Blocks', () => {
        test('should NOT error for multiple separate If blocks', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'multiple-if-blocks.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // First If block
        wfa.flowLogic.if({ $id: Now.ID['if1'], condition: 'condition1' }, () => {
            console.log('if1 branch')
        })

        wfa.flowLogic.else(() => {
            console.log('else1 branch')
        })

        // Some other logic
        wfa.flowLogic.forEach({ $id: Now.ID['foreach1'] }, 'some_array', () => {
            console.log('in loop')
        })

        // Second If block (valid - starts a new chain)
        wfa.flowLogic.if({ $id: Now.ID['if2'], condition: 'condition2' }, () => {
            console.log('if2 branch')
        })

        wfa.flowLogic.elseIf({ $id: Now.ID['elseif2'], condition: 'condition3' }, () => {
            console.log('elseif2 branch')
        })

        wfa.flowLogic.else(() => {
            console.log('else2 branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            const siblingErrors = diagnostics.filter((d) => d.message.includes('clause must follow'))

            expect(siblingErrors.length).toBe(0)
        })
    })

    describe('Missing Condition Validation', () => {
        test('should error when If/ElseIf/Else are used without condition', async () => {
            const project = ctx.createProject()
            const file = project.addFile({
                path: 'missing-condition.now.ts',
                content: `
import '@servicenow/sdk/global'
import { Flow, wfa, trigger } from '@servicenow/sdk/automation'

Flow(
    {
        $id: Now.ID['test_flow'],
        name: 'Test Flow',
    },
    wfa.trigger(
        trigger.record.created,
        { $id: Now.ID['test_trigger'] },
        {
            table: 'incident',
            condition: '',
        }
    ),
    () => {
        // Invalid: If without condition field
        wfa.flowLogic.if({ $id: Now.ID['if1'] }, () => {
            console.log('if branch')
        })

        // Invalid: ElseIf without condition field
        wfa.flowLogic.elseIf({ $id: Now.ID['elseif1'] }, () => {
            console.log('elseif branch')
        })

        // Else doesn't require condition (valid)
        wfa.flowLogic.else(() => {
            console.log('else branch')
        })
    }
)
                `,
            })

            const diagnostics = await file.getDiagnostics()

            // Should have errors for If and ElseIf missing condition
            const ifError = diagnostics.find(
                (d) => d.message.includes("requires a 'condition' field") && d.message.includes('if')
            )
            const elseIfError = diagnostics.find(
                (d) => d.message.includes("requires a 'condition' field") && d.message.includes('elseIf')
            )

            expect(ifError).toBeDefined()
            expect(ifError?.level).toBe(Diagnostic.Level.Error)
            expect(elseIfError).toBeDefined()
            expect(elseIfError?.level).toBe(Diagnostic.Level.Error)
        })
    })
})
