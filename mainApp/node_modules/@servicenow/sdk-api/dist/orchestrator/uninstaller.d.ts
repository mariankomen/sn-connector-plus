import type { IConnector } from '../connector';
import type { Logger } from '@servicenow/sdk-build-core';
export declare class Uninstaller {
    private connector;
    private logger;
    constructor(connector: IConnector, logger: Logger);
    process(scope: string, scopeId: string, isStoreApp: boolean): Promise<void>;
    private monitorStatus;
    private monitorProgress;
    private monitorMutexRelease;
}
export declare const TrackerStatus: {
    PENDING: string;
    RUNNING: string;
    SUCCESSFUL: string;
    ERROR: string;
};
