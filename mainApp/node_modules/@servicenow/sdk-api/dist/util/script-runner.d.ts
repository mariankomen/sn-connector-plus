import type { Project } from '../project';
import type { LazyCredential } from '../credentials';
export declare function projectHasScript(project: Project, scriptName: string): boolean;
export declare function runScriptInProject({ scriptName, project, credential, throwIfNotFound, }: {
    scriptName: string;
    project: Project;
    credential?: LazyCredential | undefined;
    throwIfNotFound?: boolean;
}): Promise<void>;
