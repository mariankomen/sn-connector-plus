"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeFileWithParentDirs = writeFileWithParentDirs;
exports.createDirIfNotExists = createDirIfNotExists;
exports.defaultDownloadDir = defaultDownloadDir;
exports.writeFileStream = writeFileStream;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const os = __importStar(require("os"));
function writeFileWithParentDirs(fs, { path, content }) {
    const parentDirectory = sdk_build_core_1.path.dirname(path);
    if (!sdk_build_core_1.FileSystem.existsSync(fs, parentDirectory)) {
        fs.mkdirSync(parentDirectory, { recursive: true });
    }
    fs.writeFileSync(path, content, { encoding: 'utf-8' });
}
function createDirIfNotExists(dirPath, fs) {
    if (!sdk_build_core_1.FileSystem.existsSync(fs, dirPath)) {
        fs.mkdirSync(dirPath, { recursive: true });
    }
}
function defaultDownloadDir() {
    //Generate a unique temporary path for unpacking to avoid collisions
    return sdk_build_core_1.path.join(os.tmpdir(), '.now/metadata', (0, sdk_build_core_1.GUID)());
}
function writeFileStream(fs, filePath, content) {
    const fileStream = sdk_build_core_1.FileSystem.createWriteStream(fs, filePath);
    // `createWriteStream` returns a Node-like Writable,
    // but we need a browser-like WritableStream 🙃
    return content.pipeTo(new WritableStream({
        write: (chunk) => {
            return new Promise((resolve, reject) => {
                fileStream.write(chunk, (error) => {
                    if (error) {
                        reject(error);
                    }
                    else {
                        resolve();
                    }
                });
            });
        },
        close: () => {
            return new Promise((resolve, reject) => {
                fileStream.end((error) => {
                    if (error) {
                        reject(error);
                    }
                    else {
                        resolve();
                    }
                });
            });
        },
        abort: (reason) => {
            return new Promise((_, reject) => {
                fileStream.end((error) => {
                    reject(reason ?? error);
                });
            });
        },
    }));
}
//# sourceMappingURL=file.js.map