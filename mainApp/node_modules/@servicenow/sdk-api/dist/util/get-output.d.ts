import type { File, Database, Context, Record } from '@servicenow/sdk-build-core';
export declare function getOutput(database: Database, context: Omit<Context, 'self'>): Promise<(File & {
    source: Record;
})[]>;
