import { ts, type Plugin, type Transform as ITransform, type Result, Shape, type Database, type Record, type File, type PluginUsageTracker } from '@servicenow/sdk-build-core';
import { Project } from '../project';
export declare class MultiProjectTransform implements ITransform {
    private readonly rootProject;
    private readonly pluginUsageTracker;
    private fallbackTransform?;
    private readonly rootTransform;
    private readonly transformMap;
    constructor(rootProject: Project, pluginUsageTracker: PluginUsageTracker);
    toRecord(source: ts.Node | Shape | File, ...plugins: Plugin[]): Promise<Result<Record>>;
    toShape(node: ts.Node, ...plugins: Plugin[]): Promise<Result<Shape>>;
    recordToShape(record: Record, database: Database, ...plugins: Plugin[]): Promise<Result<Shape>>;
    toSubclass<const S extends Shape>(shape: S, ...plugins: Plugin[]): Promise<Result<S>>;
    getUpdateName(record: Record, ...plugins: Plugin[]): Promise<string>;
    private createTransform;
    private getFallbackTransform;
    private getTransformForPath;
    private findProjectRootForPath;
    private readonly isProjectRootCache;
    private isProjectRoot;
}
