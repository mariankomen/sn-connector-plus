/**
 * Application Dependencies Module
 *
 * Handles fetching, transforming, and generating TypeScript type definitions
 * for ServiceNow dependencies (actions, triggers, subflows, tables, etc.)
 */

export { ApplicationDependencies } from './application-dependencies'
export { cleanTypeDefinitions } from './script-definition-generator'
export { RelationshipResolver } from './relationship-resolver'
