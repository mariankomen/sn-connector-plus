import { type Context, Database } from '../plugins';
export declare function deleteMultipleDiff(existing: Database, incoming: Database, descendants: Database, { factory }: Pick<Context, 'factory'>): Promise<{
    success: boolean;
    value: Database;
}>;
