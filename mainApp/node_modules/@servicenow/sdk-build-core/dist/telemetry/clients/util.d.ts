import type { TelemetryEvent, TimerMetric } from '../types';
export declare class AppSeeTimerMetric implements TimerMetric {
    private readonly name;
    private readonly startTime;
    constructor(name: TelemetryEvent['name']);
    getDurationMs(): {
        duration: number;
        now: number;
    };
    end(eventData?: Pick<TelemetryEvent, 'appInfo' | 'data'>): TelemetryEvent;
}
