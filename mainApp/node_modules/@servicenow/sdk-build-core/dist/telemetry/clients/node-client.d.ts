import type { TelemetryEvent, InstanceSettings } from '../types';
import { AbstractAppSeeClient } from './abstract-client';
export declare class NodeTelemetryClient extends AbstractAppSeeClient {
    constructor(config: InstanceSettings, telemetryAttributes?: {
        sdkVersion?: string;
        clientName?: 'cli' | string;
        hostname?: string;
    });
    private userId?;
    doStart(): Promise<void>;
    doSendEvent(events: TelemetryEvent | TelemetryEvent[]): void;
    private getHeaders;
    private getDefaultDataValues;
    private applyCITelemetry;
}
