"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateClaimsFile = validateClaimsFile;
const json_1 = require("./json");
const jsonschema_1 = require("jsonschema");
const claimSchema = {
    id: '/.claims',
    type: 'object',
    patternProperties: {
        '.*': { type: 'array' },
    },
};
function validateClaimsFile(json) {
    const parsedJson = json_1.JSON5.parse(json);
    const validator = new jsonschema_1.Validator();
    const result = validator.validate(parsedJson, claimSchema);
    if (result.errors && result.errors.length > 0) {
        throw new Error(`Error reading .claims.json, ${result.errors[0]?.stack}`);
    }
}
//# sourceMappingURL=claims.js.map