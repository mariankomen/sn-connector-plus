import fs from "fs";
import path from "path";
import build from "./now.prebuild.mjs";

build({
  rootDir: path.resolve("."),
  config: JSON.parse(fs.readFileSync("now.config.json", "utf-8")),
  fs,
  path,
  logger: console,
  registerExplicitId: (table, key) => `#sys_id#${table}#${key}#`,
});
