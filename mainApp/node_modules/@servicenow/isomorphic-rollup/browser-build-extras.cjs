const fs = require("fs");
const path = require("path");

const rollupWasm =
  require.resolve("@rollup/browser/dist/bindings_wasm_bg.wasm");
fs.copyFileSync(
  rollupWasm,
  path.join(__dirname, "dist/browser/bindings_wasm_bg.wasm"),
);
