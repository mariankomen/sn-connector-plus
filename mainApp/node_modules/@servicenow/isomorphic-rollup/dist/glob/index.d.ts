import { GlobOptions } from "glob";
import type { FileSystem } from "@servicenow/sdk-build-core";
interface GlobOptionsWithFluentFs extends Omit<GlobOptions, "fs"> {
    fs: FileSystem;
}
export declare function glob(pattern: string | string[], options: GlobOptionsWithFluentFs): Promise<string[] | import("glob").Path[]>;
export {};
