import { InputPluginOption } from "rollup";
interface CssUrlOptions {
    include?: string | string[];
    exclude?: string | string[];
    shouldExtract?: (filePath: string, fileContent: Uint8Array, mimeType: string) => boolean;
    getExtractNameAndUrl?: (url: string, filePath: string, mimeType: string) => {
        name: string;
        url: string;
    };
    warnSize?: number;
}
export declare function cssUrl(options?: CssUrlOptions): InputPluginOption;
export {};
