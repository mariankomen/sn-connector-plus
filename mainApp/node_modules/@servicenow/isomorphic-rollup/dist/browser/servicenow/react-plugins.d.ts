import type { InputPluginOption } from "rollup";
import type { RegisterExplicitIdFunction, Credential } from "./types";
interface ReactPluginsOptions {
    scope: string;
    rootDir: string;
    registerExplicitId?: RegisterExplicitIdFunction;
    dev?: boolean;
    enableCacheBusting?: boolean;
    watchPaths: string[];
    proxyPaths: string[];
    credential?: Credential;
    omitDevUxGlobals?: boolean;
}
/**
 * Creates an updateSrcPath function for transforming script source paths
 * @param config Configuration options
 * @returns Function that transforms original path and emitted filename to final source path
 */
export declare const createUpdateSrcPath: (config: {
    scope: string;
    preserveSrc: boolean;
    enableCacheBusting: boolean;
}) => (originalPath: string, emittedFileName: string) => string;
declare const _default: ({ scope, rootDir, registerExplicitId, dev, enableCacheBusting, watchPaths, proxyPaths, credential, omitDevUxGlobals, }: ReactPluginsOptions) => Promise<InputPluginOption[]>;
export default _default;
