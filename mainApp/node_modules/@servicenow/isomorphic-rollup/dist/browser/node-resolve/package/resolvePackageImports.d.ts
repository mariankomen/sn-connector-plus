import { IsomorphicRollupFs } from "../../fs-iface";
interface ParamObject {
    importSpecifier: string;
    importer: string;
    moduleDirs: readonly string[];
    conditions: readonly string[];
    resolveId: (id: string) => any;
    fs: IsomorphicRollupFs;
    validateImportsExports: boolean;
}
declare function resolvePackageImports({ importSpecifier, importer, moduleDirs, conditions, resolveId, fs, validateImportsExports, }: ParamObject): Promise<string | URL | null | undefined>;
export default resolvePackageImports;
