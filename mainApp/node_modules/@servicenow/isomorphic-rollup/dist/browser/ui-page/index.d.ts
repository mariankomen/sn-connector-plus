import type { Plugin } from "rollup";
import { DomSerializerOptions } from "dom-serializer";
import type { IsomorphicRollupFs } from "../fs-iface";
interface HtmlScriptEntrypointsOptions {
    fs?: IsomorphicRollupFs;
    rootDir: string;
    updateSrcPath?: (originalSrc: string, emittedFileName: string) => string;
    entryPath?: (entry: string, { fullPath, dir }: {
        fullPath: string;
        dir: string;
    }) => {
        [key: string]: string;
    };
    serializerOptions?: DomSerializerOptions;
}
export default function htmlScriptEntrypoints(options: HtmlScriptEntrypointsOptions): Plugin;
export {};
