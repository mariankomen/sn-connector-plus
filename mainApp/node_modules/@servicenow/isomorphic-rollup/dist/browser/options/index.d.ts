import type { InputOptions, NullValue, OutputOptions } from "rollup";
export declare const inputOptions: (updateOptions: (options: InputOptions) => InputOptions | NullValue) => {
    name: string;
    options(rawOptions: InputOptions): NullValue | InputOptions;
};
export declare const outputOptions: (updateOptions: (options: OutputOptions) => OutputOptions | NullValue) => {
    name: string;
    outputOptions(rawOptions: OutputOptions): NullValue | OutputOptions;
};
