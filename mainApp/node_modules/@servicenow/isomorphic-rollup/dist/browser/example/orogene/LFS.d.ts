import { OrogeneFileSystem } from "./ts-pkg-manager";
export declare class LFS implements OrogeneFileSystem {
    private efs;
    private encoder;
    constructor();
    readDirectory(_path: string): Promise<string[]>;
    removeDirectory(path: string, options?: {
        recursive?: boolean;
    }): Promise<void>;
    createDirectory(_path: string): Promise<void>;
    directoryExists(_path: string): Promise<boolean>;
    readFile(_path: string, _encoding?: string): Promise<string | undefined>;
    writeFile(path: string, content: string | Uint8Array, options?: {
        encoding?: "utf8";
        mode?: number;
    }): Promise<void>;
    deleteFile(_path: string): Promise<void>;
    fileExists(_path: string): Promise<boolean>;
}
