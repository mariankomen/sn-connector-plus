import type { IsomorphicRollupFs } from "../fs-iface";

export const exists = (fs: IsomorphicRollupFs) => async (filePath: string) => {
  try {
    const res = await fs.stat(filePath);
    return res.isFile();
  } catch {
    return false;
  }
};
