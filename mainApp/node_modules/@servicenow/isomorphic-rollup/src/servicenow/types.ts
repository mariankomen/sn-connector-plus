export type RegisterExplicitIdFunction = (
  table: string,
  key: string | number,
  guidOverride?: string,
) => string;

export interface Credential {
  getUrl(): URL;
  getHeaders(): Promise<Record<string, string>>;
}
