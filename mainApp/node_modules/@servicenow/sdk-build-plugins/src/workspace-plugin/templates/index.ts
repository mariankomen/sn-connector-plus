import { handledEvents, extendedFrom, category } from './record-page'
import { data } from './record-page-data'
import { properties } from './record-page-properties'
import { internalEventMappings } from './record-page-interalEventMappings'
import { layoutModel } from './record-page-layoutModel'
import { composition } from './record-page-composition'

export const recordPage = {
    data,
    internalEventMappings,
    layoutModel,
    composition,
    properties,
    handledEvents,
    extendedFrom,
    category,
}
