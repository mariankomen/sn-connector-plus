"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TableOwnership = exports.RecordPlugin = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const now_id_plugin_1 = require("./now-id-plugin");
const app_1 = require("@servicenow/sdk-core/runtime/app");
const service_portal_1 = require("@servicenow/sdk-core/runtime/service-portal");
const service_catalog_1 = require("@servicenow/sdk-core/runtime/service-catalog");
const clientscript_1 = require("@servicenow/sdk-core/runtime/clientscript");
const sys_1 = require("@servicenow/sdk-core/runtime/sys");
const ui_1 = require("@servicenow/sdk-core/runtime/ui");
const db_1 = require("@servicenow/sdk-core/runtime/db");
const rest_1 = require("@servicenow/sdk-core/runtime/rest");
const notification_1 = require("@servicenow/sdk-core/runtime/notification");
const ui_2 = require("@servicenow/sdk-core/runtime/ui");
const uxf_1 = require("@servicenow/sdk-core/runtime/uxf");
const dashboard_1 = require("@servicenow/sdk-core/runtime/dashboard");
exports.RecordPlugin = sdk_build_core_1.Plugin.create({
    name: 'RecordPlugin',
    records: {
        '*': {
            getUpdateName(record) {
                return { success: true, value: `${record.getTable()}_${record.getId().getValue()}` };
            },
            async diff(existing, incoming) {
                const changeDatabase = existing.compare(incoming);
                return {
                    success: true,
                    value: changeDatabase.hasChanges() ? new sdk_build_core_1.Database(changeDatabase.query()) : new sdk_build_core_1.Database(),
                };
            },
            async toShape(record, { compiler }) {
                const tableName = record.getTable();
                const columnTypes = compiler.getTableColumnTypes(tableName);
                const dataProperties = record.transform(({ $ }) => Object.fromEntries(record
                    .keys()
                    .filter((key) => key !== 'sys_name')
                    .map((key) => {
                    const shape = record.get(key);
                    if (columnTypes?.has(key) && shape?.isString()) {
                        const expectedType = columnTypes.get(key);
                        try {
                            if (expectedType === 'boolean') {
                                return [key, $.val(shape.toBoolean())];
                            }
                            else if (expectedType === 'number') {
                                return [key, $.val(shape.toNumber())];
                            }
                            else if (expectedType === 'array') {
                                return [key, $.val(shape.asString().split(',')).def([''])];
                            }
                            else if (expectedType === 'array-optional') {
                                const arrayParts = shape.asString().includes(',');
                                return [key, arrayParts ? $.val(shape.asString().split(',')).def(['']) : $];
                            }
                        }
                        catch {
                            // Keep as string if conversion fails
                        }
                    }
                    return [key, shape?.isString() ? $.def('') : $]; //to avoid writing empty string values to the code
                })));
                const value = new sdk_build_core_1.CallExpressionShape({
                    source: record,
                    callee: 'Record',
                    exportName: record.get('sys_name')?.ifDefined()?.asString().getValue(),
                    args: [
                        {
                            $id: now_id_plugin_1.NowIdShape.from(record),
                            table: tableName,
                            data: dataProperties,
                        },
                    ],
                });
                return {
                    success: true,
                    value,
                };
            },
            async toFile(record, { config, database, transform }) {
                const recordBuilder = (0, sdk_build_core_1.unloadBuilder)({
                    scope: config.scope,
                    scopeId: config.scopeId,
                    table: record.getTable(),
                });
                const updateName = await transform.getUpdateName(record);
                const builder = recordBuilder.record(record, updateName);
                record
                    .entries()
                    .sort(([a], [b]) => a.localeCompare(b)) // Sort keys to make outputs more deterministic
                    .forEach(([prop, shape]) => builder.field(prop, shape));
                const claims = database
                    .query('sys_claim')
                    .filter((claim) => claim.get('metadata_update_name').equals(updateName));
                for (const claim of claims) {
                    const claimUpdateName = await transform.getUpdateName(claim);
                    const claimBuilder = recordBuilder.record(claim, claimUpdateName);
                    claim
                        .entries()
                        .sort(([a], [b]) => a.localeCompare(b)) // Sort keys to make outputs more deterministic
                        .forEach(([prop, shape]) => claimBuilder.field(prop, shape));
                }
                return {
                    success: true,
                    value: {
                        source: record,
                        name: `${updateName}.xml`,
                        category: record.getInstallCategory(),
                        content: recordBuilder.end(),
                    },
                };
            },
        },
    },
    shapes: [
        {
            shape: sdk_build_core_1.CallExpressionShape,
            fileTypes: ['fluent'],
            async toRecord(callExpression, { factory, diagnostics }) {
                if (callExpression.getCallee() !== 'Record') {
                    return { success: false };
                }
                const record = callExpression.getArgument(0).asObject();
                const table = record.get('table').asString().getValue();
                const tableOwningPlugin = exports.TableOwnership[table];
                if (tableOwningPlugin) {
                    diagnostics.hint(callExpression, `For a better experience, consider using the ${tableOwningPlugin} API`);
                }
                return {
                    success: true,
                    value: await factory.createRecord({
                        source: callExpression,
                        table,
                        explicitId: record.get('$id'),
                        properties: record.get('data').asObject(),
                    }),
                };
            },
        },
    ],
    files: [
        {
            matcher: /\.xml$/,
            async toRecord(file, { parser, logger }) {
                try {
                    const records = await parser.parsePayload(file);
                    const recordMap = new Map();
                    for (const record of records) {
                        const key = `${record.getTable()}::${record.getId().getValue()}`;
                        const existing = recordMap.get(key);
                        const merged = existing
                            ? existing.merge(record.properties()) // merge properties only to retain the action
                            : record;
                        recordMap.set(key, merged);
                    }
                    const mergedRecords = Array.from(recordMap.values());
                    const [mergedFirst, ...mergedRest] = mergedRecords;
                    if (!mergedFirst) {
                        return { success: false };
                    }
                    return { success: true, value: mergedFirst.with(...mergedRest) };
                }
                catch (e) {
                    logger.debug(e);
                    return { success: false };
                }
            },
        },
    ],
});
exports.TableOwnership = {
    contract_sla: app_1.Sla.name,
    sys_security_acl: app_1.Acl.name,
    sys_security_acl_role: app_1.Acl.name,
    sys_app_application: app_1.ApplicationMenu.name,
    sys_script: app_1.BusinessRule.name,
    sys_script_client: clientscript_1.ClientScript.name,
    sys_scope_privilege: app_1.CrossScopePrivilege.name,
    sys_ui_list_element: ui_1.List.name,
    sys_ui_list: ui_1.List.name,
    sys_properties: app_1.Property.name,
    sys_user_role: app_1.Role.name,
    sys_user_role_contains: app_1.Role.name,
    sys_script_include: sys_1.ScriptInclude.name,
    sp_widget: service_portal_1.SPWidget.name,
    m2m_sp_widget_dependency: service_portal_1.SPWidget.name,
    m2m_sp_ng_pro_sp_widget: service_portal_1.SPWidget.name,
    sp_ng_template: service_portal_1.SPWidget.name,
    sp_dependency: service_portal_1.SPWidgetDependency.name,
    m2m_sp_dependency_css_include: service_portal_1.SPWidgetDependency.name,
    m2m_sp_dependency_js_include: service_portal_1.SPWidgetDependency.name,
    sp_css_include: service_portal_1.CssInclude.name,
    sp_js_include: service_portal_1.JsInclude.name,
    sp_angular_provider: service_portal_1.SPAngularProvider.name,
    m2m_sp_ng_pro_sp_ng_pro: service_portal_1.SPAngularProvider.name,
    sys_atf_test: app_1.Test.name,
    sys_ws_header_map: rest_1.RestApi.name,
    sys_ws_query_parameter_map: rest_1.RestApi.name,
    sys_ws_definition: rest_1.RestApi.name,
    sys_ws_operation: rest_1.RestApi.name,
    sys_ws_version: rest_1.RestApi.name,
    sys_ws_header: rest_1.RestApi.name,
    sys_ws_query_parameter: rest_1.RestApi.name,
    sys_user_preference: app_1.UserPreference.name,
    sys_ui_page: ui_2.UiPage.name,
    sys_ui_action: ui_2.UiAction.name,
    sys_ui_action_role: ui_2.UiAction.name,
    sys_ui_action_view: ui_2.UiAction.name,
    sys_ui_policy: ui_2.UiPolicy.name,
    sys_ui_policy_action: ui_2.UiPolicy.name,
    sys_ui_policy_rl_action: ui_2.UiPolicy.name,
    sysevent_script_action: sys_1.ScriptAction.name,
    sysevent_email_action: notification_1.EmailNotification.name,
    sys_db_object: db_1.Table.name,
    sys_dictionary: db_1.Table.name,
    sys_hub_flow: 'Flow',
    sys_hub_trigger_instance_v2: 'wfa.trigger',
    sys_hub_action_instance_v2: 'wfa.action',
    sys_hub_sub_flow_instance_v2: 'wfa.subflow',
    sys_hub_flow_logic_instance_v2: 'wfa.flowLogic',
    sys_flow_step_definition: 'ActionStepDefinition',
    sys_decision: 'DecisionTablePlugin',
    sys_hub_action_type_definition: 'Action',
    sys_hub_step_instance: 'wfa.action_step',
    sys_transform_map: app_1.ImportSet.name,
    sys_transform_entry: app_1.ImportSet.name,
    sys_transform_script: app_1.ImportSet.name,
    sys_ux_applicability: uxf_1.Applicability.name,
    sys_ux_list_menu_config: uxf_1.UxListMenuConfig.name,
    sys_ux_list_category: uxf_1.UxListMenuConfig.name,
    sys_ux_list: uxf_1.UxListMenuConfig.name,
    sys_ux_applicability_m2m_list: uxf_1.UxListMenuConfig.name,
    sys_ux_page_registry: uxf_1.Workspace.name,
    sys_ux_app_config: uxf_1.Workspace.name,
    par_dashboard: dashboard_1.Dashboard.name,
    par_dashboard_canvas: dashboard_1.Dashboard.name,
    par_dashboard_tab: dashboard_1.Dashboard.name,
    par_dashboard_widget: dashboard_1.Dashboard.name,
    par_dashboard_permission: dashboard_1.Dashboard.name,
    par_dashboard_visibility: dashboard_1.Dashboard.name,
    item_option_new_set: service_catalog_1.VariableSet.name,
    sc_cat_item: service_catalog_1.CatalogItem.name,
    sc_cat_item_producer: service_catalog_1.CatalogItemRecordProducer.name,
    catalog_script_client: service_catalog_1.CatalogClientScript.name,
    catalog_ui_policy: service_catalog_1.CatalogUiPolicy.name,
};
//# sourceMappingURL=record-plugin.js.map