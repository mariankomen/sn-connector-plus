import { type CallExpressionShape, type Diagnostics, type Factory, type Record, type Shape } from '@servicenow/sdk-build-core';
/**
 * Extracts the label for the default language from a label shape.
 * When label is a string, returns it directly.
 * When label is an array of documentation objects, finds the entry matching
 * the default language and returns its label property.
 */
export declare function getLabelForDefaultLanguage(labelShape: Shape, tableDefaultLanguage: string): Shape | undefined;
export declare function getDocumentationRecords(callExpression: CallExpressionShape, aliasedKeys: globalThis.Record<string, string[]>, factory: Factory, diagnostics: Diagnostics, generateDefaultDoc?: boolean, tableDefaultLanguage?: string): Promise<Record[]>;
export declare function getChoiceRecords(callExpression: CallExpressionShape, aliasedKeys: globalThis.Record<string, string[]>, factory: Factory, diagnostics: Diagnostics, tableDefaultLanguage?: string): Promise<Record[]>;
