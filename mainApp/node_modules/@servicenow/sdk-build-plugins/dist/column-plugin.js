"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ColumnPlugin = exports.BCP47_LANGUAGE_TAG_PATTERN = void 0;
exports.columnToCallExpression = columnToCallExpression;
exports.generatePlural = generatePlural;
exports.isDefaultDocumentation = isDefaultDocumentation;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const db_1 = require("@servicenow/sdk-core/runtime/db");
const isEqual_1 = __importDefault(require("lodash/isEqual"));
const column_to_record_1 = require("./column/column-to-record");
const column_helper_1 = require("./column/column-helper");
const server_module_plugin_1 = require("./server-module-plugin");
const utils_1 = require("./utils");
const xmlbuilder2_1 = require("xmlbuilder2");
// BCP 47 language tag pattern: 2-3 letter base language with optional subtags (e.g., 'en', 'zh-Hans', 'en-US')
exports.BCP47_LANGUAGE_TAG_PATTERN = /^[a-z]{2,3}(-[a-zA-Z0-9]{2,8})*$/;
const PLURAL_EQUALS_LABEL = [
    's',
    'ed',
    'by',
    'Active',
    ')',
    ' to',
    ' with',
    ' for',
    ' in',
    ' out',
    ' use',
    'Data',
    ' data',
];
const PLURAL_SPECIAL_CASE_Y_TO_IES = ['ry', 'ty', 'dy', 'cy', 'fy', 'ly', 'ny'];
const PLURAL_SPECIAL_CASE_ADD_ES = ['x', 'sh', 'ch', 'z'];
const columnAliases = {
    readOnly: ['read_only'],
    functionDefinition: ['function_definition'],
    tableReference: ['table_reference'],
    elementReference: ['element_reference'],
    spellCheck: ['spell_check'],
    xmlView: ['xml_view'],
    textIndex: ['text_index'],
    dynamicValueDefinitions: ['dynamic_value_definitions'],
    referenceFloats: ['reference_floats'],
    dynamicCreationScript: ['dynamic_creation_script'],
    dynamicCreation: ['dyamic_creation'], // maintaining backwards compatibility with typo
    referenceKey: ['reference_key'],
    referenceQual: ['reference_qual'],
    columnType: ['column_type'],
};
const calculationAliases = {
    calculatedValue: ['calculated_value'],
    columnName: ['column_name'],
};
const choiceAliases = {
    dependentValue: ['dependent_value'],
    inactiveOnUpdate: ['inactive_on_update'],
};
const documentationAliases = {
    urlTarget: ['url_target'],
};
exports.ColumnPlugin = sdk_build_core_1.Plugin.create({
    name: 'ColumnPlugin',
    records: {
        sys_choice: {
            coalesce: ['name', 'element', 'value'],
            getUpdateName: (record) => ({
                success: true,
                value: `sys_choice_${record.get('name').getValue()}_${record.get('element').getValue()}`,
            }),
        },
        sys_choice_set: {
            coalesce: ['name', 'element'],
            relationships: {
                sys_choice: {
                    via: {
                        name: 'name',
                        element: 'element',
                    },
                    descendant: true,
                },
            },
            getUpdateName: (record) => ({
                success: true,
                // Seems odd, but according to UpdateName.java, sys_choice and sys_choice_set share the same update naming logic
                value: `sys_choice_${record.get('name').getValue()}_${record.get('element').getValue()}`,
            }),
            async toFile(choiceSet, { config, descendants, transform }) {
                if (config.tableOutputFormat === 'bootstrap' && config.type === 'package') {
                    // Defer to table plugin
                    return { success: false };
                }
                const tableName = choiceSet.get('name').asString().getValue();
                const elementName = choiceSet.get('element').asString().getValue();
                const xml = (0, xmlbuilder2_1.create)().ele('record_update');
                const root = xml.ele('sys_choice_set', { table: tableName, field: elementName });
                descendants.query('sys_choice').forEach((choice) => {
                    const child = root.ele('sys_choice', { action: choice.getAction() });
                    child.ele('sys_id').txt(choice.getId().getValue());
                    child.ele('name').txt(tableName);
                    child.ele('element').txt(elementName);
                    for (const prop of [
                        'label',
                        'value',
                        'sequence',
                        'dependent_value',
                        'hint',
                        'inactive',
                        'inactive_on_update',
                        'language',
                    ]) {
                        choice
                            .get(prop)
                            .ifDefined()
                            ?.toString()
                            .pipe((p) => child.ele(prop).txt(p.getValue()));
                    }
                });
                const child = root.ele('sys_choice_set', { action: choiceSet.getAction() });
                child.ele('sys_id').txt(choiceSet.getId().getValue());
                child.ele('sys_scope', { display_value: config.scope }).txt(config.scopeId);
                child.ele('name').txt(tableName);
                child.ele('element').txt(elementName);
                return {
                    success: true,
                    value: {
                        source: choiceSet,
                        name: `${await transform.getUpdateName(choiceSet)}.xml`,
                        category: choiceSet.getInstallCategory(),
                        content: xml.end({ prettyPrint: true }),
                    },
                };
            },
        },
    },
    shapes: [
        {
            shape: sdk_build_core_1.CallExpressionShape,
            fileTypes: ['fluent'],
            async toRecord(callExpression, { factory, diagnostics, config }) {
                const callee = callExpression.getCallee();
                if (!(callee in column_helper_1.COLUMN_API_TO_TYPE) && !(callee === db_1.GenericColumn.name)) {
                    return { success: false };
                }
                const column = callExpression.getArgument(0).asObject().withAliasedKeys(columnAliases);
                (0, utils_1.generateDeprecatedDiagnostics)(column, diagnostics);
                if (!column.has('table') || !column.has('name')) {
                    return { success: false };
                }
                const documentationRecords = await (0, column_to_record_1.getDocumentationRecords)(callExpression, documentationAliases, factory, diagnostics, true, config.tableDefaultLanguage);
                const choiceRecords = await (0, column_to_record_1.getChoiceRecords)(callExpression, choiceAliases, factory, diagnostics, config.tableDefaultLanguage);
                const choiceSetRecords = [];
                if (choiceRecords.length > 0) {
                    choiceSetRecords.push(await factory.createRecord({
                        source: callExpression,
                        table: 'sys_choice_set',
                        properties: {
                            name: column.get('table').asString().getValue(),
                            element: column.get('name').asString().getValue(),
                        },
                    }));
                }
                const columnType = column_helper_1.COLUMN_API_TO_TYPE[callee] ?? column.get('columnType').asString().getValue();
                return {
                    success: true,
                    value: (await factory.createRecord({
                        source: callExpression,
                        table: 'sys_dictionary',
                        properties: column.transform(({ $ }) => ({
                            name: $.from('table'),
                            element: $.from('name'),
                            internal_type: $.from('isFullUTF8')
                                .map((isFullUTF8) => isFullUTF8?.isBoolean() &&
                                isFullUTF8.asBoolean().getValue() &&
                                columnType === 'string'
                                ? 'string_full_utf8'
                                : columnType)
                                .def(columnType),
                            active: $.toBoolean().def(true),
                            array: $.toBoolean().def(false),
                            attributes: $.from('attributes', 'scale', 'script')
                                .map((attributes, scale, script) => {
                                const attrs = attributes?.isObject() ? attributes.asObject().getValue() : {};
                                // Add special parameters to attributes
                                if (scale?.isNumber()) {
                                    attrs['scale'] = scale.asNumber().getValue();
                                }
                                if (script?.isString()) {
                                    attrs['script'] = script.asString().getValue();
                                }
                                return Object.entries(attrs)
                                    .map(([key, value]) => `${key}=${value}`)
                                    .join(',');
                            })
                                .def(''),
                            audit: $.toBoolean().def(false),
                            reference_cascade_rule: $.from('cascadeRule'),
                            default_value: $.from('default'),
                            display: $.toBoolean().def(false),
                            dynamic_creation: $.from('dynamicCreation').toBoolean(),
                            dynamic_creation_script: $.from('dynamicCreationScript'),
                            reference_floats: $.from('referenceFloats'),
                            reference_key: $.from('referenceKey'),
                            reference_qual: $.from('referenceQual'),
                            calculation: $.from('dynamicValueDefinitions')
                                .map((dynamicValueDefinitions) => {
                                if (dynamicValueDefinitions.ifObject()?.get('type').ifString()?.getValue() !==
                                    'calculated_value') {
                                    return undefined;
                                }
                                const dynamicValueObject = dynamicValueDefinitions
                                    .asObject()
                                    .withAliasedKeys(calculationAliases);
                                (0, utils_1.generateDeprecatedDiagnostics)(dynamicValueObject, diagnostics);
                                const calculatedValue = dynamicValueObject.get('calculatedValue');
                                if (calculatedValue.isUnresolved()) {
                                    diagnostics.error(calculatedValue.getOriginalNode(), `Unable to resolve the script reference, ensure the imported module is within the ${config.serverModulesDir} directory.`);
                                }
                                return (calculatedValue
                                    .if(server_module_plugin_1.ModuleFunctionShape)
                                    ?.toString((n) => `${n}({{PARAMS}})`, ['current']) ?? calculatedValue);
                            })
                                .toCdata(),
                            choice: $.from('dropdown')
                                .map((d) => d.isString()
                                ? Math.max(db_1.choiceDropdown.indexOf(d.getValue()), 0)
                                : undefined)
                                .def(0),
                            choice_table: $.from('dynamicValueDefinitions').map((dynamicValueDefinitions) => dynamicValueDefinitions.ifObject()?.get('type').ifString()?.getValue() ===
                                'choices_from_other_table'
                                ? dynamicValueDefinitions.asObject().get('table').ifString()
                                : undefined),
                            virtual: $.from('dynamicValueDefinitions')
                                .map((dynamicValueDefinitions) => {
                                const calculationType = dynamicValueDefinitions
                                    .ifObject()
                                    ?.get('type')
                                    .ifString()
                                    ?.getValue();
                                if (!calculationType) {
                                    return undefined;
                                }
                                return calculationType === 'calculated_value';
                            })
                                .toBoolean(),
                            choice_field: $.from('dynamicValueDefinitions').map((dynamicValueDefinitions) => dynamicValueDefinitions.ifObject()?.get('type').ifString()?.getValue() ===
                                'choices_from_other_table'
                                ? dynamicValueDefinitions.asObject().get('field').ifString()
                                : undefined),
                            dependent: $.from('dependent', 'dynamicValueDefinitions').map((dependent, dynamicValueDefinitions) => {
                                // New direct approach (DocumentIdColumn, TemplateValueColumn, FieldListColumn)
                                if (dependent?.isString()) {
                                    return dependent.asString().getValue();
                                }
                                if (dependent?.isObject()) {
                                    diagnostics.warn(dependent.getOriginalNode(), `Passing a TableNameColumn object to 'dependent' is deprecated and will not work as expected. Pass the field name as a string instead.`);
                                }
                                // Legacy approach via dynamicValueDefinitions
                                if (dynamicValueDefinitions?.ifObject()?.get('type').ifString()?.getValue() ===
                                    'dependent_field') {
                                    const dynamicValueObject = dynamicValueDefinitions
                                        .asObject()
                                        .withAliasedKeys(calculationAliases);
                                    (0, utils_1.generateDeprecatedDiagnostics)(dynamicValueObject, diagnostics);
                                    diagnostics.warn(dynamicValueDefinitions.getOriginalNode(), `Using 'dynamicValueDefinitions: { type: "dependent_field" }' is deprecated. ` +
                                        `Use 'dependent: "field_name"' directly on the column instead.`);
                                    return dynamicValueObject.get('columnName').ifString()?.getValue();
                                }
                                return undefined;
                            }),
                            dependent_on_field: $.from('dynamicValueDefinitions').map((dynamicValueDefinitions) => {
                                if (dynamicValueDefinitions.ifObject()?.get('type').ifString()?.getValue() !==
                                    'dependent_field') {
                                    return undefined;
                                }
                                const dynamicValueObject = dynamicValueDefinitions
                                    .asObject()
                                    .withAliasedKeys(calculationAliases);
                                (0, utils_1.generateDeprecatedDiagnostics)(dynamicValueObject, diagnostics);
                                return dynamicValueObject.get('columnName').ifString();
                            }),
                            use_dependent_field: $.from('dynamicValueDefinitions')
                                .map((dynamicValueDefinitions) => dynamicValueDefinitions.ifObject()?.get('type').ifString()?.getValue() ===
                                'dependent_field')
                                .def(false),
                            element_reference: $.from('elementReference').toBoolean().def(false),
                            function_definition: $.from('functionDefinition'),
                            function_field: $.from('functionDefinition')
                                .map((f) => f.isString() && f.getValue() !== '')
                                .def(false),
                            column_label: $.from('label')
                                .map((label) => (0, column_to_record_1.getLabelForDefaultLanguage)(label, config.tableDefaultLanguage))
                                .def(column.get('name').asString()),
                            mandatory: $.toBoolean().def(false),
                            max_length: $.from('maxLength')
                                .map((maxLength) => maxLength.ifNumber()?.getValue() ?? maxLength.ifString()?.getValue())
                                .def((0, column_helper_1.getDefaultMaxLength)(columnType)),
                            primary: $.toBoolean().def(false),
                            read_only: $.from('readOnly', 'readOnlyOption')
                                .map((readOnly, readOnlyOption) => {
                                const readOnlyValue = readOnly.ifBoolean()?.getValue();
                                const readOnlyOptionValue = readOnlyOption.ifString()?.getValue();
                                if (readOnlyValue === false && readOnlyOptionValue) {
                                    diagnostics.error(readOnly, `readOnly cannot be false with readOnlyOption ${readOnlyOptionValue}.`);
                                }
                                return readOnlyValue ?? !!readOnlyOptionValue;
                            })
                                .def(false),
                            read_only_option: $.from('readOnlyOption'),
                            reference: $.from('referenceTable'),
                            spell_check: $.from('spellCheck').toBoolean().def(false),
                            table_reference: $.from('tableReference').toBoolean().def(false),
                            text_index: $.from('textIndex').toBoolean().def(false),
                            unique: $.toBoolean().def(false),
                            widget: $,
                            xml_view: $.from('xmlView').toBoolean().def(false),
                        })),
                    })).with(...documentationRecords, ...choiceRecords, ...choiceSetRecords),
                };
            },
        },
    ],
});
function columnToCallExpression(column, related) {
    const internalType = column.get('internal_type').asString().getValue();
    // Normalize string_full_utf8 to string for column type lookup
    const normalizedType = internalType === 'string_full_utf8' ? 'string' : internalType;
    const callExpression = column_helper_1.COLUMN_TYPE_TO_API[normalizedType] ?? db_1.GenericColumn.name;
    const { choices, documentation } = related;
    return new sdk_build_core_1.CallExpressionShape({
        source: column,
        callee: callExpression,
        args: [
            column
                .transform(({ $ }) => ({
                active: $.toBoolean().def(true),
                array: $.toBoolean().def(false),
                attributes: $.map((attributes) => {
                    if (!attributes.isString()) {
                        return undefined;
                    }
                    const result = {};
                    attributes
                        .toString()
                        .getValue()
                        .split(',')
                        .forEach((attr) => {
                        if (attr === '') {
                            return;
                        }
                        const [key, value] = attr.split('=').map((s) => s.trim());
                        // Filter out special parameters that have their own fields
                        if (key === 'scale' || key === 'script') {
                            return;
                        }
                        if (!key || value === undefined) {
                            return;
                        }
                        if (value === 'true') {
                            result[key] = true;
                        }
                        else if (value === 'false') {
                            result[key] = false;
                        }
                        else {
                            result[key] = value;
                        }
                    });
                    return result;
                }).def({
                    edge_encryption_enabled: true,
                }),
                // Extract special parameters from attributes
                scale: $.from('attributes').map((attributes) => {
                    if (!attributes.isString()) {
                        return undefined;
                    }
                    const scaleMatch = attributes
                        .asString()
                        .getValue()
                        .match(/scale=([^,]+)/);
                    return scaleMatch ? Number(scaleMatch[1]) : undefined;
                }),
                script: $.from('attributes').map((attributes) => {
                    if (!attributes.isString()) {
                        return undefined;
                    }
                    const scriptMatch = attributes
                        .asString()
                        .getValue()
                        .match(/script=([^,]+)/);
                    return scriptMatch ? scriptMatch[1] : undefined;
                }),
                source_table: $.from('attributes').map((attributes) => {
                    if (!attributes.isString()) {
                        return undefined;
                    }
                    const sourceTableMatch = attributes
                        .asString()
                        .getValue()
                        .match(/source_table=([^,]+)/);
                    return sourceTableMatch ? sourceTableMatch[1] : undefined;
                }),
                isFullUTF8: $.from('internal_type').map((internalType) => internalType?.isString() && internalType.asString().getValue() === 'string_full_utf8'
                    ? true
                    : undefined),
                audit: $.toBoolean().def(false),
                cascadeRule: $.from('reference_cascade_rule').def(''),
                columnType: callExpression === db_1.GenericColumn.name ? $.from('internal_type') : $.val(undefined),
                default: $.from('default_value')
                    .map((defaultValue) => {
                    // TODO Solve default type inference generally
                    if (callExpression === db_1.BooleanColumn.name) {
                        if (defaultValue.isBoolean()) {
                            return defaultValue;
                        }
                        else if (defaultValue.isString()) {
                            const defaultString = defaultValue.asString().getValue().trim();
                            if (defaultString === 'true') {
                                return true;
                            }
                            if (defaultString === 'false') {
                                return false;
                            }
                        }
                    }
                    if (defaultValue.isNumber()) {
                        return defaultValue.getValue().toString();
                    }
                    return defaultValue;
                })
                    .def(''),
                choices: choices.length
                    ? $.val(choices.reduce((acc, choiceRecord) => {
                        const value = choiceRecord.get('value').asString().getValue();
                        acc[value] = choiceRecord
                            .transform(({ $ }) => ({
                            label: $,
                            sequence: $.map((sequence) => {
                                if (sequence.isNumber()) {
                                    return sequence;
                                }
                                else if (sequence.isString()) {
                                    const sequenceVal = sequence.asString().getValue().trim();
                                    return sequenceVal !== '' && !isNaN(Number(sequenceVal))
                                        ? Number(sequenceVal)
                                        : undefined;
                                }
                                return undefined;
                            }),
                            dependentValue: $.from('dependent_value').map((depVal) => {
                                if (depVal.isNumber()) {
                                    return depVal.asNumber().getValue();
                                }
                                else if (depVal.isString()) {
                                    const depValue = depVal.asString().getValue().trim();
                                    return ((isNaN(Number(depValue)) ? depValue : Number(depValue)) ||
                                        undefined);
                                }
                                return undefined;
                            }),
                            inactive: $.toBoolean().def(false),
                            inactiveOnUpdate: $.from('inactive_on_update').toBoolean().def(false),
                            hint: $.def(''),
                            language: $.def(related.tableDefaultLanguage ?? 'en'),
                        }))
                            .withAliasedKeys(choiceAliases);
                        return acc;
                    }, {}))
                    : $.val(undefined),
                dropdown: $.from('choice')
                    .map((choice) => {
                    let dropdownIndex = -1;
                    if (choice.isNumber()) {
                        dropdownIndex = choice.asNumber().getValue();
                    }
                    else if (choice.isString()) {
                        const parsedChoice = Number(choice.asString().getValue());
                        if (!isNaN(parsedChoice)) {
                            dropdownIndex = parsedChoice;
                        }
                    }
                    return dropdownIndex !== -1 ? db_1.choiceDropdown[dropdownIndex] : undefined;
                })
                    .def('none'),
                dynamicValueDefinitions: $.from('calculation', 'choice_table', 'choice_field').map((calculation, choiceTable, choiceField) => {
                    if (calculation.ifString()?.getValue() &&
                        calculation.asString().getValue() !== column_helper_1.DEFAULT_COLUMN_CALCULATION) {
                        return new sdk_build_core_1.ObjectShape({
                            source: calculation,
                            properties: {
                                type: 'calculated_value',
                                calculatedValue: calculation,
                            },
                        }).withAliasedKeys(calculationAliases);
                    }
                    if (choiceTable.ifString()?.getValue() && choiceField.ifString()?.getValue()) {
                        return {
                            type: 'choices_from_other_table',
                            table: choiceTable.asString().getValue(),
                            field: choiceField.asString().getValue(),
                        };
                    }
                    return undefined;
                }),
                // Direct dependent parameter for DocumentIdColumn, TemplateValueColumn, and FieldListColumn
                dependent: $.from('dependent').map((dependent) => {
                    if (dependent?.isString()) {
                        const value = dependent.asString().getValue();
                        return value !== '' ? value : undefined;
                    }
                    return undefined;
                }),
                elementReference: $.from('element_reference').toBoolean().def(false),
                functionDefinition: $.from('function_definition').def(''),
                label: documentation.length &&
                    !isDefaultDocumentation(column.get('element').asString().getValue(), documentation, related.tableDefaultLanguage)
                    ? $.val(documentation.map((doc) => doc
                        .transform(({ $ }) => ({
                        hint: $.def(''),
                        help: $.def(''),
                        label: $.def(''),
                        plural: $.def(''),
                        language: $.def(related.tableDefaultLanguage ?? 'en'),
                        url: $.def(''),
                        urlTarget: $.from('url_target').def(''),
                    }))
                        .withAliasedKeys(documentationAliases)))
                    : $.from('column_label').def(column.get('element').getValue()),
                mandatory: $.toBoolean().def(false),
                maxLength: $.from('max_length')
                    .map((maxLength) => {
                    if (maxLength.isNumber()) {
                        return maxLength.asNumber();
                    }
                    if (maxLength.isString()) {
                        const numVal = parseInt(maxLength.asString().getValue());
                        return isNaN(numVal) ? maxLength : numVal;
                    }
                    return undefined;
                })
                    .def((0, column_helper_1.getDefaultMaxLength)(column.get('internal_type').asString().getValue())),
                primary: $.toBoolean().def(false),
                readOnly: $.from('read_only').toBoolean().def(false),
                readOnlyOption: $.from('read_only_option').def(''),
                referenceTable: $.from('reference').def(''),
                referenceFloats: $.from('reference_floats').toBoolean().def(false),
                referenceKey: $.from('reference_key').def(''),
                referenceQual: $.from('reference_qual').def(''),
                dynamicCreation: $.from('dynamic_creation').toBoolean().def(false),
                dynamicCreationScript: $.from('dynamic_creation_script').def(''),
                spellCheck: $.from('spell_check').toBoolean().def(false),
                tableReference: $.from('table_reference').toBoolean().def(false),
                textIndex: $.from('text_index').toBoolean().def(false),
                unique: $.toBoolean().def(false),
                widget: $.def(''),
                xmlView: $.from('xml_view').toBoolean().def(false),
            }))
                .withAliasedKeys(columnAliases),
        ],
    });
}
function generatePlural(label) {
    if (!label) {
        return undefined;
    }
    for (const suffix of PLURAL_EQUALS_LABEL) {
        if (label.endsWith(suffix)) {
            return label;
        }
    }
    for (const suffix of PLURAL_SPECIAL_CASE_Y_TO_IES) {
        if (label.endsWith(suffix)) {
            return label.substring(0, label.lastIndexOf('y')) + 'ies';
        }
    }
    for (const suffix of PLURAL_SPECIAL_CASE_ADD_ES) {
        if (label.endsWith(suffix)) {
            return label + 'es';
        }
    }
    return label + 's';
}
function isDefaultDocumentation(columnName, documentation, tableDefaultLanguage = 'en') {
    if (documentation.length !== 1) {
        return false;
    }
    const doc = documentation[0].asObject().getValue();
    const label = doc['label'] ?? columnName;
    const defaultRecord = {
        name: doc['name'],
        element: doc['element'],
        label,
        plural: generatePlural(label),
        language: tableDefaultLanguage,
    };
    if ('sys_name' in doc) {
        defaultRecord['sys_name'] = label;
    }
    const optionalFields = ['hint', 'help', 'url', 'url_target', 'sys_policy'];
    optionalFields.forEach((field) => {
        if (field in doc) {
            defaultRecord[field] = '';
        }
    });
    return (0, isEqual_1.default)(defaultRecord, doc);
}
//# sourceMappingURL=column-plugin.js.map