"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.JsonPlugin = exports.JsonFileShape = exports.JsonStringifyShape = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
class JsonStringifyShape extends sdk_build_core_1.CallExpressionShape {
    constructor({ source, json }) {
        super({ source, callee: 'JSON.stringify', args: [json] });
    }
    getJson() {
        const arg = this.getArgument(0);
        return arg.isArray() ? arg.asArray() : arg.asObject();
    }
    getValue() {
        return JSON.stringify(this.getJson().getValue());
    }
}
exports.JsonStringifyShape = JsonStringifyShape;
class JsonFileShape extends sdk_build_core_1.SourceFileShape {
    json;
    constructor({ source, json, path, content, }) {
        super({ source, path, content });
        const shape = sdk_build_core_1.Shape.from(source, json);
        this.json = shape.isArray()
            ? shape.asArray()
            : shape.asObject('Expected JSON file content to be a JSON object or array');
    }
    getJson() {
        return this.json;
    }
}
exports.JsonFileShape = JsonFileShape;
exports.JsonPlugin = sdk_build_core_1.Plugin.create({
    name: 'JsonPlugin',
    shapes: [
        {
            shape: sdk_build_core_1.SourceFileShape,
            fileTypes: ['json'],
            async toSubclass(file, { compiler, transform }) {
                const actualFile = compiler.getSourceFile(file.getPath());
                if (!actualFile) {
                    return {
                        success: true,
                        value: new JsonFileShape({
                            source: file,
                            path: file.getPath(),
                            content: file.getContent(),
                            json: sdk_build_core_1.JSON5.parse(file.getContent()),
                        }),
                    };
                }
                // Get the root-level expression from the first statement
                // We need to check the direct child, not descendants, to avoid matching nested arrays/objects
                const firstStatement = actualFile.getStatements()[0];
                const rootExpression = firstStatement?.isKind(sdk_build_core_1.ts.SyntaxKind.ExpressionStatement)
                    ? firstStatement.getExpression()
                    : undefined;
                const root = rootExpression?.isKind(sdk_build_core_1.ts.SyntaxKind.ObjectLiteralExpression) ||
                    rootExpression?.isKind(sdk_build_core_1.ts.SyntaxKind.ArrayLiteralExpression)
                    ? rootExpression
                    : undefined;
                if (!root) {
                    return { success: false };
                }
                const json = await transform.toShape(root);
                if (!json.success) {
                    return { success: false };
                }
                return {
                    success: true,
                    value: new JsonFileShape({
                        source: file,
                        json: json.value,
                        path: file.getPath(),
                        content: file.getContent(),
                    }),
                };
            },
        },
        {
            shape: sdk_build_core_1.CallExpressionShape,
            fileTypes: ['fluent'],
            toSubclass(callExpression, { diagnostics }) {
                if (callExpression.getCallee() !== 'JSON.stringify') {
                    return { success: false };
                }
                if (callExpression.getArguments().length > 1) {
                    diagnostics.error(callExpression.getArgument(1), 'Additional arguments to JSON.stringify are not supported');
                    return { success: false };
                }
                const arg = callExpression.getArgument(0);
                if (!arg.isObject() && !arg.isArray() && !arg.is(JsonFileShape)) {
                    diagnostics.error(arg, 'Argument to JSON.stringify must be an object, array, or an import from a JSON file');
                    return { success: false };
                }
                return {
                    success: true,
                    value: new JsonStringifyShape({
                        source: callExpression,
                        json: arg.if(JsonFileShape)?.getJson() ?? arg,
                    }),
                };
            },
        },
        {
            shape: sdk_build_core_1.StringShape,
            async commit(shape, target, { logger, transform, commit }) {
                const result = await transform.toShape(target);
                if (!result.success) {
                    return { success: false };
                }
                const { value: targetShape } = result;
                if (targetShape.is(JsonStringifyShape)) {
                    await commit(shape, targetShape.getJson().getOriginalNode());
                    return { success: true };
                }
                if (!targetShape.isObject() && !targetShape.isArray() && !targetShape.is(JsonFileShape)) {
                    return { success: false };
                }
                let json;
                try {
                    const parsed = sdk_build_core_1.Shape.from(shape, sdk_build_core_1.JSON5.parse(shape.getValue()));
                    json = parsed.isArray() ? parsed.asArray() : parsed.asObject();
                }
                catch (e) {
                    logger.info('Incoming string value could not be parsed as JSON and will overwrite the node');
                    return { success: false };
                }
                await commit(json, (targetShape.if(JsonFileShape)?.getJson() ?? targetShape).getOriginalNode());
                return { success: true };
            },
        },
    ],
});
//# sourceMappingURL=json-plugin.js.map