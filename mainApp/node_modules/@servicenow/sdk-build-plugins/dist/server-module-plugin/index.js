"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServerModulePlugin = exports.ModuleDependencyShape = exports.ModuleFunctionShape = exports.NODE_MODULES = void 0;
exports.clearDependencyCache = clearDependencyCache;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const sdk_build_core_2 = require("@servicenow/sdk-build-core");
const repack_1 = require("../repack");
const sbom_builder_1 = require("./sbom-builder");
const isEqual_1 = __importDefault(require("lodash/isEqual"));
const utils_1 = require("../utils");
const zip_1 = __importDefault(require("lodash/zip"));
const GLUE_CODE_PREFIX = '// @fluent-module';
const GLUE_CODE_META_REGEX = new RegExp(`^${GLUE_CODE_PREFIX} (.*);(true|false);(.*)`); // name;isDefault;path
const GLUE_CODE_WARNING = `
// WARNING: This code is generated by the ServiceNow SDK in order to provide
// support for modular JavaScript. Modifications of any kind are likely to
// result in unintended behavior. In most cases, you should edit the source
// file of the module imported below. If you are absolutely certain you want
// to take control of this code, you can remove this comment to prevent the
// SDK from regenerating it. However, you will then be responsible for the
// management of this code in your Fluent file.`;
exports.NODE_MODULES = 'node_modules';
/**
 * Check if a module matches any trusted module patterns.
 * Supports exact matches ('lodash', '@servicenow/sdk') and org wildcards ('@servicenow/*').
 * Invalid patterns (like '*' or 'lodash-*-utils') are ignored.
 */
function isModuleTrusted(moduleName, trustedModules, logger) {
    if (trustedModules.length === 0) {
        return false;
    }
    return trustedModules.some((pattern) => {
        // @org/* pattern
        if (pattern.startsWith('@') && pattern.endsWith('/*')) {
            return moduleName.startsWith(pattern.slice(0, -1));
        }
        // Exact match (no wildcards allowed)
        if (!pattern.includes('*')) {
            return moduleName === pattern;
        }
        // Invalid pattern, ignore
        logger.warn(`Invalid trusted module pattern used in fluent config: ${pattern}`);
        return false;
    });
}
function isGlueCode(string) {
    return string.startsWith(GLUE_CODE_PREFIX);
}
function parseMeta(string) {
    const value = typeof string === 'string' ? string : string.getValue();
    const result = value.match(GLUE_CODE_META_REGEX);
    const [, name, isDefault, path] = result ?? [];
    if (typeof name !== 'string' || typeof isDefault !== 'string' || typeof path !== 'string') {
        throw new Error(`Invalid glue code format: ${value}`);
    }
    return { name, path, isDefault: isDefault === 'true' };
}
class ModuleFunctionShape extends sdk_build_core_1.Shape {
    meta;
    constructor(source, meta) {
        super({ source });
        this.meta = meta;
    }
    toString(callExpressionProvider = (name) => `${name}()`, defaultParams) {
        const { name, path, isDefault, relativePath } = this.meta;
        const moduleParamNames = getFunctionParameters(this.getSource());
        const variables = (0, zip_1.default)(defaultParams ?? [], moduleParamNames).map(([defaultParam, moduleParamName]) => defaultParam ?? moduleParamName);
        const moduleCallExpression = callExpressionProvider(name).replace('{{PARAMS}}', variables.join(', '));
        const requirePath = relativePath ?? path;
        const code = isDefault
            ? `const ${name} = require('${requirePath}').default;\n${moduleCallExpression};`
            : `const { ${name} } = require('${requirePath}');\n${moduleCallExpression};`;
        return sdk_build_core_1.Shape.from(this, `${GLUE_CODE_PREFIX} ${name};${isDefault};${path}${GLUE_CODE_WARNING}\n${code}`)
            .asString()
            .withContentType('cdata');
    }
    equals(other) {
        if (other instanceof ModuleFunctionShape) {
            return (0, isEqual_1.default)(this.meta, other.meta);
        }
        const string = other instanceof sdk_build_core_1.StringShape ? other.getValue() : other;
        if (typeof string === 'string') {
            if (!isGlueCode(string)) {
                return false;
            }
            return (0, isEqual_1.default)(this.meta, parseMeta(string));
        }
        return super.equals(other);
    }
}
exports.ModuleFunctionShape = ModuleFunctionShape;
/**
 * Removes invalid control characters that would cause XML parsing errors.
 */
function sanitizeModuleContent(content) {
    return content.replace(utils_1.INVALID_XML_CHARACTERS, '');
}
function getEmitOutput(file, { fs, diagnostics, config, project, compiler, }) {
    const path = file.getFilePath();
    if (!path.endsWith('.ts')) {
        return file.getFullText();
    }
    const tsConfigPath = config.tsconfigPath ? project.resolvePath(config.tsconfigPath) : undefined;
    return compiler.compileModule(file, fs, diagnostics, tsConfigPath);
}
function getFunctionParameters(source) {
    if (!(source instanceof sdk_build_core_2.ts.VariableDeclaration || source instanceof sdk_build_core_2.ts.FunctionDeclaration)) {
        return [];
    }
    const node = source;
    let functionNode;
    if (sdk_build_core_2.ts.Node.isFunctionDeclaration(node)) {
        functionNode = node;
    }
    else if (sdk_build_core_2.ts.Node.isVariableDeclaration(node) && node.getInitializerIfKind(sdk_build_core_2.ts.SyntaxKind.FunctionExpression)) {
        functionNode = node.getInitializerIfKindOrThrow(sdk_build_core_2.ts.SyntaxKind.FunctionExpression);
    }
    else if (sdk_build_core_2.ts.Node.isVariableDeclaration(node) && node.getInitializerIfKind(sdk_build_core_2.ts.SyntaxKind.ArrowFunction)) {
        functionNode = node.getInitializerIfKindOrThrow(sdk_build_core_2.ts.SyntaxKind.ArrowFunction);
    }
    else {
        return [];
    }
    return functionNode.getParameters().map((param) => param.getName()) ?? [];
}
async function parseDeclaration(node, { transform, project, config }) {
    const result = await transform.toRecord(node.getSourceFile());
    if (!result.success) {
        return result;
    }
    const record = result.value;
    if (record.getTable() !== 'sys_module') {
        return { success: false };
    }
    const path = record.get('path').asString().getValue();
    if (sdk_build_core_2.NowConfig.legacyPackageResolution(config) || config.scope === 'global') {
        return {
            success: true,
            value: new ModuleFunctionShape(node, {
                name: node.getName() ?? 'functionModule',
                path: path,
                isDefault: node.isDefaultExport(),
            }),
        };
    }
    const originalPath = record.getSource().getPath();
    const relativePath = sdk_build_core_2.path.relative(project.getRootDir(), project.resolvePath(originalPath));
    return {
        success: true,
        value: new ModuleFunctionShape(node, {
            name: node.getName() ?? 'functionModule',
            path: path,
            relativePath: `./${relativePath}`,
            isDefault: node.isDefaultExport(),
        }),
    };
}
const dependencyIgnoreList = ['@servicenow/glide', '@servicenow/sdk'];
function isIgnoredDependency(name) {
    return dependencyIgnoreList.some((dependency) => name.startsWith(dependency));
}
function validateAndGetModuleSpecifier(name) {
    const scopedRegex = /@[a-z\d][\w\-.]+\/[a-z\d][\w\-.]*/gi;
    scopedRegex.test(name);
    const idx = scopedRegex.lastIndex;
    const isSubPathImport = name.indexOf('/', idx);
    if (isSubPathImport > 0) {
        return { name: name.slice(0, isSubPathImport), entry: name.slice(isSubPathImport + 1) };
    }
    return { name };
}
function isValidRequireCall(callExpression, requirePath) {
    const expression = callExpression.getExpression();
    const isRequire = sdk_build_core_2.ts.Node.isIdentifier(expression) && expression.getText() === 'require';
    const isRelativePath = requirePath.getLiteralText().startsWith('../') || requirePath.getLiteralText().startsWith('./');
    return isRequire && !isRelativePath;
}
class ModuleDependencyShape extends sdk_build_core_1.Shape {
    moduleName;
    constructor({ node, moduleName, }) {
        super({ source: node });
        this.moduleName = moduleName;
    }
    getModuleName() {
        return this.moduleName;
    }
}
exports.ModuleDependencyShape = ModuleDependencyShape;
// TODO: Need to have some invalidation mechanism. Maybe the plugin framework can provide plugins with a managed cache to use for stuff like this?
let DEPENDENCY_CACHE = {};
// Track external module record IDs for SBOM generation (not persisted to XML)
let EXTERNAL_MODULE_IDS = new Set();
// Expose cache clearing for tests
function clearDependencyCache() {
    DEPENDENCY_CACHE = {};
    EXTERNAL_MODULE_IDS = new Set();
}
function parseModuleDependency(node) {
    //Check if this is a type only import and skip it
    if (node.asKind(sdk_build_core_2.ts.SyntaxKind.ImportDeclaration)?.getImportClause()?.isTypeOnly()) {
        return { success: false };
    }
    let moduleName;
    if (sdk_build_core_2.ts.Node.isImportDeclaration(node) && !node.isModuleSpecifierRelative()) {
        moduleName = node.getModuleSpecifierValue();
    }
    else if (sdk_build_core_2.ts.Node.isExportDeclaration(node) && node.hasModuleSpecifier() && !node.isModuleSpecifierRelative()) {
        moduleName = node.getModuleSpecifierValue();
    }
    else if (sdk_build_core_2.ts.Node.isCallExpression(node)) {
        const args = node.getArguments();
        const requirePath = args[0];
        if (sdk_build_core_2.ts.Node.isStringLiteral(requirePath) && isValidRequireCall(node, requirePath)) {
            moduleName = requirePath.getLiteralValue();
        }
    }
    if (!moduleName || isIgnoredDependency(moduleName)) {
        return { success: false };
    }
    const cached = DEPENDENCY_CACHE[moduleName];
    if (cached && (!cached.success || !cached.value.getOriginalNode().wasForgotten())) {
        return cached;
    }
    return (DEPENDENCY_CACHE[moduleName] = {
        success: true,
        value: new ModuleDependencyShape({ node, moduleName }),
    });
}
function generateSBOMContent(context) {
    const { database } = context;
    // Query sys_module records that are tracked as external
    const moduleRecords = database.query('sys_module').filter((mod) => {
        const recordId = mod.getId().getValue();
        return EXTERNAL_MODULE_IDS.has(recordId);
    });
    const sbomBuilder = new sbom_builder_1.SBOMBuilder();
    for (const mod of moduleRecords) {
        const modulePath = mod.get('path').asString().getValue();
        if (modulePath.endsWith('/package.json')) {
            const moduleContent = mod.get('content').asString().getValue();
            sbomBuilder.addPackageJson(modulePath, moduleContent);
        }
    }
    return sbomBuilder.generateSBOM(context);
}
function getModuleDependencyPath(config, module) {
    const { name, file, packageJson, modulePath, version } = module;
    if (sdk_build_core_2.NowConfig.legacyPackageResolution(config)) {
        return sdk_build_core_2.NowConfig.moduleResolutionPath(config, packageJson, true, name, version, file);
    }
    if (modulePath) {
        return sdk_build_core_2.NowConfig.moduleResolutionPath(config, packageJson, true, modulePath, file);
    }
    return sdk_build_core_2.NowConfig.moduleResolutionPath(config, packageJson, true, exports.NODE_MODULES, name, file);
}
exports.ServerModulePlugin = sdk_build_core_1.Plugin.create({
    name: 'ServerModulePlugin',
    files: [
        {
            entryPoint: true,
            matcher: /[/\\]package\.json$/,
        },
        {
            entryPoint: true,
            matcher: (path, { config, project }) => !path.endsWith('.test.ts') &&
                !path.endsWith('.test.js') &&
                project.isInDir(config.serverModulesDir, path),
        },
    ],
    records: {
        sys_module: {
            toFile(record, context) {
                const { config } = context;
                // If the record is not the sbom, we defer to using the record plugin
                if (!record.get('path').ifString()?.endsWith('/bom.json')) {
                    return {
                        success: false,
                    };
                }
                const sbomContent = generateSBOMContent(context);
                return {
                    success: true,
                    value: {
                        source: record,
                        name: `sys_module_${record.getId().getValue()}.xml`,
                        category: record.getInstallCategory(),
                        content: `<?xml version="1.0"?>
<record_update>
  <sys_module action="INSERT_OR_UPDATE">
    <sys_id>${record.getId().getValue()}</sys_id>
    <sys_scope display_value="${config.scope}">${config.scopeId}</sys_scope>
    <path>${record.get('path').getValue()}</path>
    <external_source>${record.get('external_source').getValue()}</external_source>
    <content><![CDATA[${sdk_build_core_1.StringShape.escapeCdataTags(sanitizeModuleContent(sbomContent))}]]></content>
  </sys_module>
</record_update>`,
                    },
                };
            },
            toShape(record) {
                return { success: true, value: sdk_build_core_1.Shape.noOp(record) };
            },
        },
    },
    shapes: [
        {
            shape: sdk_build_core_1.SourceFileShape,
            fileTypes: ['module'],
            async toRecord(file, { factory, fs, diagnostics, project, config, packageJson, compiler }) {
                if (config.type === 'configuration') {
                    throw new Error(`Modules cannot be used in a configuration project`);
                }
                const path = file.getPath();
                if (!path.startsWith(project.resolvePath(config.serverModulesDir))) {
                    return { success: false };
                }
                const mappedPath = (0, utils_1.applyPathMappings)(path, config.modulePaths);
                const resolvedPath = project.resolvePath(mappedPath);
                const mappedFile = compiler.getSourceFile(resolvedPath) ?? compiler.addSourceFileAtPathIfExists(resolvedPath);
                if (!mappedFile) {
                    diagnostics.error(file, `Module path was mapped from '${path}' to '${resolvedPath}' but no file exists at the mapped location.`);
                    return { success: false };
                }
                const content = getEmitOutput(mappedFile, { fs, diagnostics, config, project, compiler });
                if (!content) {
                    return { success: false };
                }
                const relativePath = sdk_build_core_2.path.relative(project.getRootDir(), resolvedPath);
                const sysModulePath = sdk_build_core_2.NowConfig.moduleResolutionPath(config, packageJson, false, relativePath);
                return {
                    success: true,
                    value: await factory.createRecord({
                        source: file,
                        table: 'sys_module',
                        explicitId: relativePath.replaceAll(/[./\\]/g, '_'),
                        properties: {
                            // Module resolution at runtime requires this format
                            path: sysModulePath,
                            content: sdk_build_core_1.Shape.from(file, sanitizeModuleContent(content))
                                .asString()
                                .withContentType('cdata'),
                            external_source: false,
                            sys_name: sysModulePath,
                        },
                    }),
                };
            },
        },
        {
            shape: ModuleDependencyShape,
            fileTypes: ['module'],
            // TODO: When managed cache is provided to plugins, cache dependencies that were already handled to avoid reprocessing
            async toRecord(shape, context) {
                const { packageJson, diagnostics, fs, logger, project, factory, config } = context;
                if (config.type === 'configuration') {
                    throw new Error(`Modules cannot be used in a configuration project`);
                }
                const dependencies = packageJson.dependencies ?? {};
                const { name: parentName, entry } = validateAndGetModuleSpecifier(shape.getModuleName());
                const version = dependencies[parentName];
                if (!version) {
                    diagnostics.error(shape, `Dependency ${parentName} is not found in package.json`);
                    return { success: false };
                }
                const isTrusted = isModuleTrusted(parentName, config.trustedModules, logger);
                const id = `${parentName}@${version}`;
                const repack = await repack_1.RepackService.create(logger, fs, project.getRootDir());
                const dependencyNodes = await repack.execute({
                    id,
                    entry: entry ? [entry] : ['.'],
                    legacyPackageResolution: sdk_build_core_2.NowConfig.legacyPackageResolution(config),
                });
                if (!dependencyNodes) {
                    throw new Error(`Failed to build dependency ${id}`);
                }
                const modules = [];
                const { Lint } = await import('../repack/lint/index.js');
                for (const node of dependencyNodes) {
                    const { packagePath, files, updatedManifest, originalPath } = node;
                    const { name, version } = updatedManifest;
                    const { modulePath, idPath } = buildDependencyPackagePath(project.getRootDir(), name, version, originalPath);
                    for (const file of files) {
                        const fileContent = fs.readFileSync(sdk_build_core_2.path.join(packagePath, file)).toString('utf-8');
                        if (/(.js|.cjs|.mjs)$/.test(sdk_build_core_2.path.extname(file))) {
                            const result = await new Lint().check(fileContent);
                            if (result) {
                                logger.warn(`Use of unsupported APIs detected in npm dependency ${name}`);
                                logger.warn(result);
                            }
                        }
                        modules.push({
                            id: sdk_build_core_2.NowConfig.legacyPackageResolution(config)
                                ? `${name}@${version}/${file}`
                                : `${idPath}@${version}/${file}`,
                            path: getModuleDependencyPath(config, {
                                name,
                                file,
                                version,
                                packageJson,
                                modulePath,
                            }),
                            content: fileContent,
                        });
                    }
                }
                const records = [];
                for (const m of modules) {
                    const record = await factory.createRecord({
                        source: shape,
                        table: 'sys_module',
                        explicitId: m.id,
                        properties: {
                            path: m.path,
                            content: sdk_build_core_1.Shape.from(shape, sanitizeModuleContent(m.content))
                                .asString()
                                .withContentType('cdata'),
                            external_source: !isTrusted,
                            sys_name: m.path,
                        },
                    });
                    // Track external module IDs for SBOM generation
                    EXTERNAL_MODULE_IDS.add(record.getId().getValue());
                    records.push(record);
                }
                const [first, ...rest] = records;
                if (!first) {
                    return { success: false };
                }
                return {
                    success: true,
                    value: first.with(...rest),
                };
            },
        },
    ],
    nodes: [
        { node: 'CallExpression', fileTypes: ['module'], entryPoint: true, toShape: parseModuleDependency },
        { node: 'ImportDeclaration', fileTypes: ['module'], entryPoint: true, toShape: parseModuleDependency },
        { node: 'ExportDeclaration', fileTypes: ['module'], entryPoint: true, toShape: parseModuleDependency },
        { node: 'FunctionDeclaration', fileTypes: ['module'], toShape: parseDeclaration },
        {
            node: 'FunctionExpression',
            fileTypes: ['module'],
            toShape(node, context) {
                const parent = node.getParentIfKind(sdk_build_core_2.ts.SyntaxKind.VariableDeclaration);
                if (!parent) {
                    return { success: false };
                }
                return parseDeclaration(parent, context);
            },
        },
        {
            node: 'ArrowFunction',
            fileTypes: ['module'],
            toShape(node, context) {
                const parent = node.getParentIfKind(sdk_build_core_2.ts.SyntaxKind.VariableDeclaration);
                if (!parent) {
                    return { success: false };
                }
                return parseDeclaration(parent, context);
            },
        },
    ],
});
/**
 * Builds a relative dependency package path from an absolute path.
 * Handles special cases for monorepo dependencies and pnpm-style paths.
 *
 * @param rootDir - The project root directory
 * @param name - The package name
 * @param version - The package version
 * @param path - The absolute path to the package (optional)
 * @returns Relative path suitable for module resolution
 */
function buildDependencyPackagePath(rootDir, name, version, path) {
    let relative = sdk_build_core_2.path.relative(rootDir, path ?? '');
    const isMonorepoDependency = relative.includes(`../`);
    const isPnpm = relative.includes('.pnpm');
    if (isMonorepoDependency) {
        // Treat monorepo node_modules as if they are inside app node_modules
        relative = relative.replaceAll('../', '').replaceAll('+', '/');
    }
    if (isPnpm) {
        // Do our best to turn this pnpm path into an npm path
        relative = relative.replaceAll('+', '/').replaceAll(`node_modules/.pnpm/${name}@${version}/`, '');
    }
    // IDs will resemble the legacy paths and share sys_ids if possible
    const idPath = relative.replaceAll('node_modules/', '').replaceAll('.pnpm/', '');
    return {
        modulePath: relative,
        idPath,
    };
}
//# sourceMappingURL=index.js.map