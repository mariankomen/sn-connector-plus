"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DoInParallelInstanceShape = exports.AssignSubflowOutputsInstanceShape = exports.SetFlowVariablesInstanceShape = exports.TryCatchInstanceShape = exports.GoBackToInstanceShape = exports.ForEachInstanceShape = exports.ConditionalLogicInstanceShape = exports.FlowLogicInstanceShape = void 0;
exports.createFlowLogicInstanceShape = createFlowLogicInstanceShape;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const arrow_function_plugin_1 = require("../../arrow-function-plugin");
const now_id_plugin_1 = require("../../now-id-plugin");
const flow_logic_constants_1 = require("./flow-logic-constants");
const flow_shapes_1 = require("../utils/flow-shapes");
/**
 * Base class for flow logic constructs that use config + additional arguments pattern.
 * Provides common functionality for accessing config, sysId, and annotation.
 */
class FlowLogicInstanceShape extends flow_shapes_1.FDInstanceShape {
    getConfig() {
        return this.getArgument(0).as(sdk_build_core_1.ObjectShape);
    }
    getSysId() {
        return this.getConfig().get('$id').as(now_id_plugin_1.NowIdShape);
    }
    getAnnotation() {
        const annotationShape = this.getConfig().get('annotation');
        if (annotationShape?.isUndefined()) {
            return undefined;
        }
        return annotationShape?.asString() ?? undefined;
    }
}
exports.FlowLogicInstanceShape = FlowLogicInstanceShape;
/**
 * Shape class for conditional flow logic constructs (If/ElseIf/Else).
 * Handles the pattern: If/ElseIf/Else(config, body)
 *
 * @example
 * If({ $id: Now.ID['if_1'], condition: 'user.role === "admin"', label: 'Admin Check' }, () => { ... })
 * ElseIf({ $id: Now.ID['elseif_1'], condition: 'user.role === "user"' }, () => { ... })
 * Else({ $id: Now.ID['else_1'], annotation: 'Default case' }, () => { ... })
 */
class ConditionalLogicInstanceShape extends FlowLogicInstanceShape {
    getBody() {
        return this.getArgument(1).as(arrow_function_plugin_1.ArrowFunctionShape);
    }
    getCondition() {
        return this.getConfig().get('condition')?.asString();
    }
    getLabel() {
        return this.getConfig().get('label')?.asString();
    }
}
exports.ConditionalLogicInstanceShape = ConditionalLogicInstanceShape;
/**
 * Shape class for ForEach loop logic.
 * Handles the pattern: ForEach(config, items, body)
 *
 * @example
 * ForEach({ $id: Now.ID['foreach_1'], annotation: 'Process items' }, ['a', 'b', 'c'], (item) => { ... })
 */
class ForEachInstanceShape extends FlowLogicInstanceShape {
    getConfig() {
        return this.getArgument(1).as(sdk_build_core_1.ObjectShape);
    }
    getItems() {
        return this.getArgument(0, false).as(sdk_build_core_1.PropertyAccessShape);
    }
    getBody() {
        return this.getArgument(2).as(arrow_function_plugin_1.ArrowFunctionShape);
    }
}
exports.ForEachInstanceShape = ForEachInstanceShape;
/**
 * Shape class for GoBackTo navigation logic.
 * Handles the pattern: GoBackTo(config, step)
 *
 * @example
 * GoBackTo({ $id: Now.ID['goback_1'], annotation: 'Retry logic' }, previousStep)
 */
class GoBackToInstanceShape extends FlowLogicInstanceShape {
    getStep() {
        const step = this.getArgument(1);
        return step?.isRecord() ? step.asRecord() : step?.asString();
    }
}
exports.GoBackToInstanceShape = GoBackToInstanceShape;
/**
 * Shape class for TryCatch error handling logic.
 * Handles the pattern: TryCatch(config, handlers)
 *
 * @example
 * TryCatch({ $id: Now.ID['trycatch_1'] }, { try: tryFn, catch: catchFn })
 */
class TryCatchInstanceShape extends FlowLogicInstanceShape {
    getHandlers() {
        return this.getArgument(1)?.as(sdk_build_core_1.ObjectShape);
    }
    getTryHandler() {
        return this.getHandlers()?.get('try')?.as(arrow_function_plugin_1.ArrowFunctionShape);
    }
    getCatchHandler() {
        return this.getHandlers()?.get('catch')?.as(arrow_function_plugin_1.ArrowFunctionShape);
    }
}
exports.TryCatchInstanceShape = TryCatchInstanceShape;
/**
 * Shape class for SetFlowVariables logic.
 * Handles the pattern: SetFlowVariables(config, variables)
 *
 * @example
 * SetFlowVariables({ $id: Now.ID['set_flow_variables_1'] }, _params.flow_variables, { variable1: 'value1', variable2: 'value2' })
 */
class SetFlowVariablesInstanceShape extends FlowLogicInstanceShape {
    getVariablesToSet() {
        return this.getArgument(2)?.as(sdk_build_core_1.ObjectShape);
    }
    getVariablesToSetAsString() {
        const variables = this.getVariablesToSet();
        const entries = variables.entries({ resolve: false });
        return Array.from(entries, ([key]) => key).join(',');
    }
}
exports.SetFlowVariablesInstanceShape = SetFlowVariablesInstanceShape;
/**
 * Shape class for AssignSubflowOutputs logic.
 * Handles the pattern: AssignSubflowOutputs(config, outputs)
 *
 * @example
 * AssignSubflowOutputs({ $id: Now.ID['assign_subflow_outputs_1'] }, _params.outputs, { output1: 'value1', output2: 'value2' })
 */
class AssignSubflowOutputsInstanceShape extends FlowLogicInstanceShape {
    getAssignedSubflowOutputs() {
        return this.getArgument(2)?.as(sdk_build_core_1.ObjectShape);
    }
    getAssignedSubflowOutputsAsString() {
        const assignedSubflowOutputs = this.getAssignedSubflowOutputs();
        const entries = assignedSubflowOutputs.entries({ resolve: false });
        return Array.from(entries, ([key]) => key).join(',');
    }
}
exports.AssignSubflowOutputsInstanceShape = AssignSubflowOutputsInstanceShape;
/**
 * Shape class for DoInParallel execution logic.
 * Handles the pattern: DoInParallel(config, ...functions)
 *
 * @example
 * DoInParallel({ $id: Now.ID['do_in_parallel_1'] }, fn1, fn2, fn3)
 */
class DoInParallelInstanceShape extends FlowLogicInstanceShape {
    getParallelFunctions() {
        const functions = [];
        const allArgs = this.getArguments();
        // Start from argument 1 (after config) and collect all arrow functions
        for (let i = 1; i < allArgs.length; i++) {
            const arg = this.getArgument(i);
            if (arg) {
                functions.push(arg.as(arrow_function_plugin_1.ArrowFunctionShape));
            }
        }
        return functions;
    }
    getParallelFunctionCount() {
        return this.getArguments().length - 1; // Subtract 1 for config argument
    }
}
exports.DoInParallelInstanceShape = DoInParallelInstanceShape;
/**
 * Factory function to create the appropriate specific flow logic shape based on the logic type.
 * This provides type-safe access to flow logic constructs while maintaining backward compatibility.
 *
 * @param expr - The CallExpressionShape to convert
 * @param callee - The flow logic type (If, ElseIf, Else, ForEach, etc.)
 * @param args - The arguments for the flow logic construct
 * @returns The appropriate specific shape instance
 */
function createFlowLogicInstanceShape(expr, callee, args) {
    switch (callee) {
        case flow_logic_constants_1.FLOW_LOGIC.IF:
        case flow_logic_constants_1.FLOW_LOGIC.ELSEIF:
        case flow_logic_constants_1.FLOW_LOGIC.ELSE:
            return new ConditionalLogicInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.FOR_EACH:
            return new ForEachInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.GOBACKTO:
            return new GoBackToInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.TRY_CATCH:
            return new TryCatchInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.DO_IN_PARALLEL:
            return new DoInParallelInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.SET_FLOW_VARIABLES:
            return new SetFlowVariablesInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.ASSIGN_SUBFLOW_OUTPUTS:
            return new AssignSubflowOutputsInstanceShape(expr, callee, args);
        case flow_logic_constants_1.FLOW_LOGIC.WAIT_FOR_A_DURATION:
        case flow_logic_constants_1.FLOW_LOGIC.ENDFLOW:
        case flow_logic_constants_1.FLOW_LOGIC.EXITLOOP:
        case flow_logic_constants_1.FLOW_LOGIC.SKIP_ITERATION:
            return new FlowLogicInstanceShape(expr, callee, args);
        default:
            // Fallback to the deprecated generic shape for unknown types
            throw new Error(`Unknown flow logic type: ${callee}`);
    }
}
//# sourceMappingURL=flow-logic-shapes.js.map