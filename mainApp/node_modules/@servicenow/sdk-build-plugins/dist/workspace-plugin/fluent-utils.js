"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeActualRecordAsOverrideToTheDefault = exports.convertFieldValueToString = exports.doesActualRecordMatchGeneratedRecord = exports.buildRecordKey = exports.getExplicitIdGenerator = void 0;
const now_id_plugin_1 = require("../now-id-plugin");
const getExplicitIdGenerator = (source, appConfigId) => (table, uniqueString) => new now_id_plugin_1.NowIdShape({ source, id: `${appConfigId}_${table}_${uniqueString}` });
exports.getExplicitIdGenerator = getExplicitIdGenerator;
const buildRecordKey = (record) => `${record.getTable()}_${record.getId().getValue()}`;
exports.buildRecordKey = buildRecordKey;
/*
This is just an equality check similar to the existing "strictEqual", except it's a little more relaxed.
We want to make sure that our defaulted records aren't "tampered" with. If they are, this will detect it.

Record.strictEqual() tests that the number of properties match, which is too agressive for our needs, because
the number of properties on an actual record is almost always higher than on a generated record (due to sys_ columns, mostly).
*/
const doesActualRecordMatchGeneratedRecord = (actualRecord, generatedRecord) => {
    for (const [gFieldName, gFieldValue] of generatedRecord.entries()) {
        const generatedRecordValueAsString = (0, exports.convertFieldValueToString)(gFieldValue);
        const actualRecordValueAsString = (0, exports.convertFieldValueToString)(actualRecord.get(gFieldName));
        if (generatedRecordValueAsString !== actualRecordValueAsString) {
            return false;
        }
    }
    return true;
};
exports.doesActualRecordMatchGeneratedRecord = doesActualRecordMatchGeneratedRecord;
const convertFieldValueToString = (fieldValue) => {
    if (fieldValue.isRecord()) {
        return fieldValue.asRecord().getId().getValue();
    }
    else if (fieldValue.isBoolean()) {
        return fieldValue.asBoolean().getValue().toString();
    }
    else if (fieldValue.isNumber()) {
        return fieldValue.asNumber().getValue().toString();
    }
    return fieldValue.asString().getValue();
};
exports.convertFieldValueToString = convertFieldValueToString;
const serializeActualRecordAsOverrideToTheDefault = (actualRecord, generatedRecord) => {
    // Looping through the generated record keys gives us a more controllable and precise list to track/record.
    const serializedRecordValue = {};
    for (const field of generatedRecord.keys()) {
        const fieldValue = actualRecord.get(field);
        if (fieldValue.isRecord()) {
            serializedRecordValue[field] = fieldValue.asRecord().getId().getValue();
        }
        else {
            try {
                serializedRecordValue[field] = JSON.stringify(JSON.parse(fieldValue.asString().getValue())).replace(/\\/g, '\\\\');
            }
            catch (e) {
                serializedRecordValue[field] = fieldValue.asString().getValue();
            }
        }
    }
    return serializedRecordValue;
};
exports.serializeActualRecordAsOverrideToTheDefault = serializeActualRecordAsOverrideToTheDefault;
//# sourceMappingURL=fluent-utils.js.map