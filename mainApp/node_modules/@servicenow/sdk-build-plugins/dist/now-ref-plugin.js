"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NowRefPlugin = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const call_expression_plugin_1 = require("./call-expression-plugin");
exports.NowRefPlugin = sdk_build_core_1.Plugin.create({
    name: 'NowRefPlugin',
    nodes: [
        {
            node: 'CallExpression',
            async toShape(node, { transform, factory }) {
                const result = await transform.toShape(node, call_expression_plugin_1.CallExpressionPlugin);
                if (!result.success ||
                    !result.value.is(sdk_build_core_1.CallExpressionShape) ||
                    result.value.getCallee() !== 'Now.ref') {
                    return { success: false };
                }
                const callExpression = result.value;
                const table = callExpression
                    .getArgument(0)
                    .asString('The first argument to Now.ref() must be a table name');
                const guidOrKeys = callExpression
                    .getArgument(1)
                    .as([sdk_build_core_1.StringShape, sdk_build_core_1.ObjectShape], 'The second argument to Now.ref() must be a GUID, a Now ID string, or a coalesce keys object');
                const maybeKeys = callExpression.hasArgument(2)
                    ? callExpression
                        .getArgument(2)
                        .asObject('The third argument to Now.ref() must be a coalesce keys object')
                    : undefined;
                const [guid, keys] = maybeKeys
                    ? [guidOrKeys.asString(), maybeKeys] // Now.ref(table, guid, keys)
                    : guidOrKeys.isString()
                        ? [guidOrKeys.asString(), undefined] // Now.ref(table, guid)
                        : [undefined, guidOrKeys.asObject()]; // Now.ref(table, keys)
                return {
                    success: true,
                    value: await factory.createReference({ source: node, table, guid, keys }),
                };
            },
        },
    ],
});
//# sourceMappingURL=now-ref-plugin.js.map