"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestPlugin = void 0;
exports.serializeValueToRecord = serializeValueToRecord;
exports.serializeValueToShape = serializeValueToShape;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const now_id_plugin_1 = require("../now-id-plugin");
const arrow_function_plugin_1 = require("../arrow-function-plugin");
const xmlbuilder2_1 = require("xmlbuilder2");
const app_1 = require("@servicenow/sdk-core/runtime/app");
const step_configs_1 = require("./step-configs");
const sdk_build_core_2 = require("@servicenow/sdk-build-core");
exports.TestPlugin = sdk_build_core_1.Plugin.create({
    name: 'TestPlugin',
    records: {
        sys_atf_test: {
            relationships: {
                sys_atf_step: {
                    via: 'test',
                    descendant: true,
                    relationships: {
                        sys_variable_value: {
                            descendant: true,
                            via: 'document_key',
                        },
                        sys_element_mapping: {
                            descendant: true,
                            via: 'id',
                        },
                    },
                },
            },
            toShape(record, { descendants }) {
                const arrowParameter = getArrowFunctionParameter(record);
                const elementMappingRecords = descendants.query('sys_element_mapping');
                const stepsWhichShouldDeclareAVariable = getStepIdsThatAreBeingReferencedByOtherSteps(elementMappingRecords);
                const elementMappingValueMap = getFieldValuesFromElementMappingRecords(elementMappingRecords);
                const stepRecords = descendants
                    .query('sys_atf_step')
                    .sort((a, b) => a.get('order').toNumber().getValue() - b.get('order').toNumber().getValue());
                if (anyStepsAreUnknown(stepRecords, record.get('name').toString().getValue())) {
                    return { success: false };
                }
                const stepVarNames = getVariableNamesForSteps(stepRecords);
                const statements = stepRecords.map((step) => {
                    const stepConfigId = step.get('step_config').toString().getValue();
                    const stepId = step.getId().getValue();
                    const stepConfig = step_configs_1.ToShapeStepConfigs[stepConfigId];
                    const gemIdentifier = stepsWhichShouldDeclareAVariable.has(stepId)
                        ? stepVarNames.get(stepId)
                        : undefined;
                    const variables = descendants
                        .query('sys_variable_value', { document: 'sys_atf_step', document_key: stepId })
                        .map((svvRecord) => {
                        const variableShape = svvRecord.get('variable');
                        const variableInfo = stepConfig.variables[variableShape.getValue()];
                        if (!variableInfo) {
                            throw new Error(`sys_variable_value variable ${variableShape.getValue()} does not correspond to any known ATF input variables for step type '${stepConfig.name}'`);
                        }
                        const rawValue = elementMappingValueMap.get(`${stepId}:${variableInfo.field}`) ?? svvRecord.get('value');
                        const deserializedValue = serializeValueToShape(rawValue?.ifDefined()?.toString().getValue(), variableInfo.type, variableInfo.mandatory);
                        if (deserializedValue === undefined) {
                            return undefined;
                        }
                        const value = getElementMappingValues(svvRecord, deserializedValue, stepVarNames);
                        if (variableInfo.default !== undefined && value === variableInfo.default) {
                            // no need to set the value if its already default
                            return undefined;
                        }
                        const name = variableInfo.name;
                        return {
                            name,
                            value,
                        };
                    })
                        .filter((v) => v !== undefined)
                        .map((v) => ({ [v.name]: v.value }))
                        .reduce((acc, v) => ({ ...acc, ...v }), {});
                    const rawTimeoutValue = step.get('timeout').ifString()?.getValue();
                    const timeout = (0, sdk_build_core_2.parseGlideDuration)(rawTimeoutValue);
                    const callExpression = new sdk_build_core_1.CallExpressionShape({
                        source: step,
                        callee: `${arrowParameter}.${stepConfig?.callName}`,
                        args: [
                            new sdk_build_core_1.ObjectShape({
                                source: step,
                                properties: step.transform(({ $, merge }) => ({
                                    $id: $.val(now_id_plugin_1.NowIdShape.from(step)),
                                    active: $.toBoolean().def(true),
                                    description: $.def(''),
                                    notes: $.def(''),
                                    warning: $.from('warning_message').def(''),
                                    timeout: $.val(timeout),
                                    [merge]: $.val(variables).def({}),
                                })),
                            }),
                        ],
                    });
                    return gemIdentifier
                        ? new sdk_build_core_1.VariableStatementShape({
                            source: step,
                            variableName: gemIdentifier,
                            initializer: callExpression,
                        })
                        : callExpression;
                });
                const arrowFunction = statements.length > 0
                    ? new arrow_function_plugin_1.ArrowFunctionShape({
                        source: record,
                        parameters: [arrowParameter],
                        statements,
                    })
                    : undefined;
                return {
                    success: true,
                    value: new sdk_build_core_1.CallExpressionShape({
                        source: record,
                        callee: app_1.Test.name,
                        args: [
                            record.transform(({ $ }) => ({
                                $id: $.val(now_id_plugin_1.NowIdShape.from(record)),
                                name: $,
                                description: $.def(''),
                                active: $.toBoolean().def(true),
                                failOnServerError: $.from('fail_on_server_error').toBoolean().def(false),
                            })),
                            arrowFunction,
                        ].filter((arg) => arg),
                    }),
                };
            },
            async diff(existing, incoming, descendants, context) {
                return (0, sdk_build_core_1.deleteMultipleDiff)(existing, incoming, descendants, context);
            },
        },
        sys_variable_value: {
            coalesce: ['document', 'document_key', 'variable'],
        },
        sys_atf_step: {
            composite: true,
            toFile(step, { database, config }) {
                const recordUpdate = (0, xmlbuilder2_1.create)().ele('record_update', { table: 'sys_atf_step' });
                const stepElement = recordUpdate.ele('sys_atf_step', { action: step.getAction() });
                const stepId = step.getId().getValue();
                if (!stepId) {
                    throw new Error('Invalid step id');
                }
                stepElement.ele('sys_id').txt(stepId);
                stepElement.ele('sys_scope', { display_value: config.scope }).txt(config.scopeId);
                Object.entries(step.properties()).forEach(([key, value]) => {
                    stepElement.ele(key).txt(value.ifDefined()?.toString()?.getValue() ?? '');
                });
                const variableValues = database.query('sys_variable_value', {
                    document: 'sys_atf_step',
                    document_key: step.getId(),
                });
                recordUpdate.ele('sys_variable_value', {
                    action: 'delete_multiple',
                    query: `document_key=${stepId}`,
                });
                variableValues.forEach((varRecord) => {
                    const variableElement = recordUpdate.ele('sys_variable_value', { action: step.getAction() });
                    variableElement.ele('sys_id').txt(varRecord.getId().getValue());
                    Object.entries(varRecord.properties()).forEach(([key, value]) => {
                        variableElement.ele(key).txt(value instanceof sdk_build_core_1.UndefinedShape ? '' : value.toString().getValue());
                    });
                });
                const elementMappingRecords = database.query('sys_element_mapping', { id: stepId });
                if (elementMappingRecords.length > 0) {
                    recordUpdate.ele('sys_element_mapping', {
                        action: 'delete_multiple',
                        query: `id=${stepId}`,
                    });
                }
                elementMappingRecords.forEach((record) => {
                    const elementMappingElement = recordUpdate.ele('sys_element_mapping', {
                        action: step.getAction(),
                    });
                    elementMappingElement.ele('sys_id').txt(record.getId().getValue());
                    Object.entries(record.properties()).forEach(([key, value]) => {
                        elementMappingElement.ele(key).txt(value.toString().getValue());
                    });
                });
                return {
                    success: true,
                    value: {
                        source: step,
                        name: `sys_atf_step_${step.getId().getValue()}.xml`,
                        category: step.getInstallCategory(),
                        content: recordUpdate.end({ prettyPrint: true }),
                    },
                };
            },
        },
        sys_element_mapping: {
            coalesce: ['field', 'id'],
        },
    },
    shapes: [
        {
            shape: sdk_build_core_1.CallExpressionShape,
            fileTypes: ['fluent'],
            async toRecord(callExpression, { factory, diagnostics }) {
                if (callExpression.getCallee() !== app_1.Test.name) {
                    return { success: false };
                }
                const prop = callExpression.getArgument(0).asObject();
                const arrowFunc = callExpression.getArgument(1).if(arrow_function_plugin_1.ArrowFunctionShape);
                const testId = prop.get('$id');
                const steps = arrowFunc ? getStepInfoFromArrowFunction(arrowFunc, diagnostics) : [];
                if (steps.some((step) => !step)) {
                    return { success: false };
                }
                const testRecord = await factory.createRecord({
                    source: callExpression,
                    table: 'sys_atf_test',
                    explicitId: testId,
                    properties: prop.transform(({ $ }) => ({
                        name: $,
                        active: $.def(true),
                        description: $.def(''),
                        fail_on_server_error: $.from('failOnServerError').def(false),
                    })),
                });
                const stepRecords = await Promise.all(steps.map(async (step, index) => {
                    const rawTimeout = step.values.get('timeout');
                    const timeout = rawTimeout.isUndefined()
                        ? step.defaultTimeout
                        : convertTimeoutToSystemFormat(rawTimeout);
                    return await factory.createRecord({
                        table: 'sys_atf_step',
                        source: step.source,
                        explicitId: step.values.get('$id'),
                        properties: step.values.transform(({ $ }) => ({
                            active: $.toBoolean().def(true),
                            description: $,
                            display_name: $.val(step.displayName),
                            notes: $,
                            order: $.val(index + 1),
                            step_config: $.val(step.stepConfigId),
                            table: $.val(step.table),
                            test: $.val(testRecord.getId()),
                            timeout: $.val(timeout),
                            warning_message: $.from('warning'),
                        })),
                    });
                }));
                const stepAndVariableValueRecords = [];
                for (const [index, step] of steps.entries()) {
                    for (const [varName, atfVariableInfo] of Object.entries(step.variables)) {
                        const valueShape = step.values.get(varName, false);
                        let elementMappingRecord;
                        const value = createGemExpressionsFromShape(valueShape, stepRecords, diagnostics);
                        if (value !== undefined) {
                            elementMappingRecord = await factory.createRecord({
                                table: 'sys_element_mapping',
                                source: step.source,
                                properties: {
                                    field: atfVariableInfo.field,
                                    id: stepRecords[index]?.getId(),
                                    table: `var__m_atf_input_variable_${step.stepConfigId}`,
                                    value: serializeValueToRecord(value, atfVariableInfo.type, atfVariableInfo.default),
                                },
                            });
                        }
                        stepAndVariableValueRecords.push(await factory.createRecord({
                            table: 'sys_variable_value',
                            source: step.source,
                            properties: {
                                document: 'sys_atf_step',
                                document_key: stepRecords[index]?.getId(),
                                value: serializeValueToRecord(value ??
                                    valueShape
                                        .ifArray()
                                        ?.getElements()
                                        .map((v) => v.ifRecord()?.getId().getValue() ?? v.getValue()) ??
                                    valueShape.ifResolvable()?.resolve().ifRecord()?.getId().getValue() ??
                                    valueShape.getValue(), atfVariableInfo.type, atfVariableInfo.default),
                                variable: atfVariableInfo.inputVariableId,
                                order: atfVariableInfo.order,
                            },
                        }));
                        if (elementMappingRecord) {
                            stepAndVariableValueRecords.push(elementMappingRecord);
                        }
                    }
                }
                return {
                    success: true,
                    value: testRecord.with(...stepRecords, ...stepAndVariableValueRecords),
                };
            },
        },
    ],
});
const UNSUPPORTED_STEP_CONFIGS = {
    '38907e937322130007d738682bf6a742': 'Component State Validation (Custom UI)',
    '475e0de3d732130089fca2285e610361': 'Assert Text on Page (Custom UI)',
    b4758c7453370300c792ddeeff7b128d: 'Component Value Validation (Custom UI)',
    def25c4b73730300c79260bdfaf6a700: 'Click Component (Custom UI)',
    e5dd168473330300c79260bdfaf6a794: 'Set Component Values (Custom UI)',
    fc7e65d577332300e46abe41a9106106: 'Open Service Portal Page',
    '012105620fe2330091d0f00c97767ec4': 'Validate List UI Action Visibility',
    '0200ac2fe72003005c85cd19d2f6a942': 'Open a Record in List',
    '7bdce31387400300709861fb97cb0b5a': 'Validate Record Present in List',
    '8b84e5e837b1030064a52f3c8e41f170': 'Validate Related List Visibility',
    a69843f2531332007e7829cac2dc34d7: 'Apply Filter to List',
    c5f44934532332007e7829cac2dc342e: 'Click a List UI Action',
};
function getFieldValuesFromElementMappingRecords(elementMappingRecords) {
    return elementMappingRecords
        .filter((em) => em.get('value').getValue())
        .reduce((map, em) => {
        const field = em.get('field').getValue();
        const stepId = em.get('id').getValue();
        return map.set(`${stepId}:${field}`, em.get('value'));
    }, new Map());
}
function convertTimeoutToSystemFormat(timeoutShape) {
    const timeoutDuration = timeoutShape.ifObject()?.getValue();
    const timeoutDate = timeoutDuration && (0, sdk_build_core_2.durationFieldToXML)(timeoutDuration);
    return timeoutDate && (0, sdk_build_core_2.formatDateToPlatformFormat)(timeoutDate);
}
function anyStepsAreUnknown(stepRecords, testName) {
    return stepRecords.some((stepRecord) => {
        const stepConfigId = stepRecord.get('step_config').toString().getValue();
        if (!step_configs_1.ToShapeStepConfigs[stepConfigId]) {
            const stepConfigName = UNSUPPORTED_STEP_CONFIGS[stepConfigId] ?? stepConfigId;
            console.error(`Unable to transform ATF test '${testName}' because of unsupported step_config: '${stepConfigName}'`);
            return true;
        }
        return false;
    });
}
function getArrowFunctionParameter(record) {
    const source = record.getOriginalSource();
    return ((sdk_build_core_1.ts.Node.isNode(source) &&
        source.isKind(sdk_build_core_1.ts.SyntaxKind.CallExpression) &&
        source.getArguments()[1]?.asKind(sdk_build_core_1.ts.SyntaxKind.ArrowFunction)?.getParameters()[0]?.getName()) ||
        'atf');
}
function propertyAccessToGemExpression(propertyAccess, stepRecords, diagnostics) {
    const split = propertyAccess.getCode().split('.');
    if (!split[0]) {
        diagnostics.error(propertyAccess, `Invalid expression`);
        return undefined;
    }
    const sourceStep = stepRecords.find((rec) => rec.getSource() === propertyAccess.getFirstElement().resolve().if(sdk_build_core_1.CallExpressionShape));
    if (!sourceStep) {
        diagnostics.error(propertyAccess, `Invalid expression, expecting a reference to an ATF step`);
    }
    const stepId = sourceStep?.getId().getValue();
    const elementPath = split.slice(1).join('.');
    const suffix = elementPath ? `.${elementPath}` : '';
    return `{{step['${stepId}']${suffix}}}`;
}
function createGemExpressionsFromShape(shape, stepRecords, diagnostics) {
    if (shape instanceof sdk_build_core_1.PropertyAccessShape) {
        return propertyAccessToGemExpression(shape, stepRecords, diagnostics);
    }
    if (shape instanceof sdk_build_core_1.TemplateExpressionShape) {
        const gemExpressions = shape
            .getSpans()
            .map((span) => propertyAccessToGemExpression(span.getExpression().as(sdk_build_core_1.PropertyAccessShape), stepRecords, diagnostics))
            .reduce((acc, gemExpression, index) => ({ ...acc, [index]: gemExpression }), {});
        return shape.getValue(gemExpressions);
    }
    if (shape instanceof sdk_build_core_1.ObjectShape) {
        const obj = {};
        let gemExpressionFound = false;
        for (const key in shape.properties()) {
            const value = createGemExpressionsFromShape(shape.get(key, false), stepRecords, diagnostics);
            gemExpressionFound = gemExpressionFound || value !== undefined;
            obj[key] = value || shape.get(key).getValue();
        }
        return gemExpressionFound ? obj : undefined;
    }
    return undefined;
}
function getStepInfoFromArrowFunction(arrowFunc, diagnostics) {
    return arrowFunc
        .getStatements()
        .filter((statement) => statement instanceof sdk_build_core_1.CallExpressionShape || statement instanceof sdk_build_core_1.VariableStatementShape)
        .map((statement) => getStepInfoFromAtfCallExpression(statement, diagnostics))
        .filter((step) => step);
}
function getStepInfoFromAtfCallExpression(statement, diagnostics) {
    let callExpression;
    let identifier;
    if (statement instanceof sdk_build_core_1.VariableStatementShape) {
        callExpression = statement.getInitializer();
        identifier = statement.getVariableName().getName();
    }
    else if (statement instanceof sdk_build_core_1.CallExpressionShape) {
        callExpression = statement;
    }
    else {
        return undefined;
    }
    const [, category, funcName] = callExpression.getCallee().split('.');
    if (!category || !funcName) {
        diagnostics.info(callExpression, 'Invalid ATF step');
        return undefined;
    }
    const categoryKey = category;
    if (!step_configs_1.ToRecordStepConfigs[categoryKey]) {
        diagnostics.warn(callExpression, `Unknown category: ${category}`);
        return undefined;
    }
    const categorySteps = step_configs_1.ToRecordStepConfigs[categoryKey];
    if (!(funcName in categorySteps)) {
        diagnostics.warn(callExpression, `Unknown step: ${funcName}`);
        return undefined;
    }
    const stepConfig = categorySteps[funcName];
    const values = callExpression.getArgument(0).asObject();
    return {
        displayName: stepConfig.name,
        stepConfigId: stepConfig.stepConfigId,
        variables: stepConfig.variables,
        table: values.get('table')?.ifString(),
        source: callExpression,
        identifier,
        defaultTimeout: stepConfig.defaultTimeout,
        values,
    };
}
function getVariableNamesForSteps(stepRecords) {
    return stepRecords.reduce((map, step, index) => {
        const source = step.getOriginalSource();
        const name = (sdk_build_core_1.ts.Node.isNode(source) && source.getParentIfKind(sdk_build_core_1.ts.SyntaxKind.VariableDeclaration)?.getName()) ||
            `step${index + 1}`;
        return map.set(step.getId().getValue(), name);
    }, new Map());
}
const GemRegex = /{{step\[(?:'|.{6})(?<stepId>[0-9a-f]{32})(?:'|.{6})\]\.(?<elementPath>.*?)}}/gm;
function getStepIdsThatAreBeingReferencedByOtherSteps(elementMappingRecord) {
    return elementMappingRecord
        .flatMap((rec) => {
        const str = rec.get('value').toString().getValue();
        const matches = str.matchAll(GemRegex);
        return Array.from(matches).map((m) => {
            const [, stepId] = m;
            return stepId;
        });
    })
        .filter((stepId) => stepId !== undefined)
        .reduce((acc, stepId) => acc.add(stepId), new Set());
}
function variableElementPathToPropertyAccess(source, propertyAccessPath) {
    const elements = propertyAccessPath.split('.').map((e) => new sdk_build_core_1.IdentifierShape({ source, name: e }));
    return new sdk_build_core_1.PropertyAccessShape({
        source,
        elements: [elements[0], elements[1], ...elements.slice(2)],
    });
}
function getElementMappingValues(source, value, stepIdToVarName) {
    if (typeof value === 'object' && value !== null) {
        if (Array.isArray(value)) {
            return value.map((v) => getElementMappingValues(source, v, stepIdToVarName));
        }
        else {
            const obj = {};
            const valueObj = value;
            for (const key in valueObj) {
                obj[key] = getElementMappingValues(source, valueObj[key], stepIdToVarName);
            }
            return obj;
        }
    }
    const potentialGemExpression = typeof value === 'string' ? value : undefined;
    if (!potentialGemExpression) {
        return value;
    }
    const [matched] = Array.from(potentialGemExpression.matchAll(GemRegex));
    const simpleGemExpression = matched && matched[0] === potentialGemExpression;
    if (!matched) {
        return value;
    }
    // If the GEM expression is simple, it only contains the GEM
    // expression and nothing else. This means we can transform it
    // a PropertyAccessExpression, otherwise we'll need to create
    // a TemplateExpression
    if (simpleGemExpression) {
        const stepId = matched.groups['stepId'];
        const elementPath = matched.groups?.['elementPath'];
        const varName = stepIdToVarName.get(stepId);
        if (!varName) {
            return undefined;
        }
        const lastIdentifier = elementPath?.split('.')[0];
        if (!lastIdentifier) {
            throw new Error(`Invalid GEM expression found in: '${potentialGemExpression}'`);
        }
        return variableElementPathToPropertyAccess(source, `${varName}.${elementPath}`);
    }
    const matches = Array.from(potentialGemExpression.matchAll(GemRegex));
    if (!matches[0]) {
        return undefined;
    }
    const spans = [];
    const head = potentialGemExpression.substring(0, matches[0].index);
    for (let i = 0; i < matches.length; i++) {
        const m = matches[i];
        const gem = m[0];
        const nextGemIndex = matches[i + 1]?.index;
        const tail = potentialGemExpression.substring(m.index + gem.length, nextGemIndex);
        const stepId = m.groups?.['stepId'];
        const elementPath = m.groups?.['elementPath'] ?? '';
        const varName = stepIdToVarName.get(stepId);
        spans.push(new sdk_build_core_1.TemplateSpanShape({
            source,
            literalText: tail,
            expression: variableElementPathToPropertyAccess(source, `${varName}.${elementPath}`),
        }));
    }
    return new sdk_build_core_1.TemplateExpressionShape({
        source,
        literalText: head,
        spans: spans,
    });
}
function objectToEncodedQuery(objectShape) {
    return Object.entries(objectShape)
        .map(([key, value]) => {
        const escapedValue = value.toString().replaceAll('^', '^^');
        return `${key}=${escapedValue}`;
    })
        .concat('EQ')
        .join('^');
}
function serializeValueToRecord(value, type, defaultValue) {
    value = value === undefined ? defaultValue : value;
    switch (type) {
        case 'field_list':
        case 'glide_list':
            return Array.isArray(value) ? value.join(',') : value;
        case 'string': {
            return value ? value : undefined;
        }
        case 'simple_name_values': {
            if (typeof value !== 'object' || value === null || Object.keys(value).length === 0) {
                return '';
            }
            return JSON.stringify(value);
        }
        case 'template_value':
            return value && objectToEncodedQuery(value);
        case 'boolean':
            return value ? 1 : 0;
        default:
            return value;
    }
}
function convertToPrimitiveValue(value) {
    if (value === '') {
        return value;
    }
    if (value === 'true') {
        return true;
    }
    if (value === 'false') {
        return false;
    }
    const maybeNumber = Number(value);
    if (!isNaN(maybeNumber)) {
        return maybeNumber;
    }
    return value;
}
function queryToFieldValues(query) {
    const caratReplacement = '__CARATREPLACEMENTSTRING__';
    return query
        .replaceAll('^^', caratReplacement)
        .split('^')
        .filter((term) => term !== 'EQ')
        .map((term) => term.split('='))
        .filter(([, value]) => value !== undefined && value !== '')
        .map(([key, value]) => ({ [key]: convertToPrimitiveValue(value.replaceAll(caratReplacement, '^')) }))
        .reduce((acc, obj) => ({ ...acc, ...obj }), {});
}
// returning undefined here means the value won't appear on an initial transform
function serializeValueToShape(value, type, mandatory = false) {
    if (value === undefined) {
        return value;
    }
    switch (type) {
        case 'integer':
            return value ? parseInt(value, 10) : undefined;
        case 'glide_list':
        case 'field_list':
            if (!value) {
                return mandatory ? [] : undefined;
            }
            return value.split(',');
        case 'simple_name_values':
            if (!value && mandatory) {
                return {};
            }
            return value ? JSON.parse(value) : undefined;
        case 'template_value':
            return queryToFieldValues(value);
        case 'boolean':
            return value === '1';
        default:
            return mandatory ? value : value || undefined;
    }
}
//# sourceMappingURL=test-plugin.js.map