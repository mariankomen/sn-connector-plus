import type { DependencyNode, RepackOptions } from '@servicenow/sdk-repack';
export declare function checkModuleExists(module: string, logger: RepackOptions['logger'], fs: RepackOptions['fs'], workingDir: RepackOptions['workingDir']): Promise<boolean>;
export declare function resolveModule(module: string, logger: RepackOptions['logger'], fs: RepackOptions['fs'], workingDir: RepackOptions['workingDir']): Promise<string | undefined>;
export declare const REPACK_OUTPUT_DIR: string;
export declare class RepackService {
    private repack;
    private logger;
    private fs;
    private workingDir;
    private constructor();
    static create(logger: RepackOptions['logger'], fs: RepackOptions['fs'], workingDir: RepackOptions['workingDir']): Promise<RepackService>;
    execute(input: Omit<RepackOptions, 'logger' | 'fs' | 'workingDir'>): Promise<DependencyNode[] | undefined>;
    getResolvedDepDetails(dependency: string): Promise<{
        name: any;
        version: any;
    }>;
    checkModuleExists(module: string): Promise<boolean>;
    resolveModule(module: string): Promise<string | undefined>;
}
