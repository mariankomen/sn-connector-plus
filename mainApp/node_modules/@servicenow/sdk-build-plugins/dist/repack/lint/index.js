"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Lint = void 0;
const eslint_1 = require("eslint");
const eslint_formatter_stylish_1 = __importDefault(require("eslint-formatter-stylish"));
const Rules_1 = require("./Rules");
class Lint {
    linter;
    config;
    async initESLint() {
        if (!this.linter) {
            this.linter = new eslint_1.Linter();
            this.linter.defineRules(await (0, Rules_1.getPluginModules)());
            this.config = await (0, Rules_1.getEslintConfig)();
        }
        return { linter: this.linter, config: this.config };
    }
    async check(fileContent) {
        await this.initESLint();
        const messages = this.linter.verify(fileContent, this.config);
        return this.outputLintingResults(messages);
    }
    outputLintingResults(messages) {
        const stats = messages.reduce((result, message) => {
            if (message.fatal) {
                result.fatalErrorCount++;
            }
            if (message.severity === 1) {
                result.warningCount++;
                if (message.fix) {
                    result.fixableWarningCount++;
                }
            }
            if (message.severity === 2) {
                result.errorCount++;
                if (message.fix) {
                    result.fixableErrorCount++;
                }
            }
            return result;
        }, {
            errorCount: 0,
            fatalErrorCount: 0,
            warningCount: 0,
            fixableErrorCount: 0,
            fixableWarningCount: 0,
        });
        const results = {
            filePath: '',
            messages,
            suppressedMessages: [],
            usedDeprecatedRules: [],
            ...stats,
        };
        const problems = results.errorCount + results.warningCount + results.fatalErrorCount;
        if (problems <= 0) {
            return;
        }
        return eslint_formatter_stylish_1.default([results]);
    }
}
exports.Lint = Lint;
//# sourceMappingURL=index.js.map