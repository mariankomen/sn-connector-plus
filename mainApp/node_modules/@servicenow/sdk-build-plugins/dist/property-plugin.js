"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PropertyPlugin = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const sdk_build_core_2 = require("@servicenow/sdk-build-core");
const now_id_plugin_1 = require("./now-id-plugin");
const getRolesArray = (roles) => {
    return roles
        .split(',')
        .map((role) => role.trim())
        .filter((role) => role);
};
exports.PropertyPlugin = sdk_build_core_2.Plugin.create({
    name: 'PropertyPlugin',
    records: {
        sys_properties: {
            toShape(record, { config }) {
                return {
                    success: true,
                    value: new sdk_build_core_2.CallExpressionShape({
                        source: record,
                        callee: 'Property',
                        args: [
                            record.transform(({ $ }) => ({
                                $id: $.val(now_id_plugin_1.NowIdShape.from(record)),
                                name: $.from('name', 'suffix').map((name, suffix) => name.ifString() ??
                                    (config.scope === 'global'
                                        ? suffix
                                        : `${config.scope}.${suffix.asString().getValue()}`)),
                                value: $,
                                description: $.def(''),
                                type: $.def('string'),
                                choices: $.map((c) => {
                                    const value = c.toString().getValue();
                                    return value
                                        .split(/[,\n]/)
                                        .map((v) => v.trim())
                                        .filter((v) => v);
                                }).def([]),
                                isPrivate: $.from('is_private').toBoolean().def(false),
                                ignoreCache: $.from('ignore_cache').toBoolean().def(false),
                                roles: $.from('read_roles', 'write_roles')
                                    .def({ read: [], write: [] })
                                    .map((r, w) => ({
                                    read: r.ifString()?.pipe((v) => getRolesArray(v.getValue())),
                                    write: w.ifString()?.pipe((v) => getRolesArray(v.getValue())),
                                })),
                            })),
                        ],
                    }),
                };
            },
        },
    },
    shapes: [
        {
            shape: sdk_build_core_2.CallExpressionShape,
            fileTypes: ['fluent'],
            async toRecord(callExpression, { config, diagnostics, factory }) {
                if (callExpression.getCallee() !== 'Property') {
                    return { success: false };
                }
                const prop = callExpression.getArgument(0).asObject();
                const scope = config.scope;
                const name = prop.get('name').asString();
                if (!(0, sdk_build_core_1.isSNScope)(scope) && config.scope !== 'global' && !name.asString().startsWith(`${scope}.`)) {
                    diagnostics.error(name.getOriginalNode(), `Property name must begin with '${scope}.'`);
                }
                return {
                    success: true,
                    value: await factory.createRecord({
                        source: callExpression,
                        table: 'sys_properties',
                        explicitId: prop.get('$id'),
                        properties: prop.transform(({ $, merge }) => ({
                            name: $,
                            suffix: $.from('name').map((v) => v.asString().replace(new RegExp(`^${config.scope}\\.`), '')),
                            description: $,
                            type: $.def('string'),
                            value: $.toString(), // TODO: Should be allowed to assign non-strings to string properties
                            is_private: $.from('isPrivate').def(false),
                            ignore_cache: $.from('ignoreCache').def(false),
                            choices: $.map((v) => v.ifArray()?.getValue().join(',')),
                            [merge]: $.from('roles')
                                .def({ read_roles: '', write_roles: '' })
                                .map((v) => v
                                .ifObject()
                                ?.transform(({ $ }) => ({
                                read_roles: $.from('read').map((r) => getRolesString(r)),
                                write_roles: $.from('write').map((r) => getRolesString(r)),
                            }))
                                .properties()),
                        })),
                    }),
                };
            },
        },
    ],
});
function getRolesString(roles) {
    return roles
        .ifArray()
        ?.pipe((r) => Array.from(new Set(r.getElements().map((r) => r.ifRecord()?.get('name').getValue() ?? r.getValue()))).join(','));
}
//# sourceMappingURL=property-plugin.js.map