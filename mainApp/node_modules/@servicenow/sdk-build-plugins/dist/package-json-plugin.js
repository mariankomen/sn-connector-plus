"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PackageJsonPlugin = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const repack_1 = require("./repack");
const json_plugin_1 = require("./json-plugin");
exports.PackageJsonPlugin = sdk_build_core_1.Plugin.create({
    name: 'PackageJsonPlugin',
    shapes: [
        {
            shape: json_plugin_1.JsonFileShape,
            async toRecord(file, { config, packageJson, project, factory, fs, logger }) {
                if (file.getBaseName() !== 'package.json' || config.type === 'configuration') {
                    return { success: false };
                }
                const relativePath = sdk_build_core_1.path.relative(project.getRootDir(), file.getPath());
                const sysModulePath = sdk_build_core_1.NowConfig.moduleResolutionPath(config, packageJson, false, relativePath);
                const sbomRecord = await factory.createRecord({
                    source: file,
                    table: 'sys_module',
                    explicitId: 'bom_json',
                    properties: {
                        path: sdk_build_core_1.NowConfig.moduleResolutionPath(config, packageJson, false, 'bom.json'),
                        external_source: false,
                    },
                });
                return {
                    success: true,
                    value: (await factory.createRecord({
                        source: file,
                        table: 'sys_module',
                        explicitId: relativePath.replaceAll(/[./\\]/g, '_'),
                        properties: {
                            // Module resolution at runtime requires this format
                            path: sysModulePath,
                            content: sdk_build_core_1.Shape.from(file, await fixDependencyVersion(file.getJson().asObject(), {
                                fs,
                                logger,
                                rootDir: project.getRootDir(),
                            }))
                                .asString()
                                .withContentType('cdata'),
                            external_source: false,
                            sys_name: sysModulePath,
                        },
                    })).with(sbomRecord),
                };
            },
        },
    ],
});
async function fixDependencyVersion(json, context) {
    const deps = json.get('dependencies').ifObject();
    if (!deps) {
        return JSON.stringify(json.getValue(), null, 2);
    }
    const repack = await repack_1.RepackService.create(context.logger, context.fs, context.rootDir);
    const resolvedDeps = {};
    for (const dep of deps.keys()) {
        const { name, version } = await repack.getResolvedDepDetails(dep);
        resolvedDeps[name] = version;
    }
    return JSON.stringify({
        ...json.getValue(),
        dependencies: resolvedDeps,
    }, null, 2);
}
//# sourceMappingURL=package-json-plugin.js.map