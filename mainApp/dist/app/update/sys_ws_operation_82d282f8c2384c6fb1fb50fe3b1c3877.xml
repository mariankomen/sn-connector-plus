<?xml version="1.0"?>
<record_update table="sys_ws_operation">
  <sys_ws_operation action="INSERT_OR_UPDATE">
    <sys_id>82d282f8c2384c6fb1fb50fe3b1c3877</sys_id>
    <sys_scope display_value="x_peekl_salesfor_0">c0c895442faccb503ee3116e8ea4e308</sys_scope>
    <sys_update_name>sys_ws_operation_82d282f8c2384c6fb1fb50fe3b1c3877</sys_update_name>
    <active>true</active>
    <consumes>application/json,application/xml,text/xml</consumes>
    <consumes_customized>false</consumes_customized>
    <default_operation_uri>/api/x_peekl_salesfor_0/x_peekl_salesfor_0_salesforce_integratio/salesforce/objects/delete</default_operation_uri>
    <enforce_acl>064a8e03949c49f88bb8321ee55fd0bb</enforce_acl>
    <http_method>POST</http_method>
    <name>/salesforce/objects/delete</name>
    <operation_script><![CDATA[(function process(request, response) {
    response.setContentType('application/json');
    
    try {
        var body = request.body;
        var data;
        var isBatch = false;
        
        
        if (typeof body === 'string') {
            body = JSON.parse(body);
        }
        
        
        if (Array.isArray(body)) {
            data = body;
            isBatch = true;
        } else if (body.data) {
            
            if (Array.isArray(body.data)) {
                data = body.data;
                isBatch = true;
            } else {
                data = [body.data];
            }
        } else if (body.sys_id || body.sf_object_name) {
            
            data = [body];
        } else if (body.objects && Array.isArray(body.objects)) {
            
            data = body.objects;
            isBatch = true;
        } else {
            data = [body];
        }

        
        if (!data || !Array.isArray(data) || data.length === 0) {
            response.setStatus(400);
            response.setBody({
                success: false,
                error: 'No valid data provided. Expected array of objects with sys_id or sf_object_name'
            });
            return;
        }

        
        var results = [];
        var successCount = 0;
        var failCount = 0;

        for (var i = 0; i < data.length; i++) {
            var item = data[i];
            var result = {
                index: i
            };

            
            var sysId = item.sys_id;
            var objectName = item.sf_object_name;

            if (!sysId && !objectName) {
                result.success = false;
                result.error = 'Missing required field: sys_id or sf_object_name';
                result.sys_id = sysId || null;
                result.sf_object_name = objectName || null;
                failCount++;
                results.push(result);
                continue;
            }

            
            var objectConfigGr = new GlideRecordSecure('x_peekl_salesfor_0_salesforce_object_config');
            var found = false;

            if (sysId) {
                objectConfigGr.addQuery('sys_id', sysId);
                objectConfigGr.query();
                found = objectConfigGr.next();
                result.sys_id = sysId;
            }

            if (!found && objectName) {
                objectConfigGr = new GlideRecordSecure('x_peekl_salesfor_0_salesforce_object_config');
                objectConfigGr.addQuery('sf_object_name', objectName);
                objectConfigGr.query();
                found = objectConfigGr.next();
                result.sf_object_name = objectName;
            }

            if (!found) {
                result.success = false;
                result.error = 'Object configuration not found';
                failCount++;
                results.push(result);
                continue;
            }

            
            var deletedObjectName = objectConfigGr.getValue('sf_object_name');
            var deletedSysId = objectConfigGr.getValue('sys_id');

            
            try {
                objectConfigGr.deleteRecord();
                result.success = true;
                result.sys_id = deletedSysId;
                result.sf_object_name = deletedObjectName;
                result.message = 'Object removed successfully';
                successCount++;
            } catch (deleteErr) {
                result.success = false;
                result.error = deleteErr.message || String(deleteErr);
                failCount++;
            }

            results.push(result);
        }

        
        var allSuccess = failCount === 0;
        var statusCode = allSuccess ? 200 : (successCount > 0 ? 207 : 400); 

        response.setStatus(statusCode);
        response.setBody({
            success: allSuccess,
            batch: isBatch,
            total: data.length,
            succeeded: successCount,
            failed: failCount,
            message: allSuccess ? 
                (isBatch ? successCount + ' objects removed successfully' : 'Object removed successfully') :
                (successCount + ' succeeded, ' + failCount + ' failed'),
            results: results
        });

    } catch (err) {
        response.setStatus(500);
        response.setBody({
            success: false,
            error: err.message || String(err)
        });
    }
})(request, response);

]]></operation_script>
    <operation_uri>/api/x_peekl_salesfor_0/x_peekl_salesfor_0_salesforce_integratio/salesforce/objects/delete</operation_uri>
    <produces>application/json,application/xml,text/xml</produces>
    <produces_customized>false</produces_customized>
    <relative_path>/salesforce/objects/delete</relative_path>
    <request_example/>
    <requires_acl_authorization>true</requires_acl_authorization>
    <requires_authentication>true</requires_authentication>
    <requires_snc_internal_role>true</requires_snc_internal_role>
    <short_description>Batch delete Salesforce objects from configuration (accepts array of objects with sys_id or sf_object_name)</short_description>
    <sys_policy/>
    <web_service_definition>25cee077e5594db7b73b41a697f46061</web_service_definition>
    <web_service_version/>
  </sys_ws_operation>
</record_update>